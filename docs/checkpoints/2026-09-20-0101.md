# Checkpoint — 2026-09-20 01:01

## Session summary

Продолжение большой многосессионной серии по декомпозиции/рефакторингу форка `rush`. В начале этой сессии обнаружилось, что локальный каталог `D:\dev\go\crush` был случайно удалён целиком. Первая реакция (объявить всю работу прошлых сессий потерянной) оказалась ОШИБОЧНОЙ: проверка `git merge-base --is-ancestor` на свежем клоне показала, что все коммиты прошлых сессий (включая всю декомпозицию `runTurn`/`ExecuteRun`, MCP two-tier API, файловый лимит ≤1000 строк, `golangci-lint` чистку) уже являются предками текущего `origin/main` — они были запушены в какой-то более ранней, не увиденной мной сессии, и `main` с тех пор ушёл на ~30 коммитов вперёд за счёт другой, не связанной работы. Ложная попытка восстановления (агент `@sm` в worktree на "утерянную" задачу) была вовремя остановлена, worktree/branch/babysit-крон снесены, задачи помечены удалёнными.

После этого пользователь напрямую поставил 3 новые задачи через `/babygoal` с явным разрешением на под-агентов (`@sm`) и коммиты между задачами:
1. Прозрачный retry не срабатывал на watchdog-stall/rate-limit ошибках, если турн уже успел частично что-то написать (`turnMadeProgress` guard в `shouldRetryTurn` блокировал ретрай именно в этом, самом частом реальном случае).
2. В UI-блоке суб-агента (`SubAgentBlock.tsx`) вызовы инструментов показывали только голое имя (`bash bash bash...`), без аргументов и времени.
3. В заголовке блока суб-агента не было видно модель/effort.

Плюс два followup `/task`, добавленных пользователем по ходу и слитых в существующий план вместо дублирования:
4. Время сообщений видно только при наведении (hover) — нужно всегда.

Все 4 задачи (`feat-1`..`feat-4`) выполнены серийно: для каждой — отдельный `@sm`-агент в изолированном git worktree, я лично проверял диф + гонял build/vet/lint/test независимо от отчёта агента, затем fast-forward мерж в `main` и удаление worktree/ветки. По ходу feat-1 нашёл и подтвердил ревёрт-чеком реальный механизм (не просто "похоже на баг"): `shouldContinueTurn` — новый путь, который вместо слепого повтора промпта отправляет continuation-промпт с цитатой частичного ответа, когда транзиентная ошибка ударила уже после того, как модель что-то написала. По feat-2/3/4 всё прошло чище — независимая проверка (`pnpm typecheck`/`build`/`lint`) на каждом шаге, диффы разумные и узкие.

Активная цель (`/goal "решить все задачи"`) была поставлена пользователем на середине этой работы — сейчас формально удовлетворена (TaskList пуст), Stop-hook должен снят.

Затем пользователь сказал "пуш, зеленый Ci" — явное разрешение на пуш (впервые в этой сессии). Запушил 4 фиче-коммита. Дальше начался длинный цикл стабилизации CI:

- `security` (grype/anchore) упала от внешнего сетевого сбоя (`connection reset` при скачивании БД уязвимостей anchore.io) — перезапустил джобу, прошла.
- `build (windows-latest)` упала на тесте `internal/agent/tools/mcp` (~20 минут до этого шага в матрице). НЕ связано с 4 запушенными фичами. Реальный механизм: `process_windows_test.go`'s `TestMain` форсил `RUSH_DISABLE_DEFAULT_PROVIDERS=1` через `os.Setenv` для ВСЕГО пакета на Windows (Go разрешает один `TestMain` на пакет/бинарник) ради ОДНОГО теста (`TestConfigureStdioProcess_CancelTreeKillsOrphanedGrandchild_Windows`), что ломало другой тест (`TestOwnerCloseCancelsBlockedStartupBeforeCleanup`), ожидающий доступности дефолтных провайдеров. Перенёс переменную в тот единственный тест через `t.Setenv` (коммит `87ee064f`). Запушил, перезапустил CI.
- Тот же build упал СНОВА, но на ДРУГОМ тесте: `TestStdioDiagnosticCommandPreservesStartupAttributes` сравнивал путь строкой, а Windows-раннер вернул короткое DOS 8.3-имя (`RUNNER~1`) вместо длинного (`runneradmin`) для того же каталога — `filepath.EvalSymlinks` не нормализует short/long имена. Заменил сравнение на `os.Stat`+`os.SameFile` (идентичность файла, не написание пути), добавил `extractQuotedField` для разбора `dir="..."` из диагностического сообщения. Полный прогон пакета локально после этого фикса упал на ТРЕТЬЕМ отдельном тесте: `TestInitializeRejectsChangedConfigBeforePublication` — общий хелпер `awaitMCPSignal`/`awaitMCPError` (5-секундный таймаут, используется в 85 местах пакета) оказался слишком тесным для реального HTTP MCP-хендшейка под нагрузкой — воспроизвёл ЛОКАЛЬНО (1 из 3 прогонов упал, один прошедший занял 8.35с — уже больше старого лимита). Поднял таймаут до 15с через новую константу `mcpAwaitTimeout` (коммит `64409ab3`). Оба фикса прошли: 5/5 прогонов упавшего теста, 2 подряд чистых прогона всего пакета локально. Запушено.

На момент этого чекпоинта: жду результата третьего CI-прогона (после `64409ab3`) — ушёл в фон, обычный цикл `ScheduleWakeup` на ~5 минут проверяет статус. `gh` сейчас временно недоступен (сетевой сбой на этой машине — DNS/connection к api.github.com/github.com), последняя реальная проверка CI была до пуша `64409ab3`.

**Важный паттерн этого хвоста сессии**: пакет `internal/agent/tools/mcp` (уже задокументирован в fork-CLAUDE.md как "самый дефект-плотный") явно накопил НЕСКОЛЬКО независимых, ранее скрытых флейков (TestMain env-leak, short/long path сравнение, тесный shared-таймаут), которые `-failfast` в CI вскрывал строго по одному за прогон. Не исключено, что при следующем прогоне всплывёт ЕЩЁ один — если так, продолжать той же методологией (найти реальный механизм через логи + локальную репродукцию, узкий таргетированный фикс, ревёрт-чек/повторные прогоны, коммит, пуш, следующий CI-цикл), не откладывать.

## Active goal

`/goal "решить все задачи"` был активен, сейчас TaskList пуст — формально удовлетворена, Stop-hook должен был автоматически сняться. Если сессия увидит, что хук всё ещё активен при пустом TaskList — это баг хука, не повод изобретать фиктивные задачи.

## TaskList

Пусто (все feat-1..feat-4 выполнены и смержены; временные restore-* задачи от ложной тревоги удалены; #931 (пуш) закрыт де-факто, поскольку пуш состоялся явно в этой сессии).

## Decisions

- Не восстанавливать "утерянную" работу — она не была утеряна, `git merge-base --is-ancestor` на свежем клоне это доказал прежде, чем были потрачены реальные усилия на дублирование.
- retry-фикс (feat-1): не убирать `turnMadeProgress`-guard целиком (это задокументированная защита от дублирования работы), а добавить ОТДЕЛЬНЫЙ путь (`shouldContinueTurn`) специально для транзиентных ошибок с частичным прогрессом — continuation-промпт, а не слепой повтор.
- CI-флейки в `internal/agent/tools/mcp` чинить по одному, по реальному механизму, с локальной репродукцией перед фиксом — не расширять таймауты/допуски вслепую (хотя в двух случаях правильным фиксом ОКАЗАЛОСЬ именно расширение таймаута — но только после подтверждения, что причина падения — тайминг под нагрузкой, а не логическая ошибка).
- `security`-джоб (grype/anchore) — сетевой сбой к внешнему сервису, просто перезапустить, не чинить код.

## Open questions

- Итоговый статус CI-прогона после `64409ab3` — не подтверждён на момент чекпоинта (сеть у `gh` временно недоступна на этой машине; запланирован автоматический wakeup для проверки).
- Если всплывёт ЕЩЁ один флейк в `internal/agent/tools/mcp` на следующем прогоне — чинить той же методологией, не считать это поводом откатывать что-либо из уже сделанного.

## Repo state

```
(чистое дерево, ничего не staged/modified)
```
```
64409ab3 fix(mcp): stop two more Windows CI flakes uncovered by the TestMain fix
87ee064f fix(mcp): stop leaking RUSH_DISABLE_DEFAULT_PROVIDERS to every Windows test
10e32c56 feat(web): always show message timestamps, not just on hover
924add8a feat(web): show model and effort in sub-agent block header
14dd4a54 feat(web): show tool arguments and timestamps in sub-agent blocks
57fffb05 fix(agent): continue-retry transient turn failures after partial output
ccc2f9b5 test(session): bound busy retry probe
57adf1e2 test(session): shorten busy retry probe
```

Все 6 коммитов этой сессии (`57fffb05`..`64409ab3`) запушены в `origin/main`. Активный `CronList`: один одноразовый wakeup на 01:05 для проверки CI-статуса после `64409ab3`. Рабочих worktree не осталось — все смержены и удалены (feat-1..feat-4).
