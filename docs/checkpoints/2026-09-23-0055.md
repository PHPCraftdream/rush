# Checkpoint — 2026-09-23 00:55

## Session summary

Long continuing session. Prior arc (already covered by an earlier checkpoint, `docs/checkpoints/2026-09-22-1829.md`): round-7 P2 batch (17 findings) fixed and merged into primary, then round-8 audit found 2 P1 + 4 P2 (1 new P1 regression `R8-2`/`R8-3` plus 3 residuals), all fixed. That whole combined batch (round-7 + round-8, 53 files) was committed as `53e18976` and **pushed** to `origin/main` — this was the first commit/push of this session, done only after the user's explicit "коммит, пуш, зеленый CI" instruction. CI was verified fully green on GitHub Actions after two harmless reruns of transient/unrelated Windows flakes in `build (windows-latest)` (one in `internal/cmd/sessions_locks_test.go`, one in `internal/session/p578_stale_error_test.go` — both pre-existing, untouched by this session's diff, confirmed via local pre-push hook runs and `gh run rerun`). One GENUINE (not environmental) Windows race was found and fixed during this arc: three CONNECT-tunnel test stubs in `internal/nettransport/audit_round7_test.go` answered before reading the client's CONNECT request, leaving it unread in the socket receive buffer at Close() — reliably reproducible as "connection forcibly closed by the remote host" under concurrent package load (200/200 repro without the fix, 200/200 clean with `drainConnectRequest`).

Immediately after that push+CI-green report, the user invoked `/babygoal`+`/wrush` for a NEW round-9 audit report (`docs/reviews/2026-09-22-weekly-commit-audit-round9.md`, committed `b8aeaea4`, report-only, not pushed): 1 P1 (**R9-1** — a genuine regression from THIS session's own R8-3 `CallResultRecorder` fix) + 5 P2 (**R9-2**, **R8-3 residual**, **F6 residual**, **R4-2 residual**, **F8 residual**). User explicitly confirmed fixing all 6 now (same AskUserQuestion pattern as round 8).

All 6 round-9 findings are now fixed and verified, task-by-task:
- **#992 R9-1 (P1) + R8-3 residual (P2)** — done personally (deep continuation of my own R8-3 work): `sessionAgent.runOwned`'s dispatcher loop (`agent_run.go`) now carries `call.OnAssistantMessageCreated` onto any accepted in-process replacement (`InterruptAndReplace`/`drainAfterCancel`) so the caller's `CallResultRecorder` observes the REPLACEMENT's own row, not the superseded original's canceled one. `coordinator.RunWithReservedOwnership` (the FailIfSessionBusy+override entry point, which bypasses `runInternal` entirely) now also wires the recorder. `app_run_terminal.go`'s tool-call collection now stops at `ownID`'s row (`pastOwn` flag) so a later independent caller's own tool calls can't leak into this reconciliation's `ToolCalls` even when `FinalText` was already correctly scoped.
- **#995 F6 residual (P2)** — delegated via `/wrush` (worktree `f6-pinned-rebuild-snapshot`), zero-trust verified and merged. `rebuildPinnedModel` now takes `cfg *config.Config` as a caller-supplied parameter instead of taking its own second, separately-timed `c.cfg.Snapshot()`; new `rebuildInputs` helper returns both `cfg` and `providerCfg` from ONE snapshot for the sub-agent/summarize rebuild callers. Found and fixed 3 real bugs in the worker's own new tests during verification: (1) two tests wrongly called `t.Parallel()` while also calling a helper that does `t.Setenv` (Go forbids the combination); (2) a fixture panicked with nil-map-write because its `&config.Config{}` literal never initialized `Providers` via `csync.NewMap`; (3) the sub-agent 401-retry test's `OAuthToken` had no `ExpiresAt`, so `IsExpired()==true` made `runSubAgent`'s PROACTIVE refresh pre-check fire before the first provider call ever happened, and separately its retry trigger (`refreshApiKeyTemplate`) would have clobbered the test's own "concurrent reload" publish (switched the fixture to `refreshOAuth2TokenFn` + non-expired token instead).
- **#996 R4-2 residual (P2)** — delegated via `/wrush` (worktree `r4-2-dial-budget`), zero-trust verified and merged cleanly, no bugs found. New `perAddressTimeout(remaining, candidates)` splits the shared 60s `resolvedDialBudget` evenly across not-yet-tried candidates (capped at the existing 30s `transportDialTimeout`) instead of giving every attempt the full 30s regardless of how many are left — closes the gap where two slow (not instantly-refused) early addresses could exhaust the whole budget before a reachable third (e.g. IPv6) address ever got tried.
- **#993 R9-2 (P2)** — done personally. `coordinator.RunWithOverrides` does session-level smart/fast inheritance (an omitted slot inherits the session's durable override) on LOCAL variables only; the 401-rebuild path (`resolveCallModels`, via `modelOverridesFrom(ctx)`) read whatever the ORIGINAL caller had armed on ctx before calling `RunWithOverrides` (e.g. `app_run_setup.go`'s `(nil, fastOverride)` for a `--fast`-only CLI run, or the reviewer pass's `(reviewerOverride, nil)`), never the locally-inherited pair — so a single omitted slot silently reset to the GLOBAL config default on every credential-refresh rebuild. Fix: `RunWithOverrides` now re-arms `ctx = WithModelOverrides(ctx, smart, fast)` with the augmented pair before both the first build and `runInternal`. First test attempt was a false negative (didn't pre-arm ctx the way real callers do, so it accidentally exercised a DIFFERENT, already-correct fallback path `resolveSessionModels`) — corrected by explicitly arming `ctx` with the pre-inheritance pair before calling `RunWithOverrides`, matching real production wiring; then reproduced the bug cleanly on revert.
- **#994 F8 residual (P2)** — done personally. `executeRunLoop.foldLiveToolCallCounts` (the reconciliation-failure fallback) had an early `if len(s.toolCallCounts) == 0 { return }` guard that skipped BOTH the (correctly-empty) folding loop AND the `toolCallCounts = invocationToolCalls` aliasing step — so a reviewer phase refused before creating any assistant row (e.g. peak-hours refusal at turn start, `coordinator_run.go`'s `checkPeakHours`) left the final envelope's `ToolCalls` empty even though the primary phase's already-reconciled inventory was sitting untouched in `invocationToolCalls`. Fix: removed the early return; the aliasing step now always runs.

Every one of the 6 fixes has its own new regression test, and every test was revert-checked (temporarily reverting the fix, confirming the exact expected failure, restoring). After each task: full serial `go test` re-run of every affected package (`internal/agent`, `internal/app`, `internal/nettransport`) — all green throughout. A final combined `go build ./...` + `go vet ./...` (clean except the same pre-existing, untouched `internal/csync` vet warning already noted in the round-7/8 checkpoint) + `golangci-lint run` on the three touched packages (0 issues) were done. A final combined serial `go test ./internal/agent/... ./internal/app/... ./internal/nettransport/...` was launched in the background (task id `bjovaanm2`) right before this checkpoint fired — **still running, result not yet seen**. babysit cron `296d8674` was deleted once the round-9 TaskList went empty.

## Active goal

None (`/goal` not armed).

## TaskList

Empty — all 5 round-9 tasks (#992–#996) completed and verified this session.

## Decisions

- Committed and pushed the combined round-7+round-8 batch (`53e18976`) only after the user's explicit "коммит, пуш, зеленый CI" — never before. Nothing has been committed since; round-9's fixes are still uncommitted, awaiting the same kind of explicit instruction.
- Ran the full `.githooks/pre-push` hook locally (build + cross-GOOS/mips64 lint + file-size/ASCII/db-release guards + full serial test suite) as a CI mirror before pushing, since `core.hooksPath` isn't wired to auto-run it — found and fixed a genuine (not flaky) `noctx` lint violation and the real CONNECT-stub Windows race this way, both before push.
- Treated two DIFFERENT unrelated test failures across consecutive local pre-push reruns (`internal/cmd/sessions_locks_test.go`, then `internal/session/p578_stale_error_test.go`) as CI-runner-load jitter, not regressions — neither file is in this session's diff, both passed in later reruns, and this pattern (a different pre-existing racy test each time) is evidence of transient contention rather than one deterministic bug.
- For round-9's P2 batch, again used AskUserQuestion (same as round-8) since the triggering `/wrush` message itself didn't say "fix everything" — user again picked "fix all now."
- Partitioned round-9's 6 findings by file-touch overlap: R9-1+R8-3-residual+R9-2+F8-residual all cluster in `coordinator_run.go`/`agent_run.go`/`app_run_reviewer.go`/`app_run_terminal.go` and were done personally, sequentially, to avoid parallel-worktree merge conflicts and because they needed deep continuity with my own same-session R8-3 work; F6-residual (`coordinator_providers.go`+subagents+summarize) and R4-2-residual (`nettransport` only) were cleanly separable and delegated via `/wrush`.

## Open questions

- Whether to commit/push round-9's fixes now — **not yet asked this turn**; per standing session rule, will not commit without explicit instruction. All 6 findings are verified green individually; the combined final serial test run across all three touched packages was still in flight when this checkpoint was written.

## Repo state

```
 M internal/agent/agent_run.go
 M internal/agent/coordinator_providers.go
 M internal/agent/coordinator_run.go
 M internal/agent/coordinator_subagents.go
 M internal/agent/coordinator_summarize.go
 M internal/app/app_run_r8_test.go
 M internal/app/app_run_reviewer.go
 M internal/app/app_run_reviewer_tool_inventory_test.go
 M internal/app/app_run_terminal.go
 M internal/nettransport/client.go
 D web/dist/.gitkeep
?? internal/agent/coordinator_pinned_rebuild_snapshot_test.go
?? internal/agent/r8_3_reserved_ownership_result_identity_test.go
?? internal/agent/r9_1_live_replacement_result_identity_test.go
?? internal/agent/r9_2_overrides_rebuild_inheritance_test.go
?? internal/nettransport/audit_round9_test.go
```
```
b8aeaea4 docs: add weekly release readiness audit round 9
53e18976 fix(agent,app,nettransport,config,oauth): round-7 P2 batch + round-8 P1/P2 fixes
abc98c00 docs: add weekly release readiness audit round 8
d67a9c7e checkpoint: 2026-09-22-1829
0c4bae10 docs: add weekly release readiness audit round 7
```

Note: `web/dist/.gitkeep` shows as deleted — pre-existing from session start, protected by CLAUDE.md's own instruction (do not commit either way without explicit instruction), untouched throughout.
