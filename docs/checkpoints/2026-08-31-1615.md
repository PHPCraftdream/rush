# Checkpoint — 2026-08-31 16:15

## Session summary

Продолжение марафонской сессии по embeddable SDK (`sdk/` пакет, PHPCraftdream/rush форк, репо `D:\dev\go\crush`). Эта секция сессии началась с `/babygoal`-подобного запроса пользователя разобрать ревью `docs/reviews/2026-08-31-latest-work-overview.md`, затем плавно переросла в итеративную разработку embeddable SDK по прямым уточнениям пользователя в чате (не по единой заранее написанной спецификации) — пользователь пошагово формулировал требования (per-call credentials, конкурентная мультитенантность, library mode без rush.json, эфемерные in-memory сессии, session-busy fail-fast, поле Origin, методы чтения истории), а я исследовал код, уточнял неоднозначности через AskUserQuestion и решал каждое через `rush run` + git worktree изоляцию, строго последовательно (задачи #810–#821), потому что почти все трогали один и тот же `sdk/sdk.go`.

По пути обнаружены и исправлены 2 реальные, ранее существовавшие гонки в `internal/agent` (shared tool-object mutation, `errgroup.Group` reuse) — найдены выделенным `oh`-агентом (Opus, effort=high) в отдельном расследовании, не мной напрямую. Вся серия задач #810–#821 завершена, закоммичена (12 коммитов, HEAD на тот момент `88735973`), README и `sdk/README.md` написаны.

**Ключевой поворот**: пользователь запустил внешнее ревью (`/rush`-агентом в отдельном research-репозитории) результата всей этой работы — `docs/reviews/2026-08-31-sdk-library-review-round-1.md` (репо `D:/dev/go/crush-research-upstream-20260830`, НЕ этот репозиторий). Ревью нашло **P0 "release-blocking" дефект** (R1-1): `ExecuteRun` мутирует SHARED, НЕ session-keyed поля coordinator/permission-сервиса (`activeModelRole`, `maxCost`/`maxTokens`/`allowPeakHours`, `runAllowlistGate`) МЕЖДУ резолвингом сессии и стартом turn-горутины — то есть заявленная "мультитенантность" `RunWithCredentials` (моя же работа этой сессии) на самом деле НЕ изолирует permission-policy/budget/timeout/model-role между конкурентными тенантами, только provider-креды. Лично перепроверил чтением кода — подтверждено на 100% (`internal/permission/permission.go:293`, `s.runAllowlistGate.load()` не использует `opts.SessionID` вообще). Плюс ещё 5 находок P1 (R1-2..R1-6) и 2 P2 (R1-7, R1-8) — все тоже перепроверены и признаны реальными.

Пользователь принял 2 явных решения: (1) взять R1-1 в этот же раунд, не откладывать (это P0/security-гейт); (2) R1-6 (отсутствие tenant-авторизации сессий) закрыть ЧЕСТНОЙ ДОКУМЕНТАЦИЕЙ, а не новой инфраструктурой capability/namespace.

Изначально я decompose'ил все 8 находок в 7 последовательных задач (#822 R1-1, #823 R1-2+R1-3, #824 R1-4, #825 R1-5, #826 R1-6, #827 R1-7, #828 R1-8) со строгой цепочкой `blockedBy` — но пользователь ПРЕРВАЛ меня в процессе (перед запуском первого `rush run`) с новой явной инструкцией: **перегруппировать задачи так, чтобы каждая группа была самодостаточна для ОДНОГО агента, и запускать их СРАЗУ ПАРАЛЛЕЛЬНО** (не последовательно, как было заведено весь предыдущий ход сессии). Перегруппировал по непересекающимся файловым множествам:
- **Группа 1** (#822, объединяет R1-1+R1-4): `internal/agent/*`, `internal/app/app_run.go`+`app_run_gates.go`, `internal/permission/*` — самая рискованная (concurrency-критичный код, тот же класс проблем что уже дважды ловил `-race` этой сессией).
- **Группа 2** (#829, объединяет R1-2+R1-3): `sdk/library_mode.go`, `sdk/sdk.go`'s `Close()` — уникальная in-memory БД на клиент + forced-close не должен рвать живые writer'ы.
- **Группа 3** (#825, объединяет R1-5+R1-6): `internal/agent/credentials.go`, `sdk/README.md` — строгая валидация CredentialSet (fail-closed вместо тихого fallback на shared creds) + честная документация trust-model.
- **Группа 4** (#827, объединяет R1-7+R1-8): `sdk/sdk.go`'s `Wrap`/`RunNonInteractive`/`Open`-валидация, `internal/cmd/run.go` — убрать internal-only seams из публичного API + lifecycle/validation hardening.

Из-за ограничения `TaskUpdate` (нет способа СНЯТЬ уже выставленный `blockedBy`) задачи #823/#824/#826/#828 были УДАЛЕНЫ (`status: deleted`) и их содержание влито в #822/#825/#827, плюс создана новая чистая #829 без унаследованной зависимости.

Все 4 группы помечены `in_progress` одновременно и запущены как 4 параллельных `rush run --role smart --agents single --timeout 60m` в изолированных git worktree (`D:/dev/go/crush-wt/review3-group{1,2,3,4}`, ветки `review3/group{1,2,3,4}`, все от базы `88735973`). Каждому агенту явно названы соседи и их file-scope, запрещены git-write операции. `babysit` cron перевооружён (`0d3a2724`, каждые 15 мин) — был снят раньше, когда TaskList опустел между раундами.

**Незакоммиченное состояние в основном репо** (обнаружено при подготовке этого чекпоинта, НЕ забыть при следующем коммите): `internal/message/origin_test.go` и `internal/session/origin_test.go` изменены (не закоммичены) — это правка форматирования doc-комментария package-уровня, сделанная ДО текущего review-round-2 раунда, во время подготовки к `коммит/пуш/ci`: `go run mvdan.cc/gofumpt@latest`/`golangci-lint --fix` испортили комментарий про `DEFAULT ''` (SQL) в typographic-кавычку (Unicode U+201D) — откатил через `git checkout --`, вручную переформулировал текст комментария, избегая проблемной последовательности `''` ("DEFAULT ''." → "empty-string default."), перепроверил `golangci-lint run` (0 issues). Пользователь прервал команду `bash .githooks/pre-push` (полный прогон CI-зеркала) новым запросом про review — **пуш и полный pre-push прогон НЕ выполнены**, коммит с этим фиксом тоже не сделан. Нужно вернуться к `коммит + пуш + CI` после того как review-round-2 (4 группы) будет смержена.

Ожидаю завершения всех 4 параллельных `rush run` (ID фоновых Bash-задач: `bvvhwkjia`, `bh6tzy2fu`, `bbgn29b3w`, `b9iidbdgg`). При мерже буду ОСОБО внимателен к пересечениям на `sdk.go`/`sdk/README.md` между группами 2/3/4 (все три трогают `sdk.go` в разных функциях — вручную сверю через `Edit`, а не слепой `cp`, если диффы реально пересекаются построчно).

## Active goal

Нет активного `/goal` Stop hook.

## TaskList

### in_progress
- #822 Группа 1 (R1-1 P0 + R1-4 P1): per-call execution context + атомарный session admission
- #825 Группа 3 (R1-5 P1 + R1-6 P1): fail-closed CredentialSet + честный trust-model доклад
- #827 Группа 4 (R1-7 P2 + R1-8 P2): убрать internal-only seams + lifecycle/validation hardening
- #829 Группа 2 (R1-2 P1 + R1-3 P1): уникальная per-client in-memory БД + forced-close не рвёт живые writer'ы

### pending
(нет)

### recently completed
- #821 sdk: метод получения полной истории сообщений сессии (Client.Messages/Session)
- #820 session/message: поле Origin (browser/sdk/cli), задаётся при создании
- #819 sdk: написать документацию по embeddable SDK
- #818 sdk: Run/RunWithCredentials фейлятся сразу, если сессия уже занята (не queue)
- #817 sdk: library mode — Open без rush.json, опциональный WorkingDir, эфемерные сессии
- #816 sdk: Open создаёт WorkingDir, если каталога нет на диске
- #815 agent: добавить готовую команду resume в сообщения о таймаут-остановках
- #814 SDK: конкурентная мультитенантная инъекция кредов+ролей через CredentialSet
- #813 CI: batched race coverage для internal/agent + отдельный cumulative-leak smoke run
- #812 DB pairing guard: fail-closed при ошибке git grep/repo discovery

(удалены как поглощённые слиянием групп: #823, #824, #826, #828 — их содержание перенесено в #822/#825/#827/#829)

## Decisions

- Взять R1-1 (P0, самый крупный и рискованный фикс сессии) в этот же раунд, не откладывать отдельно — пользователь явно подтвердил через AskUserQuestion.
- R1-6 (отсутствие tenant-авторизации сессий) закрыть честной документацией существующего положения дел, НЕ строить capability/namespace-инфраструктуру — явный выбор пользователя.
- Перегруппировать 8 находок ревью в 4 параллельные, файлово-непересекающиеся группы вместо 7 последовательных задач — по прямой инструкции пользователя, прервавшей исходный план на середине.
- При невозможности снять `blockedBy` через `TaskUpdate` — удалить и пересоздать задачу с нуля, а не оставлять "фантомную" зависимость.
- Каждая группа объединяет 2 находки ревью, естественно связанные тематически и по файлам (R1-1+R1-4 — concurrency в exec-пути; R1-2+R1-3 — ephemeral DB lifecycle; R1-5+R1-6 — доверие к CredentialSet; R1-7+R1-8 — публичная поверхность API).

## Open questions

Нет открытых вопросов пользователю — оба решения по R1-1 и R1-6 уже получены явно.

## Repo state

```
 M internal/message/origin_test.go
 M internal/session/origin_test.go
 D web/dist/.gitkeep
?? docs/checkpoints/2026-08-26-2015.md
?? docs/checkpoints/2026-08-27-0749.md
?? docs/checkpoints/2026-08-28-1210.md
?? docs/checkpoints/2026-08-28-1349.md
?? docs/checkpoints/2026-08-30-0859.md
?? docs/checkpoints/2026-08-30-1730.md
?? docs/checkpoints/2026-08-30-2015.md
?? docs/checkpoints/2026-08-31-0130.md
?? docs/checkpoints/2026-08-31-1615.md
?? docs/plans/2026-08-27-agent-tree-backend-requirements-response.md
```
```
88735973 docs(sdk): write embeddable SDK guide
ae277c26 feat(sdk): Client.Messages/Session -- read history and metadata on demand
8f2f7104 feat(session,message): tag sessions and messages with their entry channel (Origin)
c335537f feat(sdk): Run/RunWithCredentials fail fast on an already-busy session
c0d54298 feat(sdk): library mode -- Open without rush.json, ephemeral in-memory sessions
```

Активные worktree review-round-2: `D:/dev/go/crush-wt/review3-group{1,2,3,4}` (все от базы `88735973`), с запущенными фоновыми `rush run` сессиями `review3-group{1,2,3,4}`. `fork/main` НЕ синхронизирован с локальным HEAD — пуш не выполнялся с начала review-round-2 (и есть незакоммиченный gofumpt-фикс, см. summary выше).
