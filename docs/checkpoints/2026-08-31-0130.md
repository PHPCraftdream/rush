# Checkpoint — 2026-08-31 01:30

## Session summary

Continuation of the marathon CI-flake-chasing session on `D:\dev\go\crush` (PHPCraftdream/rush fork). This window opened with the user asking to delete the entire TaskList (all items were already resolved from the prior window's CI-green milestone) and to add a standing rule to the global `CLAUDE.md`: all test failures and flakes must be fixed in place, in the same turn, until genuinely resolved — never deferred as a "fix later" task. That rule was added under the `## Важно:` section of `C:\Users\Computer\.claude\CLAUDE.md`.

The user then invoked `/babygoal` with a concrete assignment: read a code review at `D:\dev\go\crush-research-upstream-20260830\docs\reviews\2026-08-30-flaky-ci-commit-review.md` (a research worktree, NOT the main repo — confirmed read-only, never modified), decompose its recommendations into grouped tasks, and solve them via `/crush` (interpreted as this repo's own `rush` CLI/skill) combined with git worktree isolation for parallel execution. Instructions: commit progressively during the work, then after all tasks are done, commit, push, get CI fully green, and stop.

I verified every claim in the review against the live repo (all accurate), then decomposed into 4 tasks, each scoped to a disjoint file set to allow safe parallel worktree execution: (1) fix two remaining `conn.Close()`-instead-of-`db.Release()` leaks in `internal/agent/tools/{bash,run_command}_test.go`; (2) rewrite `TestP2_1_SummarizeQueueDrainedFromNonWebPath`'s mock SSE server from a fixed `time.Sleep` to a channel-based handshake; (3) make `TestRunTurn_DisarmsWatchdogOnErrorReturn_NotJustSuccessPath` explicitly wait for its `hangingTitleModel`'s background goroutine to exit instead of assuming a 1s bound is short enough; (4) a combined CI-consolidation task (all three sub-items touch `.github/workflows/build.yml`, so bundled into one to avoid parallel-worktree merge conflicts) — a new `db.Connect`/`db.Release` pairing guard script mirrored into both `.githooks/pre-push` and `build.yml`, an honesty addendum on the now-stale `docs/lessons/2026-08-30-ci-goroutine-leak.md`, and a brand-new targeted `-race` CI job (ubuntu-only, scoped to `internal/hooks` + `internal/session`, with `internal/agent` deliberately excluded and the reasoning documented inline — local `-race` runs of `internal/agent` alone hit `go test`'s own 10-minute per-package timeout, a time constraint independent of the Windows-specific memory constraints found in earlier sessions).

Armed `/babysit` (15-minute interval, job `87cd60c9`), created tasks #806–#809, marked all four `in_progress` simultaneously (parallel-worktree strategy), created 4 git worktrees under `D:\dev\go\crush-wt\flaky-review-*` (careful to avoid colliding with several PRE-EXISTING, unrelated worktrees already present there — `review-cache-diag`, `review-h1-hooks`, `review-k1k2k3` at an older commit, and several `.claude/worktrees/agent-*` — none of which were touched), and launched 4 parallel `rush run --role smart --agents single` sessions, each `--cwd`-scoped to its own worktree, each prompted with an explicit file-scope boundary, a ban on git writes (orchestrator does all git operations), and a mandate to self-verify before reporting. All 4 completed; each diff was independently re-verified (not trusted) by running the relevant tests myself, then copied from the worktree into the main working tree and committed as its own commit (`faabfe07`, `ce81b165`, `455d797a`, task #809's changes).

**The most significant event this window**: verifying task #809's new `-race` job surfaced a genuine, previously-undiscovered data race in `internal/hooks` — a read of the package-level `runShell`/`abandonSeam` test-seam variables (inside a goroutine spawned by `runOne`) racing against a later test's cleanup restoring them, reproducible under load (~1-in-25-to-50 full-package `-race` runs) despite whole seconds of real wall-clock time separating the accesses in every practical run (the Go memory model doesn't credit wall-clock separation without an actual happens-before edge). This was NOT one of the 4 assigned tasks but per the user's own newly-added rule ("fix flakes on the spot"), I stayed on it until genuinely resolved. It took three iterative fix attempts to actually close, each incrementally revealing a NEW, previously-hidden mechanism: (1) added a `sync.RWMutex` (`seamMu`) guarding both variables with `getRunShell()`/`getAbandonSeam()` as the only production read sites and `setRunShellForTest()`/`setAbandonSeamForTest()` as the only test write sites — necessary but insufficient; (2) moved both production reads to happen SYNCHRONOUSLY (captured into a local variable) before spawning the inner goroutine, so the read is provably ordered by `Run()`'s own `sync.WaitGroup` join rather than depending on inner-goroutine scheduling — still insufficient; (3) finally found the actual remaining culprit: `TestRunnerAbandonRaceSafety`'s own `wg.Add(1)` ran INSIDE the swapped stub closure, a textbook "Add-after-Wait" `sync.WaitGroup` misuse (the WaitGroup docs explicitly call this pattern unsound and Go's runtime specifically race-instruments to catch it) — fixed with an explicit `started` channel signaled AFTER `wg.Add(1)` (ordering mattered: an earlier attempt signaled BEFORE `Add`, which merely moved the same race into the gap between the two statements) that the test now waits on before proceeding toward its own `wg.Wait()` in cleanup. Verified clean over 125+ repetitions (50x + 75x) after the final fix, plus the actual CI-job command (`go test -race -short -failfast ./internal/hooks/ ./internal/session/`) run 15x locally (needed to correct my own verification methodology twice along the way — two apparent "failures" during this verification were actually just MY OWN test commands hitting `go test`'s default 10-minute per-package timeout when repeating a whole package 15x, misread initially as timeouts/races before checking the actual `FAIL ... 600.xxxs`/`900.xxxs` timing signature and goroutine-dump format, which confirmed they were plain timeouts, not races).

Committed the hooks race fix as its own commit (`61c17e22`), then finished integrating task #809's remaining files, verified the new guard script actually catches violations (ran clean against the already-fixed tree) and that the exact new CI `-race` job command passes locally, committed (`da53fe42`), cleaned up all 4 task worktrees and their branches (`git worktree remove --force` + `git branch -D`), pushed all 5 commits (`49d2c2ef..da53fe42`) to `fork/main` — the full local pre-push hook suite passed cleanly first try — and confirmed via `gh run watch` + `gh api` that the resulting CI run (`33343111270`) went fully green across all 7 jobs (`build` × 3 OS, `test-agent` × 3 OS, and the brand-new `test-race`). Deleted the `/babysit` cron (`87cd60c9`) since the TaskList was empty, per the skill's own self-cleanup contract. The user's full request ("commit, push, make CI green, and stop") was satisfied and reported; the session then received this `/checkpoint` command with no further work in flight.

## Active goal

None. No `/goal` Stop hook is armed. The `/babysit` cron (`87cd60c9`) was explicitly deleted once the TaskList emptied, per its own self-cleanup contract — there is no active heartbeat.

## TaskList

Empty. All of #806–#809 (this window's `/babygoal` decomposition) were completed and are no longer present in the list (the skill's tasks were resolved and the list was already empty by the time this checkpoint was requested — no explicit deletion was needed for these four, unlike the prior window's #792–#805 which the user asked to be deleted outright).

## Decisions

- Grouped the CI-consolidation review items (guard script, docs honesty note, new `-race` job) into ONE task/worktree instead of three, specifically because all three touch `.github/workflows/build.yml` and would have produced unresolvable merge conflicts if run as separate parallel `rush run` sessions.
- When the `-race` job surfaced a genuine, unplanned data race, chose to fix it immediately in the main session rather than deferring it to a new task — directly following the user's own freshly-added global rule from earlier this session ("all test failures/flakes are fixed in place, in the same turn, not deferred").
- Excluded `internal/agent` from the new `test-race` CI job entirely (not even in reduced/batched form) after a local run hit `go test`'s own 10-minute per-package timeout under `-race` — judged this a TIME constraint distinct from the Windows-specific MEMORY constraints (`GOMEMLIMIT`, batch-splitting) established in the prior session window, and documented the gap explicitly in the workflow file's comments rather than silently omitting coverage.
- After the mutex fix and the synchronous-capture fix both failed to close the race (confirmed via repeated, disciplined re-verification with `go clean -testcache` between attempts to rule out stale-binary false confidence), kept digging for the ACTUAL remaining mechanism (the WaitGroup Add-after-Wait antipattern) rather than accepting "mostly fixed" or reaching for a broader/blunter workaround.
- Cleaned up all 4 temporary task worktrees and branches immediately after their content was merged, rather than leaving them as clutter — mirrors how the pre-existing, unrelated `bisect-agent.yml`-style temporary infrastructure was torn down in the prior session window once its purpose was served.

## Open questions

None outstanding. The user's explicit end condition for this turn of work ("commit, push, make CI green, and stop") was met and confirmed via the GitHub API before this checkpoint was requested.

## Repo state

```
 D web/dist/.gitkeep
?? docs/checkpoints/2026-08-26-2015.md
?? docs/checkpoints/2026-08-27-0749.md
?? docs/checkpoints/2026-08-28-1210.md
?? docs/checkpoints/2026-08-28-1349.md
?? docs/checkpoints/2026-08-30-0859.md
?? docs/checkpoints/2026-08-30-1730.md
?? docs/checkpoints/2026-08-30-2015.md
?? docs/plans/2026-08-27-agent-tree-backend-requirements-response.md
```
```
da53fe42 ci: add db.Connect/Release pairing guard, targeted -race job, docs honesty note
61c17e22 fix(hooks): eliminate real data race on runShell/abandonSeam test seams
455d797a fix(agent): explicitly wait for hangingTitleModel's goroutine to exit
ce81b165 fix(agent): replace fixed inter-chunk sleep with channel handshake in TestP2_1
faabfe07 fix(agent/tools): fix remaining conn.Close() leaks after f712eb88
49d2c2ef fix(session): widen lease-renewal TTL 3s -> 5s (task #805, same class as #447)
```

`fork/main` is synced with local HEAD (`da53fe42`). The triggering CI run (`33343111270`) is confirmed green 7/7: `test-race`, `build` × {ubuntu,macos,windows}, `test-agent` × {ubuntu,macos,windows}.
