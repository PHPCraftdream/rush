# Checkpoint — 2026-08-20 08:10 [eighth-review-sprint-wave-one-partial]

## Session summary

An eighth independent release review landed
(`docs/reviews/2026-08-20_07-28-07-readonly-release-review-a70d1b73.md`,
**untracked and uncommitted** — it arrived from outside this session).
Verdict NO-GO, with six findings F1–F6: three P0, three P1. Its backlog is
filed as #610–#617 and the sprint was restructured around it. Eight reviews,
eight NO-GO verdicts.

I verified three of its claims myself before filing rather than trusting the
report. F1 (the WebSocket read loop blocking behind the work semaphore) and
F6 (rerun deleting same-second messages before the target) both reproduce
verbatim in the code. The third check was of my own backlog: #611 and item 5
of #616 were the same finding filed twice, now merged.

Two of the review's recommendations I deliberately deviated from, both
recorded in the task descriptions. #615 goes before #614 rather than after,
because it is a few-line condition fix closing deterministic data loss while
#614 is a race plus a new coordinator API — cheap and always-firing before
expensive and probabilistic. And #610 was raised from the seventh review's P1
to P0, because "CleanupExpiredLeases will pick the row up later" does not undo
`crush run` having already exited 0 claiming a finished continuation.

A `/babygoal` was armed this turn with a new strategy: `@sl` agents (Sonnet,
low effort — previous rounds used `@sh`, high) in worktrees, commits between
tasks, and an `@oh` review at the end. The babysit cron is installed and
confirmed: job `972470ee`, every 15 minutes at off-minutes.

**Wave one was interrupted halfway.** Four tasks were marked `in_progress`
(#611, #612, #613, #615) but only two agents actually launched (#612
`agent-wsloop`, #615 `agent-rerunorder`). #613 and #611 are marked in_progress
with **no agent behind them** — that must be corrected on resume, either by
launching them or by moving them back to pending.

**A worktree escape is in progress right now.** `git status` in the main
checkout shows `internal/server/hub.go` and `internal/server/server.go`
modified. Neither was touched by me. Those are exactly #612's files, so the
`agent-wsloop` agent is writing into the **main checkout** instead of its
worktree — the stale-assigned-worktree failure that has recurred all session.
I have not reverted it: the agent is still running, and a `git checkout --` in
a shared tree already destroyed another agent's finished work once today. It
needs sorting out when that agent reports, not before.

## Active goal

The `/babygoal` Stop hook, verbatim:

> реализуй с помощью агентов @sl & worktrees, в конце (когда решишь все таски)
> проведи ревью с помощью агента @oh - после ревью заведи таски и остановись,
> между тасками делай коммиты.

## TaskList

### in_progress
- #611 Test oracles don't isolate the mechanism: watchdog deadline and lease loss — **no agent launched**
- #612 P0: Cancel/Interrupt can be trapped behind the work semaphore — agent running, but writing into the main checkout
- #613 P0: admission handoff publishes an error only, losing row identity and outcome kind — **no agent launched**
- #615 Rerun deletes messages created before the target in the same second — agent running

### pending
- #610 Seventh drain form: a row leased by a dead owner is invisible to the drain (blockedBy: #613)
- #614 Rerun can force-delete a new live turn (TOCTOU on the idle check) (blockedBy: #615)
- #616 Five P2 residuals from the eighth review
- #617 Design: give the durable row one typed outcome end to end (blockedBy: #613, #610)
- #601 crush-fallback sentinel — ACTIVE (hard-limit until 2026-08-20 17:01). Never complete or delete: it is state, not work.

### recently completed
#593–#609, the entire fourth-through-seventh-review backlog.

## Decisions

- **#615 before #614, against the review's stated order.** Deterministic data
  loss on every same-second rerun outranks a race, and the cheap fix ships
  first. Both touch the same loop, so they are sequenced, not parallel.
- **#610 raised to P0.** The seventh review rated it P1 on the grounds that the
  work is deferred rather than lost. The eighth rates the contract: the exit
  code already lied.
- **F3 and F4 merged into one task (#613).** They are symmetric consequences of
  the same untyped `admissionEntry` handoff, and fixing them separately would
  mean rewriting one place twice.
- **#617 filed but explicitly not authorised.** Third independent review to
  conclude the drain's representation is the problem; the fourth review
  recommended freezing the lifecycle after blockers. That call is the
  operator's.
- **`@sl` instead of `@sh` this round**, per the goal. Low effort on #613 (a
  typed-outcome refactor) is the risky one; compensate with precise briefs and
  personal verification of every diff.

## Open questions

- Is `agent-wsloop` writing into the main checkout because its worktree
  creation failed, or did it create the worktree and then edit the wrong path?
  Unresolved until it reports.
- #613 and #611 are `in_progress` with nothing behind them. Launch or revert.
- The eighth review file is untracked. It should be committed, but not while
  the working tree also holds an agent's in-flight edits.
- Nothing is pushed. Eight reviews, eight NO-GO verdicts.
- `crush` delegation is limited until 2026-08-20 17:01; `dev/crush-local.exe`
  (built from `8393fa95`) is ready for after the reset.

## Repo state

```
 M internal/server/hub.go
 M internal/server/server.go
 D web/dist/.gitkeep
?? dev/
?? docs/reviews/2026-08-20_07-28-07-readonly-release-review-a70d1b73.md
```

```
a70d1b73 docs: the seventh review, and corrections to two claims of mine it disproved
cbd6e8d4 docs(checkpoint): closing snapshot for the sixth-review round
a1bfccbc docs(changelog): the sixth review and the round that closed it
929f3bb8 test(session): anchor the watchdog measurement where the deadline is sampled
f4d19ca8 fix(app): a lost session-creation race reports busy, not a raw SQLite constraint error
```
