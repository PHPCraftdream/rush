# Checkpoint — 2026-09-10 09:43

## Session summary

Продолжение сессии из чекпоинта `2026-09-09-2212.md`. Четыре агента фазы 1
(#923, #924, #925, #928) отработали и были смержены по одному, серийно,
с моей личной перепроверкой каждого (оракул деклараций, ревёрт-чек,
`-race`), поскольку эта машина дважды роняла параллельные тяжёлые
Go-прогоны (`errno=1455 ERROR_COMMITMENT_LIMIT`, и один раз — переполнение
стека рантайма Go на голом `go test ./...`). Итоговый жёсткий вывод по
памяти: 5 ГБ — это ОБЩИЙ лимит на всё, что живёт на машине одновременно,
а не персональная квота одному прогону; рабочий режим —
`capm 2g go test -p 1 -parallel 1` (win-nice), записано в CLAUDE.md.

**Декомпозиция файлов (#924/#925/#926/#927) — полностью закрыта.**
Аллоулист `.githooks/file_size_allowlist.txt` прошёл путь 20 → 2 файла.
`internal/agent/tools/mcp/init.go` 6478 → 398 (десять новых файлов,
реестр инвариантов переякорен). Остальные 17 нарушителей (12 прод +
6 тестовых Go-файлов + `web/src/store.ts`) разрезаны тем же методом —
оракул «множество top-level деклараций до == после» пуст на каждом.
oxlint добавлен в `web/` (#928/#929) с warning-ratchet (101 находка,
`--max-warnings`), встроен в pre-push и CI.

**MCP API (#923) — закрыт.** Двухуровневый API (`*Owner`-методы +
пакетные обёртки, делегирующие текущему owner'у) плюс standalone owners
через `AcquireStandalone()` — решает изоляцию мульти-`App` без миграции
глобалов (которая была отменена ещё в предыдущей сессии, посчитана как
33 строки выигрыша за 3-5 дней риска). Приёмочный тест
`TestTwoApplicationClientsCoexistWithIndependentMCPRegistries` проходит;
я лично воспроизвёл `ErrOwnerBusy` ревёрт-чеком, убрав standalone
fallback — тест падает с `mcp: application owner is already active`,
восстановил — проходит.

**Линтер (#934) — закрыт.** `golangci-lint run` дал 86 находок,
накопленных за 180+ непушенных коммитов (staticcheck 38, gofumpt 16,
tparallel 13, noctx 11, whitespace 4, govet 3, bodyclose 1). Разобрано
до 0: часть — реальные фиксы (устаревший `GetBackgroundShellManager`,
контекстные варианты `Ping`/`Dial`/`Listen`/`Command`, утечка
`cancelWaiter`, незакрытое тело ответа), часть — задокументированные
исключения в `.golangci.yml` там, где линтер был неправ (SA2001 —
lock/unlock как барьер или сама измеряемая операция в
`contextlock_alloc_test.go`; SA1019 в `transport.go` — намеренный
legacy-cancel; QF1001 — де Морган испортил бы читаемость guard'ов MCP).
Два флейка почищены по механизму, не расширением допусков:
`TestStreamWatchdog_ExtendsOnProgress` (отношение бамп/дедлайн 2.6x→6.6x
при той же длительности) и `TestConcurrentReadersAndWriter_WriteLatencyStable`
(лучшее из 3 прогонов с чередованием вместо последовательных серий).

**Финальное ревью (#930) — закрыто.** P0/P1 не найдено, серия
завершена по правилу остановки из CLAUDE.md.

**runTurn/ExecuteRun (#933) — открыта, существенный прогресс.**
`ExecuteRun` (`internal/app/app_run.go`) 1197 → 896 строк: вынесен
`prepareExecuteRun` в `app_run_setup.go` (чисто последовательная
сборка конфига, без замыканий/горутин). По ходу поймал реальный баг
через `-race`: именованный возврат `cancel` явно занулялся на
error-путях, отложенная очистка звала уже занулённую функцию — nil
pointer dereference; поправил (передавать реальный `cancel`, не nil).

`agent_turn.go` (`runTurn`) 1981 → 1602 за четыре извлечения, каждое
проверено `-race` на весь `internal/agent` + ревёрт-чек:
1. `agent_turn_checkpoint.go` — `turnCheckpointWriter`. Ревёрт-чек на
   реальном P0-4 фиксе (немедленная отмена записи в `stop()`) — без
   него тест завис на все 10 минут таймаута `go test`.
2. `agent_turn_peakhours.go` — `peakHoursWatcher` + 3 новых теста
   (`testing/synctest`). У механизма не было ни одного теста вообще до
   этого извлечения.
3. `agent_turn_ui_notify.go` — `turnUINotifier` + 4 новых теста
   (synctest). Та же ситуация — механизм не имел теста.
4. `agent_turn_title.go` — `startTitleGeneration` + `turnTitleJoiner`.
   Самое рискованное: ревёрт-чек буквально воспроизвёл исторический
   баг #525 (поменял местами `joinTitle(); cancel()` на
   `cancel(); joinTitle()` — тест упал с тем же симптомом
   "Untitled Session"/"context canceled", что в оригинальном
   инциденте). Порядок `defer` (`wd.done` → `cancel` → `joinTitle`)
   сохранён на точном месте регистрации, не только по поведению.

Дальше уткнулся в подтверждённый факт: оставшиеся ~9 callback'ов
`fantasy.AgentStreamCall{}` (`PrepareStep`, `OnReasoningStart/Delta/End`,
`OnTextDelta`, `OnToolInputStart`, `OnToolCall`, `OnToolResult`,
`OnStepFinish`, `StopWhen`) не читают общее состояние, а совместно им
владеют — `sawToolBoundary` пишут/читают 4-5 мест разом. Резать по
одному нельзя технически, нужен единый проход.

**Долгий тупик с `/goal`-хуком.** Активна цель `"реши все задачи,
друг"`, которая структурно неразрешима моими силами: #931 (пуш) требует
явного разрешения оператора (жёсткое правило CLAUDE.md), #933 требует
решения о риске. Хук срабатывал ~15 раз подряд без нового ввода —
отвечал терпеливо, но без повторения полного объяснения каждый раз
("без изменений"), предложил `/goal clear` как выход.

**Консультация @fm по архитектуре — только что завершена.** Оператор
предложил идею "роутер высшего порядка → тематические роутеры" для
оставшегося ядра `runTurn`, прямым текстом попросив не плодить файлы и
не соглашаться на компромиссы. Ответ fm: метафора наполовину верна —
"тонкий роутер" да, "тематические роутеры как отдельные объекты" нет,
потому что тематическая граница НЕ совпадает с границей владения
состоянием (нашёл, что `sawToolBoundary` трогают 5 мест, не 4 как я
насчитал, и что я вообще упустил `currentSession` как ещё одну общую
мутируемую переменную). Рекомендация: 3 новых файла вместо 9 —
`agent_turn_stream.go` (тип `turnStream`, единственный владелец
callback-состояния с двумя явными группами полей: "только
callback-горутина, без лока" vs "общее с тикерами, под локом"; плюс
`compositionPhase` вместо голого bool), `agent_turn_step.go` (методы
`prepareStep`/`onStepFinish`/`stopConditions` на `turnStream`,
`onStepFinish` разбит на 8 именованных фаз; тип `turnOutcome` для
canal callbacks→хвост), `agent_turn_failure.go` (обработка `err != nil`
после `Stream()` — чистая функция, я её вообще не заметил).
`agent_turn.go` должен остаться ~780 строк: преамбула + сборка
`turnStream` + вызов `Stream` + хвост; порядок `joinTitle`/`cancel`/
`peakHoursWatchDone` НЕ переносится — остаётся в `runTurn` на виду.
Оракул: два коммита разной природы (чистый механический перенос closures
в методы с sed-переименованием и diff-проверкой; отдельно —
поведенческие ревёрт-чеки на разбор `onStepFinish` на фазы).

Реализация плана fm **ещё не начата** — жду вашего "да, начинай".

## Active goal

`/goal "реши все задачи, друг"` — Stop hook активен. Структурно не
может быть удовлетворён без вашего решения по #931 (пуш) и #933
(продолжать ли риск сейчас). Чтобы прекратить повторные срабатывания
без потери прогресса, можно выполнить `/goal clear` — работа сохранена
независимо от этого.

## TaskList

### in_progress
- #933 [split-4] Выделить фазы из runTurn (1981→1602, план fm готов,
  реализация не начата) — не заблокирована технически, ждёт решения

### pending
- #931 [4.2] Пуш + три зелёных прогона CI подряд — ждёт явного
  разрешения оператора

### completed (эта сессия, после чекпоинта 2026-09-09-2212)
#923 MCP two-tier owner API · #924 правило ≤1000 строк (guard+ratchet)
· #925 init.go 6478→398 · #926 остальные 12 прод-файлов · #927 6
тест-файлов + store.ts · #928 oxlint в web/ · #929 oxlint в гейтах ·
#930 финальное ревью (P0/P1 не найдено) · #932 документация в
fork-CLAUDE.md · #934 86 находок golangci-lint → 0

## Decisions

- 5 ГБ — общий лимит памяти машины на всё разом, не персональная квота;
  рабочий режим `capm 2g -p 1 -parallel 1`, записано в CLAUDE.md с
  историей трёх инцидентов.
- Декомпозиция `runTurn` продолжается инкрементально по критерию
  "владеет своим состоянием, читает общее" — НЕ атомарным проходом по
  всем колбэкам разом, кроме случаев (как title-join), где сам
  инвариант — про порядок операций, а не про владение данными.
- Архитектура для оставшегося ядра `runTurn`: одна структура
  `turnStream` с двумя явными группами полей вместо нескольких
  тематических объектов — по рекомендации fm, потому что тематические
  границы не совпадают с границами владения (проверено конкретно на
  `sawToolBoundary` и `currentSession`).
- Не форсировать #933 ради прекращения повторных срабатываний
  `/goal`-хука — предложено `/goal clear` вместо этого.

## Open questions

- **#931 (пуш)**: делать ли `git push` в `fork/main`? ~15 коммитов
  этой сессии, все проверены (build/vet/gofmt + `-race` + ревёрт-чеки).
- **#933 (риск)**: продолжать ли реализацию плана fm сейчас (поздний
  час марафонской сессии, код с историей двух P0) или отложить на
  отдельную сессию?
- **4 спорные ветки** `fix/r17c13-mcp-operation-leases`,
  `fix/r17c23-config-commit-outcome`, `fix/r17c26-mcp-uncertainty-fence`,
  `fix/r17c9-mcp-cleanup` — сносить или оставить (открыт с прошлой
  сессии, не тронуто).
- **`web/dist/.gitkeep`** — предсуществующее удаление в рабочем дереве,
  fork-CLAUDE.md запрещает трогать в любую сторону.

## Repo state

```
 D web/dist/.gitkeep
```
```
98ef67633 refactor(agent): extract runTurn's title-generation join, verify against #525
76fdeafab refactor(agent): extract runTurn's UI-notify ticker, add its missing tests
23cc69966 refactor(agent): extract runTurn's peak-hours watcher, add its missing tests
1b38ff138 refactor(agent): extract runTurn's checkpoint writer into its own type
9c69aceb0 refactor(app): extract ExecuteRun's request-to-config setup
11e6595d8 fix: clear all 86 golangci-lint findings, document the shared memory ceiling
a1b62bed2 style: make the files this work touched gofumpt-clean
b06bbbd84 refactor(mcp): decompose the three remaining oversized test files
```

Рабочих worktree агентов этой сессии не осталось — все смержены и
удалены. Оставшиеся worktree — 8 прежних (оператору/спорным веткам) +
6 `.claude/worktrees/agent-*` от других инструментов.
