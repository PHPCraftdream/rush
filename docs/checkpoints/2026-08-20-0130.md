# Checkpoint — 2026-08-20 01:30 [fourth-and-fifth-reviews-worked-off]

## Session summary

Two more release-readiness reviews (the fourth and fifth) landed on the
concurrency work and both said NO-GO. Their backlog — #593 through #600 —
is now closed, in nine commits `537d93df` … `641c0c20`, **none pushed**.
The previous entry's two open items are both closed: the Unix registry
tests had never been executed anywhere, and the dynamic release gate did
not exist. Both are done now, and doing them changed the conclusions.

The finding that reframes everything: **the guards this whole effort
produced ran on no platform in CI.** `-short` skips every test that spawns
a real child, and a `!windows` build tag covers the rest. Five static
reviews said the Unix side was never exercised; `377e4b94` is the first
commit to actually wire it into the workflow. That is also the gate's sole
reason for NO-GO — the code itself survived ~200 adversarial concurrent
process invocations without a single silent success, corruption, or
zero-exit-over-failure.

`DrainSessionNow`'s contract had been wrong four times running, each fix
closing one combination and leaving the neighbour open. #593 finally gave
it row identity and a typed verdict, and I found the fifth form still
expressible — `(DrainFailed, nil)` — reproduced it, and sent it back. The
Unix sweep fence had the mirror-image problem: the follow-up agent found
something I had not, that `TryAcquireSessionLock` overwrites the generation
sidecar as part of acquiring, so the dead holder's identity is destroyed by
the act of probing for it. My own framing of the fix would not have worked.

Mid-session `crush` hit its weekly limit ("resets 2026-08-20 17:01") on the
sixth delegated run. That is trigger 2 of the armed fallback, so delegation
switched to `@sh` silently, per the standing instruction. Five crush runs
completed before it; everything after is `@sh`.

**Eleven delegated diffs were sent back after I verified them**, each for a
defect its own report did not mention. Three are a category worth naming:
the fix was correct but nothing pinned it — reverting the single site the
task existed for left the whole tree green. One diff carried a **fabricated
`fsync(2)` citation** supporting a correct change; that is a new failure
mode here, where the code is right and the justification is invented.

Verification at `641c0c20`: `go test ./...` clean on Windows, the new CI
command green on real Linux, Playwright 323 passed / 0 failed (314 at the
start of the previous session, 318 mid-way).

## Active goal

The /babygoal Stop hook: implement with `@sh` sub-agents and worktrees,
commit between tasks, then `/checkpoint`, update the changelog, commit the
markdown and run an `@oxx` review. This is the closing sequence; the `@oxx`
review is the last step and has not run yet.

## TaskList

### pending — all newly filed this round, none blocking
- #604 Watchdog overshoots its TTL margin ~40% of runs on Linux
- #605 Session-creation race surfaces a raw SQLite UNIQUE error
- #606 Two kill tests never observe the exit: helper holder is left a zombie
- #601 crush-fallback sentinel — ACTIVE (hard-limit). Never complete or delete.

### completed this round
#593, #594, #595, #596, #597, #599, #600, #602, #603 — the entire fourth-
and fifth-review backlog.

## Decisions

- **The CI step names its exclusions instead of widening the pattern past
  them.** Two kill tests fail on Linux at any commit (#606); listing them
  by name in the workflow keeps the gap visible, where a broader `-run`
  regex would have hidden it behind a green check.
- **Nothing was made green by widening a duration.** #604's 40% failure
  rate is filed with its measurement rather than papered over, and the
  agent that found it was explicitly told not to widen the bound and held.
- **A real Linux was found and used.** WSL2 Ubuntu-24.04 with Go 1.26.3 in
  `$HOME/sdk/go`. Docker exists but its daemon is down. This removes the
  "cross-compiles but never runs" caveat that ran through all five reviews.
- **The design doc's job is naming which test pins which invariant.** It
  claimed one invariant was unpinned; the test existed, in `internal/cmd`
  rather than `internal/session`, and I corrected it from firsthand
  verification rather than leaving the agent's grep result standing.
- **Every commit hash cited in the design doc was checked against
  `git log`** — after the fabricated man-page citation turned up the same
  day, unverified references in a trust document were not acceptable.

## Open questions

- Nothing is pushed. Six NO-GO verdicts precede this state; the gate's
  NO-GO is now addressed by `377e4b94`, but no one has re-run a gate
  against the current HEAD.
- #604 is a 40% failure rate, not a flake. Its root cause is in
  `run_queue_entry_exec.go`, deliberately outside the scope the flake agent
  was given.
- `crush` delegation is unavailable until 2026-08-20 17:01. `@sh` is
  carrying it; `dev/crush-local.exe` (built from `8393fa95`) stays ready.

## Repo state

```
 D web/dist/.gitkeep
?? dev/
?? docs/checkpoints/2026-08-19-2107.md
```

```
641c0c20 docs(changelog): the fourth and fifth reviews, and the first gate that actually ran
377e4b94 ci,docs: run the fence suites on Linux without -short, and record the gate that found they never ran
95d7e0c1 docs(design): write down the admission/drain/lock state machine
d49573e8 test(session): stop the pump via t.Cleanup so an early failure doesn't flood the log
6fe0108b fix(cmd): sweep the orphaned child tree when the holder crashed instead of being killed
```
