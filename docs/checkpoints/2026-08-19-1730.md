# Checkpoint — 2026-08-19 17:30 [backlog-cleared-third-review-landed]

## Session summary

The sixteen-task backlog from two NO-GO reviews is closed. Sixteen commits,
`638bc777` … `3486ee0d`, none pushed. Full `go test ./...` green, `go vet`
clean apart from a pre-existing `csync` finding, `gofmt` clean, both
pre-push guards passing, and the Playwright suite at **312 passed / 0
failed** — it was 181 passing with a long red tail when this started.

Everything was delegated to sub-agents and **eight diffs were sent back
after I verified them**, each time for a defect the agent's own report did
not mention. Three are worth remembering because all three passed their own
tests and read as correct: the first drain fix reused `nil` for both
"committed" and "never executed", recreating the P0 through a different
door; the first `sessions kill` registry `SIGKILL`ed unvalidated PIDs read
from a world-writable path, which is worse than the leak it fixed; and the
first CI guard step would have failed on every clean run, because it was
exercised under plain `bash` while Actions uses `bash -eo pipefail`.

Two things were investigated and deliberately NOT fixed, which is the right
outcome in both cases: the second reported sub-agent tool defect (a write to
a non-existent path reporting success) did not reproduce in five path
shapes, and two `ReleaseGate` tests were read closely, found sound, and left
alone rather than have their timeouts widened — they then went green anyway
once the genuinely broken tests around them stopped starving the machine.

**A third review landed while this was finishing** and is the reason this
checkpoint is not a closing one. `docs/reviews/2026-08-19-release-readiness-static-follow-up-0635c631.md`
reviews the sixteen commits statically and returns NO-GO with two new
release blockers, both inside the outcome handoff I shipped for the previous
P0.

**I saw both of them myself and talked myself out of both.** On the
admission ABA I wrote that waiting on a different entry still yields "a real
outcome for that session, so acceptable" — it does not, because attributing
B's success to A converts A's terminal failure into an exit code 0. On the
partial-drain result I noted twice that discarding the accumulated error
"mirrors pre-existing behaviour" and let it stand. Pre-existing is not an
argument when the result is a false success. That is the same failure mode I
caught eight times in the agents, and I did it while holding the pen.

## Active goal

The /babygoal Stop hook is armed: implement the tasks with sub-agents and
worktrees, commit between tasks, then checkpoint, update the changelog,
commit the markdown and run a review agent. The first five steps are done.
The sixth is superseded in substance: a third review already exists and its
findings are filed below rather than re-derived.

## TaskList

### completed this session
#567 #568 #569 #570 #571 #572 #573 #574 #575 #576 #577 #578 #579 #580 #581
#582 #583 #584 #585 #586 — the full backlog plus four tasks it generated.

### open, all from the third review
- #587 **P0** — `admitSession` discards the entry it lost to, and the drain
  re-reads it in a separate critical section. ABA: it can wait on, and
  report, the outcome of a *different* execution.
- #588 **P0** — a partial drain returns `(true, nil)`. `drained` is
  cumulative and `stopNow` discards the accumulated error, so a busy second
  row after a successful (or failed) first one exits 0 with work still
  pending. Needs a typed result; the `(bool, error)` pair cannot express it.
- #589 — the outbox counts `context.Canceled`, `DeadlineExceeded` and most
  SQLite errors as permanent, so routine shutdowns can burn a healthy row's
  five-attempt budget and park it at `failed` forever.
- #590 — an edit to a streaming message is verified, acked, and then
  overwritten by the next checkpoint from the agent's in-memory copy.
- #591 — three residuals: MCP tools still resolved from a live registry;
  superseded sticky envelopes can sit in the broadcast queue; the kill
  registry uses truncate-and-write and is deleted even after a failed sweep.

## Work in flight

None. Working tree clean apart from untracked `dev/` and the deliberately
uncommitted `web/dist/.gitkeep` deletion.

## Decisions

- **Every delegated diff was verified by re-running the revert-check
  myself**, not by reading the report. That is what caught all eight
  returns; in at least three cases the report was confident and wrong.
- **Two changes were not made.** A non-reproducing defect was left unfixed
  and said so; two flaky tests were left untouched rather than have their
  timeouts raised. Both were later vindicated.
- **The e2e suite was fixed rather than deleted**, except for 29 tests
  covering LSP and permission-dialog UI this fork removed — each verified by
  grep before deletion.
- **The third review's findings were filed, not argued with.** Two of them
  are mine.

## Open questions

- The review asks for a dynamic release gate that has never been run:
  targeted deterministic concurrency tests, multi-process inject/drain
  stress, repeated shutdown during an outbox transaction, a real Unix
  process-tree kill, and the full e2e suite. Only the last of those exists
  today.
- `agent_turn.go` is now 1880 lines and `run_queue_drain_session.go` 625.
  The review argues #588 is a direct consequence of encoding a state machine
  in named returns and three bool/error values. Splitting was considered and
  rejected with evidence earlier; this is the second independent argument
  for revisiting it.
- At-least-once side effects after an Ack failure or lease loss are visible
  now but still not bounded by any transactional or idempotency boundary
  around provider and tool calls.

## Repo state

```
 M CHANGELOG.fork.md
 D web/dist/.gitkeep
?? dev/
?? docs/checkpoints/
?? docs/reviews/
```

```
3486ee0d test(session): wait for the outbox drain instead of sleeping at it
0bd25e3c test(session): synchronise the transient-busy outbox test instead of polling for it
0635c631 test(web): the e2e suite is green, and its red now means something
34f80494 fix(web): render the toolbar without an active session again
9048e840 fix(session,cmd): let sessions kill reach the provider child tree on Unix, provably
```
