# Checkpoint — 2026-08-24 11:30

## Session summary

Extremely long-running `/babygoal` review-fix loop on `D:\dev\go\crush`
(PHPCraftdream/crush fork), governed by the standing instruction: after each
`@oh` (later `@sh`, see below) review's results, file TaskList tasks and
CONTINUE solving them via delegated workers, then run the review again and
file tasks based on it — loop until the review says OK (clean, nothing to
file), then stop.

**This loop is now COMPLETE.** Round 29's review (`docs/reviews/2026-08-24-twenty-ninth-review-b3b470cf.md`)
returned a clean **GO** verdict with zero findings, independently re-verifying
both of round 29's own predecessor fixes down to their actual diffs, running
the full 360-test Playwright suite twice (360/360 clean, one transient
timing flake in `reasoning-effort.spec.ts` confirmed non-reproducing), and
covering fresh Go-side ground (`events.go`, `hub.go`) with nothing found. Per
the governing stop condition, I stopped after this round. No further review
rounds should be launched unless the user explicitly restarts the loop.

**Delegation mechanism, for continuity if the loop resumes:** originally all
delegated fix-work went through `crush run` (CLI). Mid-session the user gave
an explicit, unconditional instruction — "переключись на агентов @sh" — to
switch to the `Agent` tool with `subagent_type: "sh"` (Sonnet, high effort)
for all delegated work, because `crush run` was hitting sustained `zai`
provider peak-hours refusals (08:00–12:00 local window). Reviews also moved
from `oh` (Opus) to `sh` after Opus hit four consecutive instant 529-overload
failures launching round 26's review. Both switches held for the rest of the
session (rounds 26-29 reviews, tasks #683-#686 fixes, all via `sh`). If
resuming this loop, default to `sh` Agent-tool delegation unless the user
says `crush run`'s peak-hours window has cleared.

**`Agent` tool's `isolation: 'worktree'` parameter proved UNRELIABLE this
session** — twice assigned a stale worktree pointing at an ancient
pre-`web/`-UI commit. The reliable pattern that replaced it (used
successfully for tasks #684 and #685): manually create the worktree yourself
first (`git worktree add -b wt-<task>-<slug> .claude/worktrees/agent-<task>-<slug> main`),
junction-link `web/node_modules` into it
(`cmd //c mklink /J node_modules D:\dev\go\crush\web\node_modules`, run from
inside the worktree's `web/` dir), then launch the `Agent` tool WITHOUT the
`isolation` parameter at all, with explicit absolute-path instructions in the
prompt directing the worker to operate exclusively inside that manually
created path, including an up-front `git -C "<path>" log -1 --oneline`
verification step with instructions to stop and report if it doesn't match.

**Hazard discovered and hit TWICE this session: `git worktree remove
--force` can corrupt the shared physical `web/node_modules` directory** when
the worktree being removed has a junction pointing into it — git's removal
logic appears to follow the reparse point on Windows rather than just
unlinking it, deleting real files from the shared target (this session's
`#685` cleanup emptied `node_modules/@rsbuild/core`, breaking `pnpm
typecheck` repo-wide until repaired). **Established safe removal sequence
going forward:** before calling `git worktree remove`, first `cd` into the
worktree's `web/` dir and run `cmd //c "rmdir node_modules"` (relative path,
no trailing slash, no `/S`/`/Q`) to unlink the junction point on its own,
*then* remove the worktree directory/git registration. If corruption is
suspected afterward, verify with `ls node_modules/@rsbuild/core` (should be
non-empty) before trusting a subsequent `pnpm typecheck`, and repair via
`pnpm install --frozen-lockfile --force` from `web/` if needed (confirmed
safe and fast, ~13-15s, whenever no other worker is concurrently
installing/building).

**Nine consecutive successful rounds this window** (21 through 29, with 29
being the clean closer) found and fixed real, independently zero-trust-
verified defects in `web/` + its WS handlers — sub-agent block state
derivation, part-index addressing, wire-field completeness, broadcast
ownership-annotation completeness, a severe client-side merge-guard
regression, two rounds of collapsed-dialog ambush bugs, effort-badge
completeness, attachment-drop on two send paths, a React-StrictMode
disposedRef latch (which also fixed 12 previously-red repo tests), a
sidebar-rename Cancel bug, two rounds of fire-and-forget-save/delete bugs
(`SystemPromptModal`, then `Sidebar` session delete), and finally a
`messages_list` stale-reply race (task #685, commit `6810c63e`) plus a
test-hygiene fix for a stale test that round 27's own fix had left behind
red on `main` (task #686, commit `b3b470cf`). Every fix followed the same
zero-trust discipline: read the full diff myself, re-run
typecheck/build/relevant Playwright specs independently (not trusting the
worker's self-report), and perform a temporary revert-and-re-test to prove
each new regression test genuinely fails without the fix.

**`web/node_modules` corruption** was also hit and repaired via ordinary
concurrent-worktree causes (not just the `git worktree remove` hazard above)
twice earlier in the session (before round 26's review, and again before
round 28's fix work) — same repair command, `pnpm install --frozen-lockfile
--force`, always confirmed safe only when no other worker was concurrently
running.

## Active goal

None (no `/goal` Stop hook armed this session — the loop was governed by the
skill's own TaskList-driven contract via `/babygoal`, not by a `/goal`
condition). The `/babysit` cron (15m interval) is still armed and will keep
ticking; each tick currently reports "blocked" because the only pending
tasks are the two permanent sentinels below, which must never be
auto-started.

## TaskList

### in_progress
(none)

### pending
- #601 crush-fallback state (persistent — do not complete): sh — armed
  (sentinel; never completes, do not touch except to update its status text
  if a fallback trigger actually fires)
- #617 Design: give the durable row one typed outcome end to end (explicitly
  unauthorized by the user this session — do NOT start without a fresh,
  direct instruction)

### recently completed
- #687 Launch round 29 review (@sh, background) after full go test ./... completes
- #686 Minor: sessions.spec.ts:274 asserts pre-#684 optimistic-delete behavior, now permanently red on main
- #685 P3: messages_list replies have no stale-reply guard — two concurrent load_messages requests for the same externally-owned session can regress the visible transcript if replies arrive out of send order
- #684 P3: Sidebar session delete is optimistic — a rejected delete_session/delete_other_sessions still wipes the row locally until the next poll self-heals it
- #683 P2: SystemPromptModal's Save is fire-and-forget — marks the edit clean before the server replies, silently discarding it on a rejected write
- #682 Minor: ActionRow's burst thinking row still stashes its edit form across a collapse — the sibling of the round-24 ThinkingPart fix that was never ported
- #681 P3: sidebar session-rename Cancel button commits the rename instead of discarding it
- #680 P2: ProvidersModal's disposedRef latches true under React StrictMode, making the whole Providers editor a dead button in dev — and is why 12 of the repo's own Playwright tests were red on main
- #679 P2: an attached file is silently discarded on the fast-send and queue-while-busy paths; fast-send even clears the badge so it reads as sent
- #678 Minor: ActionRow.tsx's burst thinking-row Edit button doesn't force the row open (sibling of #676, Delete already correct)
- #677 Minor: round 23's effort badge still never renders on the dominant (burst) rendering path

(84 earlier tasks, #593-#676, all completed — full backend hardening series
rounds 1-19, plus web/ series rounds 20-23; see TaskList tool for the
complete list if needed.)

## Decisions

- Switched fix-worker delegation from `crush run` (CLI) to `Agent` tool
  `subagent_type: "sh"` — explicit, unconditional user instruction, due to
  `zai` provider peak-hours refusals.
- Switched review-launch tier from `oh` (Opus) to `sh` (Sonnet) — my own
  adaptive decision (not explicit instruction) after Opus hit four
  consecutive instant 529-overload failures; proved reliable for rounds
  26-29.
- Abandoned the `Agent` tool's `isolation: 'worktree'` parameter in favor of
  manually creating worktrees myself — it twice assigned a stale worktree
  pointing at a pre-`web/`-UI commit.
- For task #685's fix direction, chose "tag every `load_messages` send with
  a msgID and drop replies to superseded requests" (applied to all 9 call
  sites, not just the two named in the review) over "merge the two
  independent pollers into one" — simpler, more robust against future new
  call sites, and requires no server-side change.
- For task #686 (stale `sessions.spec.ts` test), chose "delete the stale
  test" over "patch it to send a mock reply" — it was already a strict
  subset/duplicate of `sidebar-delete.spec.ts`'s CONTROL test.
- Declared the loop's stop condition met and stopped after round 29's clean
  GO verdict, per the governing instruction — did not launch round 30.

## Open questions

None currently open. The loop is stopped by design; the only live question
for a human is whether/when to restart it (e.g. a fresh `/babygoal` or
explicit instruction), and whether the `/babysit` cron should be disarmed
now that its governing work is done (sentinels #601/#617 will keep it
reporting "blocked" indefinitely otherwise — it will not self-delete since
`pending + in_progress` never reaches 0).

## Repo state

```
 D web/dist/.gitkeep
?? docs/checkpoints/2026-08-23-1740.md
?? docs/reviews/2026-08-23-eighteenth-review-bcb098a0.md
?? docs/reviews/2026-08-23-fifteenth-review-889a2092.md
?? docs/reviews/2026-08-23-fourteenth-review-28f37afc.md
?? docs/reviews/2026-08-23-nineteenth-review-c8a79e4c.md
?? docs/reviews/2026-08-23-seventeenth-review-afb8372c.md
?? docs/reviews/2026-08-23-sixteenth-review-cb35b0be.md
?? docs/reviews/2026-08-23-thirteenth-review-b0998e92.md
?? docs/reviews/2026-08-23-twelfth-review-7ba4431a.md
?? docs/reviews/2026-08-24-twentieth-review-9c1d661b.md
?? docs/reviews/2026-08-24-twenty-eighth-review-3c6c56d4.md
?? docs/reviews/2026-08-24-twenty-fifth-review-77ac172e.md
?? docs/reviews/2026-08-24-twenty-first-review-407b5f3a.md
?? docs/reviews/2026-08-24-twenty-fourth-review-8aa89dca.md
?? docs/reviews/2026-08-24-twenty-ninth-review-b3b470cf.md
?? docs/reviews/2026-08-24-twenty-second-review-04b62d59.md
?? docs/reviews/2026-08-24-twenty-seventh-review-beed48a4.md
?? docs/reviews/2026-08-24-twenty-sixth-review-4aeb475d.md
?? docs/reviews/2026-08-24-twenty-third-review-7c8a170e.md
```

```
b3b470cf test(web): drop sessions.spec.ts test asserting pre-#684 optimistic delete
6810c63e fix(web): drop stale messages_list replies instead of applying them
3c6c56d4 fix(server,web): sidebar session delete waits for the server, and delete_other_sessions stops lying about partial failure
beed48a4 fix(web): SystemPromptModal's Save now waits for the server's reply
4aeb475d fix(web): burst thinking row no longer stashes its edit form across a collapse
```

Note: `web/dist/.gitkeep`'s deletion is pre-existing, documented in
`CLAUDE.md` as required for `//go:embed all:dist` — do not commit its
deletion without explicit instruction, and do not restore it either unless
asked (both directions are the user's call). All the untracked
`docs/reviews/*.md` / `docs/checkpoints/*.md` files are this loop's own
review reports and checkpoints — none are committed; leave that decision to
the user.
