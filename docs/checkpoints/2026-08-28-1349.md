# Checkpoint — 2026-08-28 13:49

## Session summary

Continuation of a marathon multi-day session on `D:\dev\go\crush` (PHPCraftdream/rush fork). This window's work is essentially DONE — everything is implemented, verified, and committed; the only open item is getting a push to actually land through a heavily-contended machine.

Six threads this window: (1) documented `--restrict-run` in the `/rush` orchestration skill; (2) profiled `rush run` memory live (`RUSH_PROFILE=1` + pprof) and found a globally-configured stdio MCP server (`npx @z_ai/mcp-server`) spawning a fresh ~90-100MB Node child on every single invocation regardless of use; (3) implemented `enabled_in_cli` MCP gating in response (new `MCPConfig.EnabledInCLI`, `app.RestrictMCPToCLI()` option, `internal/cmd/root.go`'s `mcpAppOptions` deciding per-invocation via `cmd.Parent() == nil`, `rush run --all-mcp` escape hatch, `rush mcp add/set --enabled-in-cli` + list column) — committed as `aeda3654`; (4) earlier, built `git_read` and `run_command` agent tools via three sequential `/rush`-delegated sessions (two crashed mid-turn on `gofumpt`, work survived each time) plus generalized the restricted-run permission gate from a `"bash"`-literal check to an interface check so `--allow-bash` also covers `run_command` — committed as `230cea83`; (5) root-caused and fixed a real `panic: close of closed channel` in the checkpoint-write ticker (a `select` race where `stopCheckpoint` closes `stop` before cancelling `writeCtx`, letting a queued tick win against the stop signal and attempt a doomed second write) plus added 5 covering DB indexes found by profiling (`EXPLAIN QUERY PLAN` against a copy of the real `.rush/rush.db`: 6/10 hot queries were paying an unindexed sort, `GetLastSession` did a full table scan) — committed as `a0136c68`; (6) bumped `forkBaseVersion` to `0.2.0-alpha.1` and did a full changelog audit against ~196 commits since the changelog was last genuinely updated (`da10303f`), finding and backfilling a large set of previously-undocumented changes — most notably the entire crush→rush rename (binary name, ~52 env vars, every config/state path, module path, npm package — had ZERO mentions in the changelog before this) — committed as `193e9c8f`.

A live memory-mechanics tangent came up mid-session (user pushback: "80MB seems like a lot"): built throwaway comparison binaries and empirically proved that (a) a trivial Go binary on Windows costs only ~11.5MB Private Bytes (ruling out "it's just platform tax"), (b) importing `crypto/tls`+`net/http` alone — no calls made — costs ~35MB more, confirmed to be import-time package init (cipher-suite/EC-curve tables), not eager `x509.SystemCertPool()` reads, and (c) that ~35MB is committed *address space*, not physical RAM: a live measurement showed Working Set collapsing from ~6MB to ~12KB within 80s of idle while Private Bytes stayed flat at ~46MB — Windows aggressively evicts/compresses those cold pages. Net conclusion communicated to the user: the ~73-78MB "Private Bytes" figure from earlier profiling overstates real-world RAM pressure once rush is idle between turns.

Currently in flight: the third attempt to `git push fork main` (background task id `b3n6y29tm`, confirmed alive and at the `go build ./...` stage of `.githooks/pre-push` as of this checkpoint). The first two attempts failed to land: attempt 1's background wrapper reported "killed" with empty output but its `golangci-lint` child process (PID 32688) was found still genuinely alive ~15 minutes later — traced to a machine-wide resource crunch from an unrelated, heavily concurrent OTHER project (`rs-workspace`/`gateway`, many parallel `cargo test`/rust-analyzer/claude-agent processes visible via `wmic`), evidenced by a literal `bash: fork: retry: Resource temporarily unavailable` when just trying to run diagnostic `wmic` queries. Attempt 2 (foreground, manually retried) failed immediately with `Error: parallel golangci-lint is running` (collided with attempt 1's still-live lint process). Once attempt 1's `brz6ytgbw` task notification finally arrived (status `killed`) and its lint process was confirmed gone, free memory had recovered from ~16.3GB to ~23.8GB, so attempt 3 was launched. No further diagnostic polling was done per the user's explicit earlier annoyance with machine slowdowns — waiting for the real completion notification this time.

## Active goal

None. No `/goal` Stop hook armed.

## TaskList

Empty — confirmed via `TaskList` call, no tasks found.

## Decisions

- Split the session's remaining work into two separate commits rather than one: `a0136c68` (checkpoint-race fix + DB indexes — pure bug/perf fixes) and `193e9c8f` (version bump + changelog — release bookkeeping), keeping "what changed in behavior" separate from "how the change is announced/versioned".
- When the changelog audit surfaced the missing crush→rush rename entry, chose to write it as a first-class `### Changed` BREAKING entry (env vars, paths, module, npm package, migration tools) rather than a passing mention — judged it the single most user-impacting undocumented change in the whole ~196-commit gap.
- Explicitly did NOT silently fix a pre-existing changelog structural defect noticed during the audit (two `### Fixed` headings in the same release section with an unrelated Added-shaped entry sandwiched between them) — flagged it to the user instead of reshuffling ~300 lines of prior wording unasked.
- On the second failed push (`parallel golangci-lint is running`), chose to let the first attempt's still-alive process keep running rather than kill it, since it represented real (if slow) in-progress work on a resource-starved machine — killing and restarting would have just re-competed for the same scarce resources.
- Stopped issuing diagnostic `wmic`/`cat` polling commands once several of them started getting auto-backgrounded/delayed themselves (a direct symptom of system contention) and one produced a literal shell `fork: retry: Resource temporarily unavailable` — judged that continuing to poll was adding load, not reducing uncertainty, and switched to waiting for the real background-task notification instead.

## Open questions

- Whether to eventually clean up the pre-existing changelog structural defect (duplicate `### Fixed` heading with a misplaced entry between them) found during this session's changelog audit — explicitly deferred, not yet answered.
- The push itself: attempt 3 (`b3n6y29tm`) is in flight as of this checkpoint; not yet known whether it will land cleanly or need a 4th attempt.

## Repo state

```
 D web/dist/.gitkeep
?? docs/checkpoints/2026-08-26-2015.md
?? docs/checkpoints/2026-08-27-0749.md
?? docs/checkpoints/2026-08-28-1210.md
?? docs/plans/2026-08-27-agent-tree-backend-requirements-response.md
```
```
193e9c8f chore(release): bump fork version to 0.2.0-alpha.1, backfill changelog
a0136c68 fix(agent,db): checkpoint-write ticker race, add covering DB indexes
aeda3654 feat(config,agent,cmd): gate MCP server startup behind enabled_in_cli
230cea83 feat(agent,config,permission): add git_read and run_command tools
ee03f26e fix(platform): use exec.CommandContext in the new console-window test (noctx)
```

`fork/main` (as of last fetch) is still at `ee03f26e` — 4 local commits (`230cea83`, `aeda3654`, `a0136c68`, `193e9c8f`) are ahead of it and NOT yet pushed. All are real, complete, verified work sitting safely in the local repo regardless of push outcome — nothing is at risk of loss.
