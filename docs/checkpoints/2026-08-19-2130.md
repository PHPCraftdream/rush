# Checkpoint — 2026-08-19 21:30 [three-reviews-worked-off]

## Session summary

Three independent release-readiness reviews, all NO-GO, all worked off.
Twenty-three commits, `638bc777` … `61c481c6`, **none pushed**. Every task
closed.

The first two reviews covered the same 21 commits by different methods —
one dynamic (reverting production changes and running the tests), one
static — and produced sixteen tasks including six release blockers. Those
are described in the previous checkpoint and in `CHANGELOG.fork.md`.

The third review arrived while that work was finishing, and its two
blockers were **inside the outcome handoff written to close the first
review's P0**. That is the part worth carrying forward: I had seen both
while reviewing that commit and argued myself out of them. Once because a
different execution's outcome is "still a real outcome for that session".
Once because discarding an accumulated error "mirrors pre-existing
behaviour". Neither survives asking what the caller does with the value —
`RunNonInteractive` turns `(true, nil)` into exit code 0 over work that did
not run. It is exactly the failure mode I caught eight times in the agents,
committed while holding the pen myself.

**Verification at HEAD:** `go test ./...` clean; `GOOS=linux` and
`GOOS=darwin` builds clean; `go vet` clean apart from a pre-existing
`csync` finding; both pre-push guards clean; `gofmt` clean; web typecheck
clean; Playwright **314 passed / 0 failed** (it was 181 passing with a long
red tail at the start of the day).

**Nine sub-agent diffs were sent back after I verified them**, each for a
defect the agent's own report did not mention. Four are worth remembering
because all four passed their own tests and read as correct:

- the first drain fix reused `nil` for both "committed" and "never
  executed", recreating the P0 through ten early-return paths;
- the first `sessions kill` registry `SIGKILL`ed unvalidated PIDs read from
  a world-writable path — worse than the process leak it fixed;
- the first CI guard step would have failed on **every clean run**, because
  it was exercised under plain `bash` while Actions uses `bash -eo pipefail`;
- the first sticky reordering fixed a drop-under-load bug and introduced a
  stale-wins bug, by deleting the comment that explained why the original
  order existed.

Two agents also caught their own vacuous tests before reporting, which is
the discipline working rather than failing.

**Three things were investigated and deliberately not changed**, each with
the measurement behind it: a reported tool defect that did not reproduce in
five path shapes; two `ReleaseGate` tests read closely, found sound, and
left alone rather than have their timeouts widened; and the MCP registry,
which cannot be pinned to a config snapshot because it holds live client
sessions — so the *claim* was narrowed instead of the behaviour changed.

**And one correction to my own earlier work.** Commit `3486ee0d` said "15
failures became 0" at eight-way concurrency. That was one sample. Measured
at the same commit without the patch: 12 failures across 8 runs; with it,
3. `internal/session` is load-sensitive independently of any single change.
Every load number in this session's final commits is now a per-8-run count
from the same machine, and the last one is reported as 1, not 0.

## Active goal

The /babygoal Stop hook: implement with sub-agents and worktrees, commit
between tasks, then checkpoint, changelog, commit the markdown, and run a
review agent. All steps done; the review agent is the last.

## TaskList

Zero open. Closed this session: #567–#592 (twenty-six task ids, several
filed mid-session as residue rather than left in prose).

## Work in flight

None. Working tree clean apart from untracked `dev/` and the deliberately
uncommitted `web/dist/.gitkeep` deletion.

## Decisions

- **Every delegated diff was verified by re-running its revert-check
  myself.** That is what caught all nine returns. Reading the report would
  have caught none of them.
- **Nothing was made green by widening a duration.** Two agents were told
  this explicitly and both held; the flakes that were genuinely fixed were
  fixed with barriers, and the load sensitivity was fixed by bounding the
  package's own parallelism.
- **Claims were narrowed when the code could not be widened** — the MCP
  pinning comment, and `updateMessageAndVerify`'s doc, which described a
  durability guarantee it never provided.
- **Numbers are reported as measured**, including when that contradicts an
  earlier commit message of mine.

## Open questions

- The dynamic release gate the third review asks for does not exist:
  multi-process inject/drain stress, repeated shutdown during an outbox
  transaction, a real Unix process-tree kill. Everything Unix-specific
  shipped this session is cross-compiled and unexecuted.
- Five new registry tests have never run. They need a Linux or macOS box.
- `agent_turn.go` is 1880 lines and `run_queue_drain_session.go` 625. The
  third review argues the partial-drain blocker was a direct consequence of
  encoding a state machine in named returns and three bool/error values.
  That is now the second independent argument for splitting them.
- At-least-once side effects after an Ack failure or lease loss are visible
  but still unbounded by any transactional or idempotency boundary.
- Bounding test parallelism removed pressure, and pressure is what surfaced
  several genuine defects here. Eight-way should stay available as an
  explicit stress mode.

## Repo state

```
 M CHANGELOG.fork.md
 D web/dist/.gitkeep
?? dev/
```

```
61c481c6 test(session): bound the package's own parallelism instead of widening its timeouts
d3ee9841 fix(session): quarantine only a proven-permanent outbox failure, and make the kill registry durable
ff9efec1 fix(server): suppress superseded sticky envelopes; narrow the MCP pinning claim
b788eb01 fix(session,app): close the admission ABA and stop reporting a partial drain as success
547b0815 fix(server,web): refuse edits to a message that is still streaming
```
