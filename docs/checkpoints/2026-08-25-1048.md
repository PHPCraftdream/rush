# Checkpoint — 2026-08-25 10:48

## Session summary

Continuation of the crush→rush rename-project session on `D:\dev\go\crush` (PHPCraftdream/rush fork). This window picked up from checkpoint `2026-08-25-0221.md` (whose TaskList was already empty and the `rush-origin` push mid-flight — that push completed successfully during this window, confirmed via `git ls-remote`, and the user then asked to commit the untracked `.md` checkpoint/review files, which was done as `7a34c972`, later superseded by the migrate-chain commits below).

Two `/babygoal`-adjacent work threads ran in this window, both delegated via the `Agent` tool with `subagent_type: "sl"` (Sonnet, low effort) and `isolation: "worktree"` — a NEW delegation pattern for this session, distinct from the `crush run` + manual-worktree pattern used throughout the rest of the session. The user explicitly requested `@sl` for both threads.

**Thread 1 — a second, harsher release-readiness review** (report at `docs/reviews/2026-08-25_02-38-50-readonly-release-review.md`, never committed — untracked, referenced by task descriptions but the file itself lives only on disk) found the release still practically NO-GO despite the prior review round's fixes: a P1 npm deploy-path regression (`crush-`/`rush-` prefix mismatch broke self-update), a WS busy-forever bug class at 5 sites, a queued-message reconnect race, a PowerShell alias guard gap, a WS shutdown registration race, and P3 cleanup items. The user first asked to implement these via `/crush` worktree agents (the established pattern), running an `@oh` review only after all tasks closed. That work landed as 8 commits (`d7f61927` through `89142b4f`), and the `@oh` review (task #728) found ONE real blocker of its own: task #726's stale-action policy fix had accidentally made the Inject button always send `send_message` instead of `inject_message`, even when online — breaking a fork-specific mid-turn-steering feature and leaving 2 pre-existing e2e tests red on `main`, undetected because no prior commit in that chain had run the FULL Playwright suite. Fixed directly (`fbfd3e8b`) plus a documentation-only nitpick (`0e241b39`), then verified with the full suite for the first time (412/412 green). The review also surfaced 3 follow-up findings, filed as tasks #731/#732/#733 (not fixed in that pass, per the user's explicit "file tasks then stop" instruction).

**Thread 2 — `rush migrate` completeness.** A separate, more detailed review (pasted directly into the conversation by the user, not saved as a file) found the migration tool structurally incomplete: it renamed `.crush`→`.rush`/`crush.json`→`rush.json` but left `crush.db`/`crush.log` under their old names inside the renamed directory (invisible to the app, which looks for `rush.db`/`rush.log`), skipped the recursive walk's own root, never touched `/etc/crush/crush.json`, never rewrote legacy values INSIDE migrated config content (`disabled_skills`, `skills_paths`, `data_directory`, `global_context_paths`), stranded leftover files when a global target directory already partially existed, and gave no signal about un-migratable things (stale `CRUSH_*` env vars, un-deleted old-named CLI installs). The user asked to fix this via `@sl` agents too. Decomposed into 3 SEQUENTIAL tasks (all touching the same `internal/cmd/migrate.go`, avoiding parallel-conflict risk) — landed as `af9b7384`, `b1bff318`, `7e3407bb` — each independently verified (build/vet/22 final `Migrate*` tests all green) before the next was dispatched.

**Then the user asked to implement the 3 review follow-ups (#731/#732/#733) too, via `@sl`.** Unlike the migrate chain, these three touch genuinely disjoint files (Go server+db+message service+web ws/store/useWS for #731; four web *component* files for #732; one Go agentguard file for #733), so all three were dispatched in PARALLEL in one message. All three landed clean: `262db812` (#733, agentguard now recurses into `commandWrappers` for the window-safety check, closing an `iex 'saps notepad'`-shaped bypass), `df561a01` (#732, migrated the 5 remaining busy-forever WS call sites — Sidebar delete×2, MCPSettings, ProviderForm, ChatToolbar shutdown — to the shared `wsRequest` helper; full suite 419/419), and `da74ca87` (#731, the most substantial: a server-side monotonic watermark — reusing SQLite's existing `rowid`, no schema migration — closes a theoretical delete-resurrection race in `mergeMessageLists`; full suite 413/414, the one failure a confirmed pre-existing worker-contention flake in `reasoning-effort.spec.ts`, unrelated to the change, verified by isolated re-run passing clean — the SAME flake independently observed during #732's verification too).

A recurring infrastructure wrinkle throughout both threads: the `Agent` tool's `isolation: "worktree"` mechanism repeatedly checked out worktrees at a STALE upstream commit (`677046d2`, pre-rename, no `migrate.go` at all) instead of the fork's current `main` — every `sl` agent this session was explicitly instructed to check `git log --oneline -1` first and self-correct via `git reset --hard main` on its own exclusive branch if needed, which every one of them did successfully and safely (confirmed via independent `git merge-base HEAD main` checks before each merge). This is a known quirk of this specific isolation mechanism in this environment, not a one-off fluke — worth remembering for future `isolation: "worktree"` dispatches in this repo.

**TaskList is now completely empty** (confirmed via `TaskList` → "No tasks found") and `CronList` shows no scheduled jobs (no babysit cron was armed this window — these were direct `Agent` tool dispatches, synchronous/foreground, not the `crush run` background-polling pattern that needs babysit).

## Active goal

None. No `/goal` Stop hook armed, no cron scheduled.

## TaskList

Empty. Every task touched this window (#726-through-#736, plus the earlier #705-#730 chain from before this checkpoint) is `completed`. Nothing pending or in_progress.

## Decisions

- `@sl` (Agent tool, `subagent_type: "sl"`, `isolation: "worktree"`) was used as the delegation mechanism for both threads in this window, per explicit user instruction — a departure from the `crush run` + manual-worktree pattern that dominated the rest of this session. Both mechanisms coexist; which to use is the user's call per-request, not a fixed default.
- The `rush migrate` completeness fix was deliberately sequenced (3 tasks, one after another) rather than parallelized, because all 3 touch the same core file (`internal/cmd/migrate.go`) and parallel edits would conflict; the follow-up review-finding fixes (#731/#732/#733) were deliberately parallelized because they touch genuinely disjoint files. The rule applied: check file-set overlap before choosing sequential vs. parallel, not a blanket default either way.
- The server watermark fix (#731) reused SQLite's existing implicit `rowid` rather than adding a new schema column/migration, after the delegated agent investigated the actual DB schema first and confirmed `rowid` was already the fork's established ordering primitive elsewhere in the same query file.
- Hook command strings containing "crush" (in migrated config content) were deliberately left un-rewritten (#735) — rewriting arbitrary shell command text was judged too risky for a rename tool, confirmed via a dedicated true-negative test.
- The `reasoning-effort.spec.ts` full-suite flake (worker-contention timeout in a shared mock-WS test helper, not a real regression) was independently reproduced and ruled out twice (once during #732's own verification, once during #731's) — established as a known, pre-existing, environment-dependent flake in this test suite, not something to chase further right now.

## Open questions

None outstanding. Nothing is currently blocked or awaiting user input.

## Repo state

```
 D web/dist/.gitkeep
?? docs/reviews/2026-08-25_02-38-50-readonly-release-review.md
```

Note: `web/dist/.gitkeep`'s deletion is pre-existing/deliberate (CLAUDE.md: required by `//go:embed all:dist`, never commit either direction without explicit instruction). The untracked review report is this window's own working doc (the second, harsher release-readiness review referenced by tasks #721-#730) — not committed by design, matching the same pattern as prior review-report files this session.

```
da74ca87 fix(server,web): server watermark closes mergeMessageLists resurrection gap
df561a01 fix(web): migrate remaining permanent-busy-on-disconnect WS call sites
262db812 fix(agentguard): recurse into commandWrappers for window-safety checks
7e3407bb fix(migrate): merge stranded legacy files, add manual-follow-up report
b1bff318 fix(migrate): rewrite legacy references inside migrated config content
af9b7384 fix(migrate): rename known artifacts, fix --recursive root skip, add /etc path
0e241b39 docs(web): pin removeSubAgentState's one-level-nesting assumption
fbfd3e8b fix(web): inject button was silently sending send_message, not inject_message
```

`fork` remote (`https://github.com/PHPCraftdream/crush.git`, the OLD repo) has not been pushed to this session — local `main` is well ahead of it, no push requested. `rush-origin` (`https://github.com/PHPCraftdream/rush.git`, the NEW repo, created earlier this session) received one push (up through the `7a34c972` md-files commit) — everything from `af9b7384` onward in this checkpoint's repo state is local-only, not yet pushed anywhere. `origin` remote remains `https://github.com/charmbracelet/crush.git` (upstream, untouched).
