# Checkpoint — 2026-09-22 13:17

## Session summary

Long continuing session. Prior portions (pre-compaction) implemented and shipped the `--idle-timeout` CLI feature (committed/pushed as `f7362f7f`), then ran five sequential rounds of an external static-analysis audit against `docs/reviews/2026-09-2*-weekly-commit-audit*.md`, delegating each round's P1 findings to solo/parallel `/wrush` sub-agents in isolated git worktrees, personally zero-trust-verifying every diff (read the diff, rebuild, rerun scoped tests, do a personal revert-check proving each fix's test is not vacuous) before merging into the primary checkout, then committing + pushing + watching CI to green each time. All five rounds' P1s are now fixed and pushed: F1-F5 (reviewer isolation, credential bypass, continuation text loss, CONNECT/DNS/DoH bounds, transport cache), R2-1..R2-3 (SOCKS5 bound, continuation-across-tools, reviewer fail-fast), R3-1 (retry/continuation evidence ownership — needed TWO passes, since the first `2169dbe3` only closed the "exact baseline match" case; round 5 found the deeper "any newer message, even a concurrent caller's" gap, closed by adding `SessionAgentCall.OnAssistantMessageCreated` so classifiers key off the exact row the current attempt created, not session-wide last-message). One noteworthy incident: mid-round-5, the delegated `/wrush` agent's session died mid-own-revert-check, leaving its worktree in a shimmed (fix-reverted) state with no final report — caught by reading `git diff --stat` directly rather than trusting the empty `final_text`/`tool_use` exit_reason, then resumed the same `--session` with explicit recovery instructions; the second pass completed cleanly and was independently re-verified as usual.

After round 5 closed with no new P1s (Review Stop Rule termination — a review finding no P1 ends the series), the user asked for and got a new feature built directly (not delegated): an optional custom exit message on a provider's peak-hours refusal window, configurable from the web UI (text field appears when the peak-hours checkbox is on), appended after a blank-line separator to `PeakHoursGuidance`'s output (the single source of truth already shared by stderr and DB finish-details) on exit. Touched `config.PeakHoursWindow` (new `Message` field), `agent.PeakHoursError`, `PeakHoursGuidance`, the WS wire protocol (`PeakHoursWirePayload`), both provider-settings React forms, and — found and fixed along the way — a real data-loss bug where `rush providers set --peak-hours HH:MM-HH:MM` would silently wipe a message set via the web UI (now preserved). Also found and fixed a genuine bug in the CLI test harness itself (`runProvidersCmdInIsolatedApp` was calling `cmd.RunE(carrier, ...)` with a `carrier` object that didn't carry the just-parsed subcommand flags, so `cmd.Flags().Changed(...)` inside RunE silently saw nothing — fixed with `carrier.Flags().AddFlagSet(cmd.Flags())`). All of this was personally implemented, tested (new unit + one CLI-level integration test), and verified with a personal revert-check — never delegated.

User then ran `/task` for a NEW, not-yet-started idea: show rate-limit/quota-exceeded messages in LOCAL time on the real `rush run` exit path, not the provider's remote time. Investigation found the fix should reuse/extract `internal/cmd/ping.go`'s existing `pingRateLimitReset`/`zaiResetRe` local-time-conversion logic (currently wired only into `rush ping`'s own text output) and apply it at the quota-classified turn-failure surface in `internal/agent`, mirroring the `PeakHoursGuidance` pattern. Filed as TaskList #979, NOT started — `/task` only plans.

Most recently, the user asked to bump the alpha version, update CHANGELOG/docs/help/skills, then commit+push+green-CI. Clarified via AskUserQuestion that the existing `alpha-0.2.0-3` git tag matches NEITHER CI publish trigger (`v*.*.*` is upstream-only/disabled; `npm-v*.*.*` triggers a REAL public npm publish) — it's an internal documentation-sync checkpoint tag, unrelated in numbering to `forkBaseVersion`/`npm/rush/package.json`'s `0.2.0-alpha.1`. User chose "just the internal repo version" (not npm-v, not a real publish). Bumped `forkBaseVersion` (`internal/version/version.go`) and `npm/rush/package.json` to `0.2.0-alpha.2`; rewrote `CHANGELOG.md`'s `[Unreleased]` section into a new `## [0.2.0-alpha.2] - 2026-09-22` release entry (folding in the pre-existing unreleased items — Codex skill installer, `/wrush` — plus new entries for auto-reviewer, network/proxy+DNS config, `--idle-timeout`, peak-hours message, web UI metadata display, and the continuation/retry correctness fixes), regenerated the stale `schema.json` (was missing the new `PeakHoursWindow.Message` field entirely, and the schema has `additionalProperties: false` so this was a real validation gap, not just cosmetic), added a `providers show` display line for the peak-hours message (CLI visibility for a web-UI-only-settable field) with a new test, and a short doc note on `providers set`'s `--peak-hours` flag about message preservation. Still IN PROGRESS at interruption: had just confirmed `internal/cmd`'s full test suite passed (33.3s, exit 0) after all these latest changes; had NOT yet committed, pushed, or checked CI. Git tag for the bump (`alpha-0.2.0-4`, continuing the existing scheme, NOT `npm-v*`) has not been created yet either.

## Active goal

None (`/goal` not armed).

## TaskList

### pending

- #979 Show rate-limit/quota reset time in local time on rush run's quota-exceeded exit, not remote time (blockedBy: none) — planned only via `/task`, not started. Investigation already done: reuse `internal/cmd/ping.go`'s `pingRateLimitReset`/`zaiResetRe`, wire into the quota-classified turn-failure surface in `internal/agent` (find exact site — likely near `agent_turn_failure.go` or wherever `classifyProviderError`'s `classTerminal` quota case becomes user-visible text), mirror the `PeakHoursGuidance` single-source-of-truth pattern. Full scope is written into the task's own description.

## Decisions

- Round 5's R3-1 finding required a genuine root-cause redesign (evidence *ownership* via a new `OnAssistantMessageCreated` callback), not a patch on the existing baseline-comparison guard — the baseline check could never distinguish "a message my own attempt wrote" from "a message anything else wrote after my baseline was taken".
- When a delegated `/wrush` session's own final report is missing/suspicious (`exit_reason: "tool_use"`, empty `final_text`), verify the actual worktree diff directly (`git diff --stat`) before trusting anything — this caught a real dead-mid-revert-check incident that would otherwise have merged reverted/broken code.
- "Bump the alpha tag" was ambiguous across three real options (internal-only tag, internal version + tag, or a real public npm publish) given the repo's actual git-tag/CI-trigger mismatch — asked rather than guessed, given the npm-publish option is irreversible and public. User picked internal-only.
- Regenerated `schema.json` by hand (`go run . schema`) rather than skipping it, since its `additionalProperties: false` makes the missing `Message` field a real validation gap for anyone using the schema, not just a docs nicety — confirmed via `.github/workflows/schema-update.yml` that this fork has the auto-update workflow disabled, so nobody else was going to do it.
- Added CLI-side (`providers show`) read-only visibility for the peak-hours message even though it's a web-UI-only *settable* field, so a CLI-only operator isn't blind to a setting they can't otherwise see.

## Open questions

- None pending on the user. The immediate next step (already decided, not yet executed) is: commit the accumulated work in logical groups, create the `alpha-0.2.0-4` tag, push commits + tag, and watch CI to green — this is what was in progress at interruption.

## Repo state

```
 M CHANGELOG.md
 M internal/agent/agent.go
 M internal/agent/agent_turn_step.go
 M internal/agent/coordinator.go
 M internal/agent/coordinator_providers.go
 M internal/agent/coordinator_retry_test.go
 M internal/agent/coordinator_run.go
 M internal/agent/peak_hours_stop.go
 M internal/agent/peak_hours_stop_test.go
 M internal/cmd/providers.go
 M internal/cmd/providers_cmd_test.go
 M internal/cmd/providers_peakhours_test.go
 M internal/config/peak_hours.go
 M internal/server/handlers_providers.go
 M internal/server/peak_hours_test.go
 M internal/server/protocol.go
 M internal/server/update_notice.go
 M internal/version/version.go
 M npm/rush/package.json
 M schema.json
 D web/dist/.gitkeep
 M web/src/components/ProvidersModal.tsx
 M web/src/store.ts
 M web/src/types.ts
```
```
6e18502f docs: add weekly release readiness audit round 5
a4f99d8b docs: add weekly release readiness audit round 4
c32c72d6 fix(lint): add missing t.Parallel to TestRetryClassifiers_AttemptScopedEvidence
2169dbe3 fix(agent): scope retry/continuation classifiers to the current attempt
8aabba4d fix(app): join continuation fragments byte-exact, never guess a break
```

Note: `web/dist/.gitkeep` shows as deleted in the working tree — this predates this session's work (present at session start per CLAUDE.md's own note that it's currently deleted and should not be committed either way without explicit instruction) and has been left untouched throughout.
