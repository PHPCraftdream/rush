# Checkpoint — 2026-09-03 08:08

## Session summary

Continuation of a multi-day session on `D:\dev\go\crush` (PHPCraftdream/rush fork, embeddable Rush SDK work). This window covered three major arcs, all now fully closed:

1. **Disk-provider feature (#856-#859), finished and merged.** A caller-injectable `internal/agent/tools.DiskProvider` interface lets the `fs_*` tool family redirect I/O to a custom filesystem instead of the real disk. Delivered via a sequential worktree-chaining topology (per explicit user instruction): each task got its own worktree branched from an accumulating `disk-provider-integration` branch, `@sh` agents did the implementation, I (orchestrator) zero-trust-reviewed every diff (read code myself, ran build/vet/test/-race myself, never trusted the agent's own report), hand-merged via `cp` (never `git merge` for sub-task merges), then merged the whole chain into `main` (`a6223924`) once all four landed. During #858/#859 I found and fixed real gaps myself: a missing Layer-3 durable-restart-refusal (`FailIfSessionBusy` wasn't forced true for `DiskProvider` calls) and a marker-string collision in a test.

2. **`/wrush` slash-command + housekeeping commits**, requested via explicit "коммит и пуш": committed a pre-existing uncommitted changeset (`/wrush` — `/rush` with mandatory git-worktree isolation, `b7563747`), a batch of session docs/checkpoints/plans (`f4e30ce4`), and a gofumpt fix for two pre-push-lint-flagged files (`be8369b1`). Push to `fork` remote was attempted but got blocked twice by the pre-push hook (a stray gitignored `tmp/before_fmt.go` breaking `go build ./...`, then a `go test`/lint issue) — fixed both root causes rather than bypassing the hook. A background push (`bc9e8brp6`) was launched but its outcome was never confirmed via notification; **`main` is currently 33 commits ahead of `fork/main`, unpushed.**

3. **SDK review round 5 — full fix chain (#860-#867), finished and merged.** User asked me to study `docs/reviews/2026-09-02-sdk-library-review-round-5-2331.md` (an independent security review of the disk-provider/folder-scope feature, originally only present in a separate `crush-research-upstream-20260830` worktree — I copied it into this repo's `docs/reviews/` during the R5-3 merge since sub-agents couldn't read a path outside their own worktree) and create tasks, then `/babygoal`'d the fix chain using the SAME worktree-chaining pattern with `@sh` agents. All 7 findings fixed and merged into `main` via `98e9f0f9`:
   - **R5-1 (P0)**: `pinCallTools` returning nil silently fell back to the shared unscoped toolset for a `FolderScope`/`DiskProvider` call instead of failing closed — fixed across all 3 call sites (`resolveSessionModelsInternal`, `applyModelOverrides`, `resolveCredentialsModels`) with `ErrScopedCallToolsUnavailable`.
   - **R5-2 (P0)**: folder-scope roots were matched lexically while requested paths were symlink-resolved, letting a nested deny carve-out be bypassed — added `tools.CanonicalizeFolderScopeSpec`, wired into `ExecuteRun` and `RebuildSessionAgentCall`.
   - **R5-3 (P1)**: durable replay restored only `FolderScope`, dropping `DisableSubAgents`/`ModelRole`/timeout policy — added `session.CallOptionsSpec` DTO, widened `RunSessionAgentCall`'s tool-rebind trigger.
   - **R5-4 (P1)**: `agent.ErrSessionBusy`/`ErrDiskProviderNotDurable` were documented for external consumers but unimportable (Go `internal/` rule) — re-exported as `sdk.ErrSessionBusy`/`sdk.ErrDiskProviderNotDurable`, proven via the pre-existing `testdata/embedder` external module (own `go.mod` + `replace` directive).
   - **R5-5 (P1)**: `fs_write` silently treated a failed pre-write content read as empty and overwrote anyway — now refuses the write, matching the existing fatal/non-fatal `MkdirAll` error convention.
   - **R5-6 (P1)**: ephemeral `sdk.ModeLibrary` had no logical filesystem root, so relative scopes/paths either hard-failed or resolved against the host process's own CWD — added `sdk.LibraryVirtualRoot` (`"/rush-library-mode-root"`), fixed `resolveScopedPath` to stop calling `filepath.Abs` on an already-`SmartIsAbs` path (Windows re-roots via `syscall.FullPath` even for driveless-absolute paths). **I (orchestrator) caught and fixed a real security regression the delegated agent introduced**: its `BuildFolderScope` change accepted `filepathext.SmartIsAbs(dir)` alone as "already resolved," but on Windows that's ALSO true for a real leading-slash-spelled entry (`{Dir: "/foo"}`) under a REAL drive-rooted `WorkingDir` — making such a deny carve-out silently go inert (same fail-open class as R5-2, different mechanism). Fixed by requiring `spec.WorkingDir` to itself be in the same driveless-virtual namespace before skipping the join; confirmed via a scratch repro before/after, pinned as a permanent test, re-verified R5-2's own tests still pass.
   - **R5-7/R5-8 (P2)**: durable rebuild dropped the persisted `Origin`; a stale doc comment on `CallOptions.DiskProvider` still said durable refusal was "a LATER task, not implemented here" (false since #859) — both fixed.
   - Final gate (#867): every affected package (`internal/agent/tools`, `internal/permission`, `internal/agent`, `internal/app`, `internal/session`, `sdk`) run individually under `-race` (never combined — this session's own standing rule after an earlier live memory-exhaustion incident), plus the external module test, all green.

Working methodology used consistently across all three arcs: **zero-trust delegation** — every `@sh` agent's diff was independently read in full by me, build/vet/test (and `-race -count=5` on new tests) re-run by me from scratch, revert-checks verified by me (not just trusted from the agent's report), before hand-merging via `cp` and committing. `internal/agent` is always run alone (never combined with `internal/session`/`internal/app`), wrapped in `capm 4g` (win-nice skill) as a safety net, per a hard-won lesson from an earlier live machine-hanging memory/CPU incident this session (not in this checkpoint's visible window, but the rule persists).

## Active goal

None. The `/babygoal` from this window ("реализуй таски с помощью @sh & worktrees") auto-cleared once its TaskList (#860-#867) emptied — confirmed via the babysit tick's own stop condition firing and self-deleting its cron job (`6cb4d45c`).

## TaskList

Empty. All of #856-#867 are completed; no new tasks pending.

## Decisions

- **Worktree-chaining topology (both arcs)**: one accumulating integration branch per feature/fix-chain; each sub-task gets its OWN worktree branched from the integration branch's CURRENT tip (not from `main`); after zero-trust verification, hand-merge via `cp` into the integration worktree, re-verify, commit; delete the sub-task worktree/branch; only merge the WHOLE integration branch into `main` once every sub-task has landed.
- **R5-6's `LibraryVirtualRoot` value** (`"/rush-library-mode-root"`, not a UNC path, not a drive letter): chosen specifically to avoid a real network-hang risk (UNC) or a real-drive collision risk, while still failing closed if ever handed to a real disk provider by mistake.
- **R5-6 regression fix's exact predicate**: `alreadyAbsInThisNamespace := filepath.IsAbs(dir) || (filepathext.SmartIsAbs(dir) && filepathext.SmartIsAbs(spec.WorkingDir) && !filepath.IsAbs(spec.WorkingDir))` — narrows the virtual-root acceptance to only fire when `WorkingDir` itself is in the same driveless namespace, restoring the original real-WorkingDir join behavior untouched.
- **Did not push** either the earlier `/wrush`+docs+gofumpt commits or the full R5 fix chain to `fork/main` — no explicit push request covered this final state; `main` sits 33 commits ahead of `fork/main`.
- **`docs/reviews/2026-09-02-sdk-library-review-round-5-2331.md` committed into this repo** (previously only in a sibling research worktree) specifically so delegated sub-agents in fresh worktrees of THIS repo could read it directly, after discovering the R5-3 agent silently couldn't.

## Open questions

- Should `main` be pushed to `fork` now? (33 commits ahead, unpushed — not yet asked this session.)
- The earlier background push task (`bc9e8brp6`, launched mid-window for the pre-R5-review commits) never surfaced a completion notification before the session moved on to the review/fix-chain work — its actual outcome is unconfirmed. A push now would supersede it either way.
- `internal/csync/maps.go:148` has a pre-existing, unrelated `go vet` finding (`JSONSchemaAlias passes lock by value`) — noted twice during this window (once by a delegated agent, once by my own final `go vet ./...`), confirmed out of scope for the R5 chain, never fixed. Flagging in case it's worth a future task.
- The pre-existing, still-uncommitted deletion of `web/dist/.gitkeep` remains untouched (per CLAUDE.md's explicit "do not commit either way without explicit instruction").

## Repo state

```
D web/dist/.gitkeep
```
```
98e9f0f9 Merge sdk-review-r5-integration: fix all 7 findings from SDK review round 5
15976210 fix(agent): restore durable Origin on rebuild + fix stale DiskProvider comment (R5-7/R5-8, P2)
336b81c0 fix(sdk,agent,permission): synthesize a stable virtual root for ephemeral library mode (R5-6, P1)
83ca7e83 fix(agent/tools): refuse fs_write when the pre-write content snapshot can't be read (R5-5, P1)
451f8f8c feat(sdk): export ErrSessionBusy and ErrDiskProviderNotDurable sentinel errors (R5-4, P1)
54a98e2b fix(agent,session): restore full CallOptions (not just FolderScope) on durable replay (R5-3, P1)
e567dd48 fix(agent,app): canonicalize folder-scope roots to close symlink deny-carve-out bypass (R5-2, P0)
a4361bb0 fix(agent): fail closed instead of widening to the shared toolset (R5-1, P0)
be8369b1 style: gofumpt internal/agent/coordinator_folder_scope_cli_test.go, internal/app/app_run_folder_scope_test.go
f4e30ce4 docs: add session checkpoints, plans and review notes
b7563747 feat(cmd): add /wrush slash-command — /rush with mandatory worktree isolation
a6223924 Merge disk-provider-integration: pluggable DiskProvider feature (#856-#859)
```

`main` is 33 commits ahead of `fork/main` (unpushed). All feature/fix-chain worktrees (`disk-provider-*`, `r5-*`, `sdk-review-r5-integration`) have been cleaned up — none remain. Unrelated worktrees still present: `.claude/worktrees/agent-*` (harness-managed), `crush-research-upstream-20260830`, `crush-verify-20260831`, `crush-wt/review-*`.
