# Checkpoint — 2026-08-30 08:59

## Session summary

Continuation of a marathon multi-day session on `D:\dev\go\crush` (PHPCraftdream/rush fork). This window had two major threads.

**Thread 1 — embeddable Go SDK (`sdk/`), fully complete and shipped.** Built via `/babygoal` + sequential `/rush` delegations following a pre-written plan (`docs/plans/2026-08-29-embeddable-library-refactoring.md`): exported the `rush run --json` wire-envelope types, split `RunNonInteractive` into `ExecuteRun` (compute) + a thin render wrapper, injected a `log.NewLogger` that doesn't hijack `slog.Default()`, built the `sdk/` facade package (`Open`/`Wrap`/`Client.Run`/`Client.RunNonInteractive`/`Client.Close`), routed the CLI's `rush run` through `sdk.Wrap(...)` so the binary and library share one code path, and proved external embeddability with a separate Go module (`testdata/embedder/`, its own go.mod + replace directive) plus a negative-import test proving `internal/` stays unreachable from outside the module. Later extended `sdk/` with `Client.SubscribeMessages`/`Client.SubscribeSessions` — thin pass-throughs to the same `pubsub.Event[T]` stream that already feeds the web UI's WS layer, giving embedders live structured message/session events "as-is" to relay into their own API/protocol, per explicit user requirement (not just flat token text). Every phase was independently verified by me (diff review, golden-envelope test, full package test runs) before being marked complete — never trusted a delegate's self-report. All of this is committed and pushed: `020cb174` (main feature) → `964ec75d` (staticcheck fix) → `d19320ab` (unrelated go.sum cleanup, found blocking CI).

**Thread 2 — chronic CI `build` workflow failure, root-caused as far as possible without GitHub-support-level runner access.** After pushing thread 1, `build`'s `go test -short -race -failfast ./...` step died with SIGTERM (exit 143) on `ubuntu-latest`, ~2-3 minutes in. Extensive investigation (many push-and-observe cycles, later escalated to a `/oxx` high-effort contemplation pass) systematically ruled out: `-race` itself (removing it entirely reproduced the identical failure on the plain `go test -short -failfast ./...`), parallelism (`-p 2` changed nothing), a cold build cache (`setup-go`'s cache reported a hit, ~1.1GB restored, right before the kill), any specific hung test (`-v` never printed even one `=== RUN` line before the kill — death happens before any test starts, i.e. in the compile+link phase), and a code-level bug (hand-reviewed every `internal/` package-level `init()` — none touch network/blocking I/O at init time; confirmed `testdata/embedder` is genuinely excluded from `go list ./...`, 51 packages exactly). Working theory: `go test ./...` must *link* one binary per package (unlike `go build ./...`, which never links library packages and reliably finishes in ~14s) — with ~50 packages this is a large one-shot link workload, and something in the runner's environment (never fully identified — GitHub Actions doesn't expose enough diagnostic surface via API/logs to pin it further) kills the job before that finishes, historically pre-existing since 2026-08-25 across many unrelated commits. Fix applied (commit `ae3ee46e`, just pushed as of writing): the test step now isolates `internal/agent`/`internal/app`/`internal/server`/`internal/session` (measured locally this session at up to ~193s/~117s/~92s each) into solo batches, and auto-splits the remaining ~47 packages (computed from `go list ./...` at run time, not hand-maintained, so future packages are never silently dropped) into 4 further batches — shrinking the max single link-workload well below the observed kill window. Verified the batching script's *logic* locally (coverage is exact: 4 solo + 47 batched = 51) and a full real local run passed except one already-known load-sensitive flake (`TestP2_3_ManualCompactionWatchdogCatchesIdleStall`, a hardcoded 10s wall-clock deadline — reproduced clean 3/3 in isolation at 0.75s once machine load eased, not a real regression).

**Currently in flight at checkpoint time:** `git push fork main` for commit `ae3ee46e` is running in the background (`.githooks/pre-push` mid-way through its own `go test -short -failfast ./...` stage — this is the *local* pre-push hook, unaffected by the CI batching change since that only touches `.github/workflows/build.yml`, not `.githooks/pre-push`). Once it lands, the next step is watching the GitHub Actions `build` run for commit `ae3ee46e` to confirm the batching actually fixes the chronic failure — **this has not yet been confirmed working**, only reasoned through and locally logic-tested. `lint` and `security` workflows have been reliably green throughout this whole CI investigation; only `build`'s test step was ever in question.

Also found and cleaned, mid-session, three stray empty files (`Advanced`, `App`, `Apps`) accidentally created in the repo root by a failed local `python3` invocation (Windows' python-launcher-not-found stub message apparently got mis-parsed as shell redirects) — deleted, untracked, zero risk, not part of any commit.

## Active goal

None. No `/goal` Stop hook armed.

## TaskList

Empty — confirmed via `TaskList` call, no tasks found. (The 8-task `/babygoal` plan for the sdk/ work — tasks #783-#790 — all completed and were not recreated; the CI investigation was handled ad hoc without task tracking since it was a single continuous diagnostic thread, not a multi-step decomposition.)

## Decisions

- Committed the sdk/ work as one large, cohesive commit (`020cb174`) rather than splitting per-phase, since all phases were verified together as one continuous unit of work and the working tree had accumulated all changes together without incremental commits along the way.
- Ruled out `-race` entirely from CI (`5aedea13`) rather than trying to preserve it in a throttled form, once proven it wasn't even the actual cause of the kill (removing it reproduced the identical failure) — keeping it would have added complexity for zero benefit; `-race` is now a local-only developer practice.
- Chose to hand-isolate the 4 known-heaviest packages into solo CI batches (based on real measured local timings from this session) rather than a purely dynamic/automatic load-balancing scheme, judging that a small amount of documented, explicit curation for known outliers is safer than hoping a generic split accidentally avoids clustering them — while keeping the remaining ~47 packages on a fully dynamic `go list ./...`-driven split specifically to avoid the "new package silently uncovered" drift risk that a fully hand-maintained list would carry.
- Did not chase the CI investigation to a fully pinned root cause (the exact external kill mechanism remains formally unidentified) once every code-level hypothesis was exhausted and the evidence converged on "large one-shot link workload vs. an opaque runner ceiling" — judged further diagnosis would require GitHub-support-level runner access I don't have, and the batching mitigation is a robust, low-risk hedge regardless of the exact mechanism.
- Deleted the three stray empty root-level files immediately on discovery rather than leaving them for later cleanup, since they were zero-risk untracked noise with an identifiable (if slightly bizarre) cause.

## Open questions

- **Did the CI batching fix (`ae3ee46e`) actually work?** Not yet confirmed — the push was still running `.githooks/pre-push` locally at checkpoint time, and the resulting GitHub Actions `build` run has not been watched yet. This is the very next thing to check once the session resumes.
- If the batching fix does NOT resolve the kill, no further hypothesis is queued — the next escalation would likely require either GitHub support involvement or accepting a permanently-red/best-effort `build` gate for the race-free full-module test step.

## Repo state

```
 D web/dist/.gitkeep
?? docs/checkpoints/2026-08-26-2015.md
?? docs/checkpoints/2026-08-27-0749.md
?? docs/checkpoints/2026-08-28-1210.md
?? docs/checkpoints/2026-08-28-1349.md
?? docs/checkpoints/2026-08-30-0859.md
?? docs/plans/2026-08-27-agent-tree-backend-requirements-response.md
```
```
ae3ee46e ci(build): split go test into batches to avoid the silent-kill window
5aedea13 ci(build): drop -race from CI, keep it as a local-only practice
20081a86 ci(build): temp -v diagnostic for the race-test SIGTERM kill
f4bd9cb8 ci(build): cap go test -race parallelism to fix a memory-driven kill
d19320ab chore(deps): prune stale go.sum entries via go mod tidy
964ec75d fix(sdk): drop redundant type declaration flagged by staticcheck
020cb174 feat(sdk,app,log,cmd): add embeddable Go library facade (sdk/)
6c09b365 docs(changelog): fix duplicate Fixed heading, move GLM-5.3 entry to Added
```

`ae3ee46e` is being pushed to `fork/main` right now (background task, not yet confirmed landed at checkpoint write time — check `git log --oneline -1 fork/main` after resuming to confirm, then watch the GitHub Actions `build` run for that commit).
