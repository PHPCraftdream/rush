# Checkpoint — 2026-08-27 07:49

## Session summary

Continuation of a marathon session on `D:\dev\go\crush` (PHPCraftdream/rush fork). This window (after resuming from an earlier checkpoint) covered three threads:

1. **GLM-5.3/5.3-Flash effort-vocabulary fix + two review waves.** User reported `rush models use --smart zai/glm-5.3-flash@high` failing; investigation found the prior session's fix had wrongly assumed a boolean off/on vocabulary for GLM-5.3/5.3-Flash, when both actually reject disabling reasoning entirely and expose low/high/max (verified via docs.z.ai). Fixed across the three places a SYNC WARNING convention requires (models_atoms.go's ReasoningLevels, coordinator_providers.go's wire switch, models_efforts.go's docs) plus the identical wrong assumption in web/src/effort.ts. Separately found and fixed a more serious bug in `internal/config/load_providers.go`'s `configureSelectedModels`: it silently reverted-and-persisted-over any smart/fast model selection not in the cached catalog on EVERY config load (even read-only `rush models state`), making the whole "accept an unverified model" feature from an earlier session practically dead on arrival — fixed via a gated `unverifiedPassthroughModel` helper. Ran two waves of 4 parallel `/rush` review agents each (8 total) against the diffs; each wave found real bugs that got fixed and reviewed further, including a subprocess-shelling regression in a new stale-effort detector, a UI effort-clamp inconsistency between two components, disk-persistence test assertions that were checking the wrong config-scope file entirely, and — most seriously — a FOURTH, previously-missed synthesized-catalog entry for glm-5.3 in load_providers.go still carrying the old wrong vocabulary (affects worker/reviewer slots specifically). Committed as `e88fa050` and `99c29aa9`.

2. **Root-caused and fixed the recurring "Windows console window sometimes flashes" bug** (`/oh` investigation, `/oxx` fix). The user's own evidence (windows also appearing during zai/glm sessions, where the external-CLI `cliprovider` path is never invoked at all) disproved the initial hypothesis report's P0 (external CLI bypassing yolo/allowedTools). Root cause instead: `SysProcAttr.HideWindow` does NOT set `CREATE_NO_WINDOW` — verified directly against Go 1.26.3's `syscall/exec_windows.go` source — despite two comments in this codebase explicitly (and wrongly) claiming it does. `HideWindow` only sets `STARTF_USESHOWWINDOW|SW_HIDE`, a create-then-hide race that loses under load. Empirically reproduced end-to-end on the user's own machine (parent calls `FreeConsole()` exactly like `cmd.maybeDetachConsole` does, then spawns a grandchild): HideWindow-only showed a real non-zero HWND; HideWindow+CREATE_NO_WINDOW showed HWND=0. Fixed `internal/platform/hide_window_windows.go` to set both flags, routed `internal/shell/exec_windows.go`'s inline SysProcAttr through the same sanctioned constructor, corrected the false comments, and added `hide_window_windows_test.go` (confirmed non-vacuous by temporarily reverting the fix and observing the expected failure). Deliberately left the ConPTY spawn path untouched (CREATE_NO_WINDOW previously measured to break it). Committed as `f69572b5`.

3. **CI investigation and fix** (user: "наладь CI"). Discovered via `gh run list --repo PHPCraftdream/rush` that `lint` had failed with `startup_failure` (0-1s, "workflow file issue") on EVERY push for at least 2 days. Root cause: `lint.yml` called a reusable workflow owned by `charmbracelet/meta` (a different GitHub org) — the file is plain-HTTP readable but GitHub's separate "reusable workflow access" permission apparently restricts it to callers within that org, so any external fork gets an instant startup_failure regardless of the file's public readability. Rewrote `lint.yml` to be self-contained, mirroring `.githooks/pre-push`'s own golangci-lint invocation (same pinned v2.13, same 3-OS matrix, plus the linux/mips64 pass for the ncruces SQLite driver branch). Verified live via `gh run watch`: all 4 lint jobs now pass — first time in at least 2 days. Committed as `1e264cc1` (+ a tiny follow-up `ee03f26e` fixing a `noctx` lint finding in the new test file that the first push attempt caught). Separately investigated `build`'s failures and confirmed via `gh run list --workflow build.yml` history (mixed pass/fail going back to before any of today's commits, e.g. 2026-08-25 dependabot runs) that these are PRE-EXISTING, intermittent, environment-driven flakiness — Windows fails with `ERROR_COMMITMENT_LIMIT` (error 1455) inside ThreadSanitizer under `-race` (already independently diagnosed earlier in this same session as an environmental pagefile/commit-charge limit, not a code bug), macOS fails with `signal: killed` after an intentionally-panicking recovery test, and Ubuntu's matrix leg gets cancelled by GitHub's default `fail-fast: true` before it even gets a real signal. Not fixed — flagged to the user as a separate, open-ended flaky-test investigation (this repo's git history already shows several prior "resolve N CI/test flakes" commits, suggesting this is a recognized recurring category of work, not a one-line fix).

All four commits (`e88fa050`, `99c29aa9`, `f69572b5`, `1e264cc1`, `ee03f26e` — 5 total, see repo state below) are pushed to `fork/main`. `lint` CI is confirmed green. `build` CI is red but pre-existing/environmental, not a regression from this session's work — last message to the user ended by asking whether to invest more time debugging the `-race` flakiness now or defer it.

## Active goal

None. No `/goal` Stop hook armed.

## TaskList

Empty — confirmed via `TaskList` call, no tasks found.

## Decisions

- Model-effort fix scope: corrected the vocabulary everywhere the SYNC WARNING convention requires (atoms, wire switch, docs, web) rather than just the originally-reported GLM-5.3-Flash case, since GLM-5.3 (non-flash) shared the identical wrong assumption.
- Used two waves of 4 parallel `/rush` review agents (not the Agent-tool subagent shortcuts, per this session's earlier standing correction) rather than a single broad review pass, to get independently-scoped, adversarial coverage of both the original diff and the fixes applied in response to the first wave.
- Windows console-window fix: keep BOTH `HideWindow` and `CREATE_NO_WINDOW` (not just the newly-added one) since `HideWindow` still covers GUI-subsystem children that ignore `CREATE_NO_WINDOW`. Deliberately did NOT touch the ConPTY spawn path in `internal/agent/cliprovider` — `CREATE_NO_WINDOW` was previously measured to break that pseudo-console outright.
- CI lint fix: rewrote `lint.yml` to be fully self-contained rather than trying to find a workaround for the cross-org reusable-workflow restriction (e.g., vendoring/forking the reusable workflow) — matches the fork's own established pattern of not relying on upstream/other-org CI infrastructure it doesn't control.
- Did NOT attempt to fix the `build` `-race` flakiness in this session — assessed as a separate, potentially open-ended investigation (multiple distinct failure modes across 3 OSes, one already independently root-caused as environmental) rather than a quick fix, and explicitly asked the user before investing further time.

## Open questions

- Whether to invest more time now debugging `build`'s `-race`-related CI flakiness (Windows `ERROR_COMMITMENT_LIMIT`, macOS `signal: killed` after a panic-recovery test, Ubuntu's matrix-cancellation hiding its own signal) — asked at the end of this session's last message, not yet answered.

## Repo state

```
 D web/dist/.gitkeep
?? docs/checkpoints/2026-08-26-2015.md
?? docs/checkpoints/2026-08-27-0749.md
```
```
ee03f26e fix(platform): use exec.CommandContext in the new console-window test (noctx)
1e264cc1 fix(ci): stop lint.yml depending on an inaccessible reusable workflow
f69572b5 fix(platform,shell): actually suppress spawned console windows on Windows
99c29aa9 fix(agent,cmd,config,web): close a stale GLM-5.3 catalog entry and clamp/persist gaps found in second-wave review
e88fa050 fix(agent,cmd,config,web): correct GLM-5.3/5.3-Flash effort vocabulary, stop config load from discarding unverified models
```

All of the above are pushed to `fork/main` (confirmed: `fork/main` at `ee03f26e` after the retry push succeeded and CI's `lint` workflow was observed green via `gh run watch`).
