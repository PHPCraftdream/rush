# Checkpoint — 2026-09-03 14:56

## Session summary

Continuation of a multi-day session on `D:\dev\go\crush` (PHPCraftdream/rush fork, embeddable Rush SDK). This window closed the SDK review round-6 fix chain, survived and root-caused a series of severe local memory crashes, pushed the accumulated work to the fork remote, monitored and fixed real CI failures on the very first push of this code to GitHub Actions, and processed two further independent reviews (round 12, round 13).

**Round-6 fix chain (#868-#872):** studied `docs/reviews/2026-09-03-sdk-library-review-round-6-0808.md`, found 4 confirmed findings (1 P0, 2 P1, 1 P2), fixed each via worktree-chaining onto `sdk-review-r6-integration` (branched from `main`), using `@sh` sub-agents with zero-trust verification (every diff read personally, build/vet/test re-run personally, revert-checks re-proven personally). Found and fixed two real gaps beyond the delegated agents' own drafts: R6-1's disabled-tools list missed the `download` tool (writes straight to real disk, no DiskProvider seam); R6-3's own sub-agent got interrupted mid-verification by the first two memory crashes and had to be resumed twice. Merged the whole chain into `main` via `80b6db11`.

**Severe memory-crash investigation (mid-session):** three consecutive OOM-class crashes while running `go build`/`go test` locally. Root-caused precisely, not just worked around:
1. First crash: ran an uncapped `go test` combining `internal/agent/tools` + `internal/permission` (violates the standing "never combine internal/agent-family packages" rule) — actual cause was a Windows commit-limit hit (`VirtualAlloc failed errno=1455`), not a real leak.
2. Second/third crashes: discovered win-nice's `capm.ps1` (the machine's own memory-cap tool) has two structural gaps — (a) `WaitForSingleObject(pi.hProcess, INFINITE)`, no timeout at all, so a hung child waits forever; (b) never sets `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`, so killing the wrapper process does NOT cascade-kill grandchildren already in the job (confirmed empirically: `taskkill /T /F` and `Stop-Process -Force` both reported success while a >1GB orphaned `link.exe` kept running — only a direct WMI `Win32_Process.Terminate()` call actually killed it). Root cause of the repeated "unkillable process" pattern was my OWN mistake: retrying a hung `go test` (the external embedder module test, `testdata/embedder`) three times in a row without confirming the PREVIOUS attempt's linker had actually died first — each retry added another linker competing for the same shared `$GOCACHE`, and none of them could ever be reliably killed.
3. **Fix, not just diagnosis:** wrote `C:\Users\Computer\AppData\Local\safego\safego.ps1` — a from-scratch replacement for `capm` with the same Job-Object memory-cap mechanism PLUS the two missing guarantees: a hard wall-clock timeout (calls `TerminateJobObject` on expiry — a single, unconditional kernel-level kill of every process ever in the job, no process-tree walk to get wrong) and `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE` set from the start. Tested all four scenarios personally (normal success, timeout-kill including a grandchild, external kill of only the top-level wrapper process cascading correctly, and the memory cap itself triggering `OutOfMemoryException`) before adopting it as the mandatory replacement for `capm` in this project going forward. One follow-up cosmetic bug (exit-code-forwarding overflow for a crashed child) found and fixed during testing.

**First-ever push + CI (`git push --no-verify fork main`, explicitly requested "без хуков"):** pushed ~44 accumulated commits. Watched `build`/`lint`/`security` GitHub Actions workflows. Found and fixed three real, independent problems, all confirmed via re-running locally before each fix:
- A `gofumpt` formatting issue in `internal/session/session_runqueue.go` (pre-existing since R5-3, never caught locally since only build/vet/test had been run this session, never the full lint suite).
- A genuine cross-platform test bug in `internal/agent/r5_2_rebuild_folderscope_symlink_test.go`: the test's `sibling` path was compared against `Check()` without going through `filepath.EvalSymlinks` first (unlike its sibling `resolvedItem` variable, which correctly does), so on platforms where the temp directory itself sits under a symlinked path component (macOS's `/var` → `/private/var`; GitHub's Windows runners' `D:\a` junction), the compiled scope's canonicalized root and the raw `sibling` diverged. Failed on `windows-latest` and `macos-latest`.
- A genuine Windows-only test-portability bug in `internal/permission/r5_6_leading_slash_regression_test.go`: hardcoded a `D:\real-project` WorkingDir literal and tested the `filepath.IsAbs`-vs-`SmartIsAbs` ambiguity that ONLY exists on Windows (on Unix, `/foo` is unambiguously absolute, so the whole R5-6 regression class this test protects against cannot occur there). Split the file: the Windows-only test moved under `//go:build windows` into a renamed file; the genuinely cross-platform sibling test moved to its own new file with no build tag. Failed on `ubuntu-latest`.

Also discovered mid-investigation that `main`/`fork/main` had received several commits from ANOTHER, entirely independent concurrent process during this session (`53aa30d9`/`cec10b5d` memory-leak fixes — confirmed via `git log` timestamps to be a full day OLDER than believed, from a prior, unrelated incident; `b177206a`, `059b5740`, `76c883bc` — external "review round N" documentation commits appearing on `main` without my involvement). No conflicts resulted; git's own linear history absorbed all of it cleanly on every push.

CI is now fully green (`build`, `lint`, `security` all passing) after two follow-up pushes (`1092f328` gofumpt fix, `73878311` the two test-portability fixes).

**Round-12 review** (`docs/reviews/2026-09-03-round-12-1124.md`): 5 of 7 findings self-corrected by the reviewer as not-confirmed/accepted-risk/documentation-gap on careful re-reading; 2 genuine documentation gaps (R12-2: `/wrush` has no runtime guard, trust-boundary-only; R12-7: `/wrush` template didn't explain the two delegation layers) fixed directly (small worktree, done myself, not delegated) and merged via `e5869ef3`.

**Round-13 review** (`docs/reviews/2026-09-03-round-13-1328.md`): checked for adequacy per explicit user request — found the review's own "M1-M4 memory-leak fixes" section factually mischaracterizes day-old, already-merged, unrelated-incident commits as "new work since round 12" (confirmed via commit timestamps: `53aa30d9`/`cec10b5d` predate round 12's base commit by nearly a full day). Everything else in the review (R6-1..4, R12-2/R12-7) independently confirmed as correctly already-closed. The ONE genuinely open, adequate finding — R13-1 (`FromSessionAgentCallData`'s test-only converter silently discards a `CallOptionsSpec` version-mismatch error) — was turned into task #875 and solved via the newly-adopted `/wrush` methodology for the first time this session: dedicated worktree, `rush run --role smart` launched in the background (used 2 internal worker sub-agents via its own orchestrator mode), then zero-trust verified personally (full diff read, build/vet/test re-run, a REAL personal revert-check reproducing the exact failure message before restoring the fix) before hand-merging into `main` via `4ef3ebcd`. Not yet pushed (no explicit push request for this specific commit).

**Methodology note for future sessions:** this window formally adopted `/wrush` (`rush run` in a mandatory dedicated git worktree, per `internal/cmd/claude_wrush_command.md`) as the standard delegation mechanism going forward, replacing the `Agent(subagent_type: 'sh')` pattern used for the R6 chain — per explicit user instruction mid-session ("далее для работы используем агентов /wrush"). All `go build`/`go test`/`go vet` invocations, whether run personally or inside a delegated `rush run`, must go through `safego.ps1` (never bare commands, never plain `capm`) given the machine's now-confirmed fragility to unkilled orphaned build/link processes.

## Active goal

None.

## TaskList

Empty. #868-#875 all completed this window.

## Decisions

- **`safego.ps1` supersedes `capm` for this project.** Written this session specifically because `capm` lacks a timeout and `KILL_ON_JOB_CLOSE`, both empirically proven necessary after repeated unkillable-orphan incidents. Lives at `C:\Users\Computer\AppData\Local\safego\safego.ps1` (a personal machine-wide tool, not committed to the repo). Usage: `powershell -NoProfile -ExecutionPolicy Bypass -File "C:\Users\Computer\AppData\Local\safego\safego.ps1" <size> <timeoutSec> <command...>`.
- **`/wrush` (not `Agent(subagent_type:'sh')`) is now the standard delegation mechanism**, per explicit user instruction. Task #875 was the first task run this way this session; the pattern (dedicated worktree, `rush run --role smart` in background, zero-trust hand-merge, worktree+branch cleanup) worked cleanly and is the template for future delegated work.
- **`internal/agent` must always run alone**, never combined with `internal/session`/`internal/app`, and always with `-parallel 2 -p 1` on this machine (172+ `t.Parallel()` sites at default parallelism was a direct contributor to at least one of the memory crashes this session).
- Two CI-only test-portability fixes (the symlink-EvalSymlinks gap, the Windows-only build-tag split) were judged genuine bugs worth fixing immediately (per the standing "any test failure is fixed on the spot" rule), not deferred to a task, since they were actively blocking the just-pushed CI run.
- Did not push commit `4ef3ebcd` (task #875's merge) — no explicit push request covered it specifically; the two CI-fix pushes earlier in this window WERE explicitly requested ("пуш игнор (без хуков), проконтролируй ci").

## Open questions

- Should `main` (currently 1 commit ahead of `fork/main` — just `4ef3ebcd`) be pushed now?
- The pre-existing `internal/csync/maps.go:148` `go vet` finding (`JSONSchemaAlias passes lock by value`) remains unfixed, out of scope for every task this session has touched. Still flagging in case it's worth a dedicated task someday.
- The pre-existing, still-uncommitted deletion of `web/dist/.gitkeep` remains untouched (per CLAUDE.md's explicit "do not commit either way without explicit instruction").
- Six `.claude/worktrees/agent-*` directories are present and NOT mine — appear to belong to other, harness-managed concurrent agent activity on this same machine (plausibly the same external process(es) responsible for the unexplained `53aa30d9`/`cec10b5d`/review-round commits appearing on `main` without my involvement). Not investigated further; flagging as a known, unresolved multi-agent-concurrency observation for this machine.

## Repo state

```
 D web/dist/.gitkeep
```
```
4ef3ebcd fix(agent): propagate CallOptionsSpec version error through FromSessionAgentCallData (R13-1, P2)
73878311 fix(test): two real CI failures on first-ever GitHub Actions run, both test bugs not code regressions
b177206a review: round 13 - 1328 — all R6 findings closed, memory-leak fixes verified, no open P0/P1
1092f328 style: gofumpt internal/session/session_runqueue.go
80b6db11 Merge sdk-review-r6-integration: fix all 4 confirmed findings from SDK review round 6
e5869ef3 docs: clarify /wrush trust boundary and worktree/delegation-layer interaction (R12-2, R12-7)
```

`main` is 1 commit ahead of `fork/main` (unpushed: `4ef3ebcd`). No worktrees or branches of mine remain outstanding — `sdk-review-r6-integration`, `r12-wrush-docs`, and `r13-1-fromcalldata-version` were all cleaned up after their respective merges. `.claude/worktrees/agent-*` (6 dirs), `crush-research-upstream-20260830`, `crush-verify-20260831`, `crush-wt/review-*` are unrelated, pre-existing, not mine.
