Search file contents by regex or literal text across a scoped directory tree, returning each hit as a numbered block with up to N lines of surrounding context; overlapping or adjacent context windows in the same file are merged into one block. Batch-capable: pass several items to run multiple searches in one call. Every match path is checked against the folder scope and out-of-scope matches are dropped from the output. Use include to filter by filename glob, not contents.
