# Checkpoint — 2026-08-23 13:15 [tenth-review-backlog-closed-eleventh-running]

## Session summary

Continuation of the long-running `/babygoal` on rerun/session-lock concurrency
fixes. Session resumed from checkpoint `2026-08-20-1132.md` (the eighth/ninth
review backlog was already closed at that point, commit `3b2664da`). This
session's arc: the tenth review ran against `c65889a1` and found seven items
(F-1 through F-7); all seven are now closed, plus one self-inflicted
regression caught before merge and one side-discovery from chasing a flake.
The eleventh review is running now (backgrounded) against the resulting
`HEAD` (`28d55c33`).

The user reinforced the `/babygoal` condition mid-round with an explicit
addendum: **"не запускай ревью, пока есть хоть одна не решённая задача"**
(don't run review while even one task is unsolved). This was necessary
because I ran the tenth review once already with two unblocked-but-unstarted
tasks still open — the Stop hook caught it and I had to restart the closing
sweep before re-running review. The addendum is now carried in the active
goal text verbatim (see below) and must be respected for any future round in
this session.

Working discipline held throughout: every worker diff was read and
independently revert-checked by me before merging — never trusted on the
worker's own report. This caught real problems multiple times this round:
`d4e64288`'s worker deleted an unrelated `defer runCancel()` and then
described it in its own report as pre-existing (caught by diffing against
HEAD); `ffbe34af` exists because an interface-shaped change was verified
per-package instead of module-wide, breaking `internal/app`'s test build
while `go build ./...` stayed silent; the first version of `#622`'s /
`bbb258dd`'s fix (found across three separate design iterations —
byte-heuristic, refined byte-heuristic, then a full redesign to a kernel
lock probe after consulting `@fh`) each had a real hole I found by reading
the diff, not by trusting the report; a `poolMu`-narrowing fix (`28d55c33`)
had a genuinely buggy first revert-check attempt (self-deadlock) that the
worker itself caught and redid honestly.

Two multi-round design escalations happened this session, both worth
remembering for a stranger picking this up cold:

1. **Task #622/#631's rerun-lock defect** went through FOUR complete design
   iterations before landing structurally sound: (a) `ce3b418e` — check
   `InspectSessionLock` once before deleting (residual TOCTOU, the "owner
   arrives a moment later" window, explicitly left open); (b) the tenth
   review's F-1 found that check ALSO false-positived on the process's own
   just-released lock for ~20s post-release (mtime refresh + missing PID
   sidecar look identical to a live-but-anonymous external owner); (c) a
   worker narrowed that window by reordering writes in the acquire path
   (write PID sidecar before truncate) — narrowed but not closed, and I
   independently found a FOURTH state (the mid-acquire window itself) the
   narrowing didn't cover; (d) consulted `@fh` (design-review sub-agent) at
   the user's explicit request, which recommended abandoning byte-inference
   entirely in favor of a kernel-attested non-blocking SHARED lock probe
   held across the destructive operation — `bbb258dd` implements this, and it
   is structurally different from every prior attempt (asks the OS instead of
   reading state left behind on disk). This is the single most scrutinized
   commit in the batch and the eleventh review was told to focus hardest on
   it.
2. **Task #630's cancel-window defect** similarly needed two rounds: the
   first fix made the tail-deletion loop atomic (cancel honored only at a
   phase boundary, not mid-loop) but left the SAME defect one step later —
   a cancel during the target-message delete (step 3) could delete the
   user's own prompt with no replacement turn started. `ca40acbf` fixes this
   by establishing one explicit "commit point": the last honored
   cancellation is before step 3; everything from there through the handoff
   runs under `context.WithoutCancel` and is guaranteed to reach
   `RunWithReservedOwnership`.

Flake-hunting this round (task numbers #618/#620/#627/#628/#632/#636/#637)
mostly ended in legitimate "did not reproduce" or "oracle was insufficiently
specific" outcomes rather than manufactured fixes — consistent with the
standing discipline in this codebase (see `internal/session/p611_*` for the
canonical prior example: two lease-loss tests that were green for a reason
unrelated to their name). One flake chase (`#636`, `TestDispatchControlDoesNotStarveWorkQueue`)
surfaced a real, unrelated, confirmed-by-reading defect as a side effect:
`internal/db/connect.go`'s `poolMu` was held across an ENTIRE `goose.Up`
migration run for a brand-new database, serializing every unrelated `dataDir`
in the process behind whichever one happened to be migrating — not a
deadlock, but a severe unintended serialization that manifested as a
whole-package 10-minute test timeout under `-race`. Fixed in `28d55c33` with
a per-path `sync.Map` of mutexes; `poolMu` itself now only guards the pool
map's bookkeeping. I personally reverted the fix and reproduced the exact
30-second block it was designed to prevent before accepting it.

One process-hygiene note for whoever resumes: the `crush` provider (z.ai,
`smart` role) had a bad stretch of `TLS handshake timeout` failures mid-round
— three-to-four consecutive retries needed on two separate tasks (`#636`,
`#637`) before a run went through. Per the `/crush-fallback` skill's own
rules, a transient network blip is NOT one of the two fallback triggers
(peak-hours refusal, hard quota limit), so I did not switch to the armed
`sh` fallback agent (task `#601` is `armed`, not `active`) — I just kept
retrying into the same session id, which eventually succeeded both times.
If this recurs and escalates into an actual hard-limit or peak-hours refusal,
`#601`'s fallback is already armed and ready.

I also investigated (at the user's explicit request) why two background
`crush run` processes were externally killed with zero output early in this
round (`#634`, `#636` first attempts). Established by file-mtime evidence:
two kills happened five seconds apart with completely empty `.out`/`.err`
files, and a third kill happened alone 22 minutes later — consistent with an
external stop signal, not a crash or resource exhaustion, but I do not have
visibility into the Claude Code harness/UI layer to confirm who or what
issued it. Reported honestly as "observed pattern, cause outside my tools'
visibility" rather than guessing. Both interrupted tasks were resumed from
their partial state (one had a complete, mergeable diff already; I finished
its verification and commit myself) and eventually completed.

## Active goal

The `/babygoal` Stop hook, verbatim (re-armed once this session, with an
explicit addendum appended by the user after the first review attempt was
premature):

> реализуй с помощью агентов /crush & worktrees, в конце (когда решишь все
> таски) проведи ревью с помощью агента @oh - после ревью заведи таски и
> остановись, между тасками делай коммиты. (не запускай ревью, пока есть
> хоть одна не решенная задача)

**Status: the TaskList is fully closed** (only the permanent sentinel #601
and the explicitly-unauthorized #617 remain `pending` — see below for why
neither counts against the goal). The eleventh review is running now
(backgrounded, agent id `af78f0b46e528bfa5`) against `HEAD` (`28d55c33`).
The goal is NOT yet satisfied — it still requires: review completes → new
tasks filed from its findings → session stops. Do not stop before the
review agent's result comes back.

## TaskList

### in_progress
None.

### pending
- #601 crush-fallback state (persistent — do not complete): sh — armed. Sentinel, not work; never counts against "all tasks solved".
- #617 Design: give the durable row one typed outcome end to end — filed by four separate reviews now, still NOT authorized by the operator. Do not start without explicit go-ahead.

### recently completed (this session, chronological)
- #628 lease-renewal flake — investigated, oracle proven sound, did not reproduce, no change.
- #630 rerun cancel-window truncation — two rounds, see session summary.
- #631 own-released-lock false-positive — four design iterations, see session summary. THE release blocker; closed by `bbb258dd`.
- #632 hub-drain-wait flake, 8 tests failing together — root-caused to a 5s wall-clock bound racing transient stalls; fixed with a condition-based wait; self-inflicted unbounded-wait regression caught and fixed same round (`ecaff5f8`).
- #633 five more comment-vs-code divergences (three survived a prior cleanup pass, two minted during it) — `93167971`.
- #634 `InspectSessionLock`'s stat-error fail-open — severity downgraded mid-round once `bbb258dd` moved the destructive path off this function entirely; closed as a diagnostics-only fix (`sessions why` reporting) in `cdb0b7cc`. Test forgery technique (file-as-directory) initially chosen was empirically wrong on Windows (`os.IsNotExist` classifies `ERROR_PATH_NOT_FOUND` as true there) — replaced with a NUL-byte-in-path forgery, verified portable.
- #636 dispatch-overload flake — did not reproduce across ~156 targeted + 6 full `-race` package repetitions; oracle independently checked against the `c65889a1` defect class and found sound. Side-discovery: #637.
- #637 `poolMu` over-broad lock scope — discovered chasing #636, confirmed by reading, fixed and personally revert-checked (`28d55c33`).
- Full ninth/tenth-review backlog (#593–#635 minus the above, per the prior checkpoint).

## Decisions

- **The `@fh` design consultation for #622/#631 was requested explicitly by the user** ("спроси у @fh как это решить") after I had already produced a narrowed-but-not-closed fix; I did not self-initiate the sub-agent call (standing rule: never call sub-agents without explicit request). Its recommendation (kernel lock probe over byte inference) was accepted and implemented as `bbb258dd`.
- **Transient network failures (TLS handshake timeout) are retried into the same session, never escalated to the `sh` fallback**, because they match neither of `/crush-fallback`'s two defined triggers (peak-hours, hard-limit). This is a strict reading of that skill's own rules, applied consistently across `#636` and `#637`'s retries.
- **`#617` remains unauthorized.** Four consecutive reviews (eighth through what would be the tenth's aftermath) have pointed at it; I have not started it and will not without the operator's explicit go-ahead.
- **A commit message paste error was fixed via `--amend`, not a new commit** (`28d55c33` briefly carried `bbb258dd`'s commit message due to a heredoc copy-paste mistake, caught immediately after `git commit`, corrected before anything was pushed — nothing shared was rewritten).
- **`#634`'s severity was actively downgraded mid-round**, not silently carried forward at its original assessment, once `bbb258dd` made its premise (feeding a destructive decision) stop being true.

## Open questions

- **The eleventh review's outcome is unknown as of this checkpoint** — it is running in the background. Its result should be read in full before deciding next steps; do not assume GO or NO-GO.
- **Why the two `crush run` processes were killed externally with empty output is still unresolved** — I reported the observed pattern (near-simultaneous, zero-output) but have no tool-level visibility into the cause. If it recurs, the same evidence-gathering approach (compare `.output` file mtimes across the task-notification IDs) is the fastest way to characterize it again.
- **`#617` still needs an explicit operator decision** — authorize, defer, or reject. It has not been re-raised as a question in a while because the answer hasn't changed, but it is the single most persistent unresolved design question in this whole series.

## Repo state

```
 D web/dist/.gitkeep
?? dev/
?? docs/checkpoints/2026-08-20-0810.md
?? docs/checkpoints/2026-08-20-1132.md
?? docs/reviews/2026-08-20-ninth-review-3b2664da.md
?? docs/reviews/2026-08-20-tenth-review-c65889a1.md
?? docs/reviews/2026-08-20_07-28-07-readonly-release-review-a70d1b73.md
```

```
28d55c33 fix(db): stop serializing unrelated databases behind one migration
cdb0b7cc fix(session,cmd): tell "could not check" apart from "verifiably absent"
ecaff5f8 test(server): bound the hub drain wait two orders of magnitude past legitimate use
bbb258dd fix(session,server): ask the kernel for external silence instead of reading its leftovers
93167971 docs(agent,server,session,app): five more comments against their code, two of them minted last round
ca40acbf fix(server): give rerun a commit point instead of a cancel that eats the prompt
c65889a1 test(server): stop asserting on ring eviction through a lossy broadcast
54dcf396 docs(agent,server): eight comments that described a mechanism the code no longer has
ffbe34af fix(app): finish the ReserveExclusive signature ripple into internal/app's mocks
d4e64288 fix(agent,server): make the reservation hold a real scope, and hand ownership over at the handoff
73d5caa2 feat(agent): take the edit tools away from the orchestrator instead of asking it not to use them
6c9af3d7 test(session): wait for the admission refusal instead of sleeping past it
ce3b418e fix(server): prove nobody else owns the session before deleting its tail
dfd20116 fix(session,app): let a cold drain look at the queue, and rank a row's cause above the loop's
d83084e0 test(app): drop three elapsed-time assertions the select above already proves
```

**Full-project `go test ./... -count=1`** run just before this checkpoint: 39
packages, 0 failures.

**Nothing pushed.** Fork remote is `fork`, not `origin`; nothing has been
pushed to either this session.
