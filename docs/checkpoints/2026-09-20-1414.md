# Checkpoint — 2026-09-20 14:14

## Session summary

Продолжение сессии после чекпоинта 10:40. Три фичи (`#962` reviewer-pass, `#963` nettransport-пакет, `#964` network-wiring — все реализованы через `/rush`-делегирование в изолированных worktree, лично проверены build/vet/test + ревёрт-чек, детали в чекпоинте 10:40) были смержены в `main` рабочим деревом, но не закоммичены. Пользователь сказал "пуш, зеленый ci" — это разрешило и коммит `#964` (заодно с несвязанным пред-существующим schema.json-дрейфом `enabled_in_cli`, честно указанным агентом), и запуск полного CI-цикла проверки.

Коммит чекпоинта 10:40 (`85ee883`) сделан через изолированный git-index скрипт (тот же известный обходной путь для Windows Git Bash heredoc-бага — писать скрипт в файл через Write, не через inline heredoc). Коммит `#964` — `eb59df12`, запушен.

**Долгий цикл стабилизации CI (5 итераций):**

1. Первый `gh run list` после пуша `eb59df12` вскрыл ДВА непочиненных провала с ПРЕДЫДУЩИХ пушей, которые проскочили незамеченными (пользователь тогда сказал "коммит и пшу без хуков" для Task A `0f445020` — `--no-verify` пропустил ТОЛЬКО локальный pre-push hook, но GitHub Actions всё равно триггерится независимо и упал):
   - `lint` на `0f445020` (Task A): `noctx` — 6 мест в `internal/nettransport/doubles_test.go`/`proxy_client_test.go` использовали `http.NewRequest`/`net.DialTimeout`/`net.Listen` вместо *WithContext/DialContext/ListenConfig-вариантов. Я прогонял `go vet`+`go test` локально (оба не ловят noctx — это чисто lint-правило), но НЕ прогонял `golangci-lint` перед пушем — реальный пробел в собственной процедуре проверки.
   - `build` на `ccb510ff` (reviewer-pass): `sdk/close_admission_test.go`'s `TestCloseWaitsForAdmittedReadsBeforeShutdown/SubscribeSessions` упал ("an admitted Subscribe must return a live channel, not a closed one") — код НЕ мой, файл не трогался с 1-8 сентября.
2. Починил 6 noctx находок (`internal/nettransport`), прогнал `golangci-lint` ЛОКАЛЬНО, но ТОЛЬКО на `internal/nettransport` (не на `internal/agent`, где тоже был новый тестовый файл) — коммит `180f1544`, запушено. Lint упал СНОВА — ещё 2 noctx находки в `internal/agent/coordinator_providers_network_test.go` (`client.Get` вместо `NewRequestWithContext`+`Do`), которые пропустил из-за узкого scope прошлой проверки.
3. Починил, на этот раз прогнал `golangci-lint` по ВСЕМ семи затронутым за сессию пакетам (`internal/agent`, `internal/nettransport`, `internal/config`, `internal/log`, `internal/oauth/copilot`, `internal/app`, `internal/cmd`) — 0 issues. Коммит `5c20c771`, запушено (пуш один раз словил транзиентный DNS-сбой `Could not resolve host: github.com`, ретрай прошёл).
4. `build` на `5c20c771` упал СНОВА — но на ТРЕТЬЕМ, полностью другом тесте: `internal/session/p0_3_long_turn_outcome_write_test.go`'s `TestP0_3_LongTurnOutcomeWriteSurvivesDBWriteTimeout` ("Condition never satisfied... entry must be Acked... even though Run() outlived the per-write DB context budget") — жёсткий lease TTL 200мс/safety margin 100мс, Windows-раннер, тоже не мой код.
5. Три РАЗНЫХ упавших теста в трёх РАЗНЫХ, не тронутых мной пакетах (sdk/, internal/session — ни разу не в моих трёх фичах) за 3 последовательных прогона — паттерн, указывающий на нагрузочную нестабильность CI Windows-раннера в этот день, а не на баг в моём коде. Локально `TestCloseWaitsForAdmittedReadsBeforeShutdown/SubscribeSessions` не воспроизвёлся за 20 прогонов (включая `-race`). Решение: `gh run rerun <run-id> --failed` вместо попытки чинить код, который не воспроизводится и не мой. Ретрай прошёл — `build` на `5c20c771` теперь `success`.

**Финал**: build/lint/security на `5c20c771` (HEAD main) — все `success`. Три фичи на main, CI зелёный.

## Active goal

Нет.

## TaskList

Пусто.

## Decisions

- CI-провалы в коде, который я НЕ трогал (`sdk/`, `internal/session`) и не смог воспроизвести локально (включая `-race`, 20 прогонов) — не чинить вслепую, не гадать на коде, который не мой; вместо этого `gh run rerun --failed` — это подтверждённый этой сессией паттерн (тот же подход применялся раньше для anchore.io network flake).
- Реальный процедурный пробел, который стоил двух лишних циклов CI: `golangci-lint` нужно прогонять локально ПО ВСЕМ пакетам, которые реально изменились за сессию (не только по пакету, где лежит "основной" код фичи) — тестовые файлы в СМЕЖНЫХ пакетах (внутри `internal/agent`, а не только `internal/nettransport`) тоже подлежат тому же lint-прогону. Впредь: перед пушем — `golangci-lint run` по ПОЛНОМУ списку `git diff --name-only <base>..HEAD` пакетов, не по подмножеству "где я думаю, что изменения".
- `--no-verify` на commit/push пропускает ТОЛЬКО локальные git hooks; GitHub Actions CI всё равно запускается независимо и может упасть — не путать "пропустил локальный хук" с "CI гарантированно зелёный".

## Open questions

Нет.

## Repo state

```
(чистое дерево, ничего не staged/modified)
```
```
5c20c771 fix(agent): satisfy golangci-lint's noctx check in provider-network test
180f1544 fix(nettransport): satisfy golangci-lint's noctx check in test doubles
eb59df12 feat(agent): wire proxy/DNS/DoH config into provider HTTP clients
85ee8836 checkpoint: 2026-09-20-1040
0f445020 feat(config,net): cascading proxy + custom DNS/DoH resolver package
ccb510ff feat(agent): auto-run reviewer pass after a clean --role smart run
df0051b1 checkpoint: 2026-09-20-0101
64409ab3 fix(mcp): stop two more Windows CI flakes uncovered by the TestMain fix
```

Все коммиты этой сессии (`ccb510ff`..`5c20c771`) запушены в `origin/main`. CI на HEAD (`5c20c771`) — build/lint/security все `success`. Активных cron-задач нет (одноразовые wakeup'ы, использованные для мониторинга CI, все сработали и самоудалились). Рабочих worktree не осталось.
