# План: развитие пиковых часов (peak hours)

Две связанные, но независимо реализуемые цели по фиче peak hours:

1. **Пиковые часы по дням недели** — статус: **ОТЛОЖЕНО**. Не входит в
   текущий релиз. Снято с TaskList 2026-08-05 по решению оператора, чтобы
   `/babysit` не подхватил фичу автоматически после закрытия релизного
   гейта #309.
2. **Пиковые часы на уровне конкретной модели, а не всего провайдера** —
   статус: **НЕ НАЧАТО**. Добавлено оператором 2026-08-06, см. отдельный
   раздел ниже.

Этот файл — единственный сохранившийся носитель обоих планов. Он
самодостаточен: исполнителю не нужен контекст той сессии, в которой план
составлялся.

Чтобы возобновить цель 1: завести пять задач из раздела «Этапы» ниже, в
том же порядке и с теми же зависимостями. Цель 2 намеренно не разбита на
этапы — сначала нужно закрыть открытые вопросы в её разделе.

---

## Требование оператора (дословно)

> продумать и сделать чтобы пиковые часы можно было задать для дней
> недели. Если дни недели не заданы, то работает каждый день, а если
> заданы, то только в заданные дни. Дни нумируются с 1 до 7, где 7 —
> это Шаббат. Так же в web ui нужно сделать это — там уже был ui для
> задания пиковых часов. Реализовать это в командной строке. Так же
> обновить хелпы, доки.

## Два закрытых вопроса — НЕ пересматривать

Оба были заданы оператору отдельно и получили прямой ответ. Если при
реализации возникнет соблазн «сделать привычнее» — это регресс, а не
улучшение.

**1. Нумерация: 1 = строго ВОСКРЕСЕНЬЕ, 7 = суббота (Шаббат).**

Формулировка оператора: «Нумерация — решенный вопрос. 1 — строго
воскресение, нужно просто отобразить это в хелпах и при установке
вывести английскими сокращёнными названия дней недели, в ui тоже».

Маппинг в Go: день `N` → `time.Weekday(N-1)`, то есть 1→`time.Sunday`(0),
7→`time.Saturday`(6).

Отображение: 1=Sun, 2=Mon, 3=Tue, 4=Wed, 5=Thu, 6=Fri, 7=Sat.

**Это НЕ ISO-8601** (там 1 = понедельник). Записать это в doc-комментарий
у типа. Причина конкретная: тест «понедельник попадает в маску» зелёный в
ОБЕИХ схемах, поэтому подмена нумерации на ISO пройдёт мимо тестов и
всплывёт у пользователя.

**2. Овернайт + маска: день начинается в 00:00, никакой anchor-логики.**

Формулировка оператора: «Овернайт плюс маска — день и ночь начинается с
00:00, не думаем об остальном».

Маска проверяется по календарному дню самого `now`. Следствие, которое
надо задокументировать как поведение, а не как ограничение: окно
`22:00-02:00` с `days=[6]` (пятница) отказывает в Пт 22:00–23:59 и **НЕ**
отказывает в Сб 00:00–02:00, потому что суббота не в маске. Так и
задумано.

---

## Текущее состояние кода (проверено 2026-08-05)

| Что | Где |
|---|---|
| `type PeakHoursWindow struct` | `internal/config/peak_hours.go:14` |
| `Validate()` | `internal/config/peak_hours.go:23` |
| `InPeakHours(now)` | `internal/config/peak_hours.go:42` |
| `EndTimeToday(now)` | `internal/config/peak_hours.go:71` |
| Тесты | `internal/config/peak_hours_test.go` |
| jsonschema поля `PeakHours` | `internal/config/config.go:183` |
| `parsePeakHoursWindow(raw)` | `internal/cmd/providers.go:22` |
| Чтение флага `--peak-hours` | `internal/cmd/providers.go:239` **и** `:309` |
| `Example:` с peak-hours | `internal/cmd/providers.go:209` |
| `formatPeakHoursStatus` | `internal/cmd/ping.go:713` |
| `peakHoursFromWire` | `internal/server/handlers.go:1673` |
| `peakHoursToWire` | `internal/server/handlers.go:1687` |
| Сборка `ProviderWire` | `internal/server/handlers.go:831` **и** `:858` |
| Текст отказа `PeakHoursGuidance` | `internal/agent/peak_hours_stop.go` |
| UI пиковых часов | `web/src/components/ProvidersModal.tsx` |

Сейчас `PeakHoursWindow{Start, End string}` — только `HH:MM`, без дней.
Пустое окно валидно и означает «фича выключена». `InPeakHours`: `Start`
включительно, `End` исключительно, овернайт (`Start > End`) заворачивается
через полночь.

### Три write-пути на сервере, а не один

`peakHoursFromWire` вызывается из **трёх** обработчиков, и обновить нужно
все три:

- `handleAddCustomProvider` — `handlers.go:1705`
- `handleUpdateCustomProvider` — `handlers.go:1791`
- `handleSetProviderPeakHours` — `handlers.go:1860`

Это уточнение найдено при записи плана: в исходной постановке был назван
только третий. Пропуск двух первых даст «дни теряются при добавлении
кастомного провайдера, но работают при последующем редактировании» — то
есть баг, который воспроизводится через раз.

### Уже существующая ложь в документации

`internal/config/config.go:183`, jsonschema-описание, дословно содержит:

> Times are HH:MM in the machine local clock, no timezone, **no weekday
> mask**.

Сразу после этапа 1 это станет прямой неправдой. Поле user-facing:
jsonschema уходит в генерируемую документацию и в подсказки редактора.
Тот же класс, что чинили в коммите `e565c030` (`StreamToolTimeoutSeconds`
обещал 900s, а реально был 2700s с `e9544a8f`) — не внутренний
комментарий, а обещание продукта, которого бинарь не держит.

---

## Этапы

Пять задач, строго последовательно по зависимостям. Каждая — отдельный
коммит.

### Этап 1 — модель и семантика

Файлы: `internal/config/peak_hours.go`, `internal/config/peak_hours_test.go`.

1. Добавить `Days []int` (json `days`, `omitempty`). Пусто или отсутствует
   = каждый день. **Обратная совместимость обязательна**: существующие
   конфиги без `days` работают ровно как сейчас.
2. `Validate()`: отвергать значения вне 1..7 и дубликаты (либо
   дедуплицировать — решить и задокументировать выбор); отвергать `days`
   при пустом окне.
3. `InPeakHours` — проверка `now.Weekday()` против маски. Граница дня —
   полночь, по решению 2.
4. `EndTimeToday` / момент открытия — перепроверить, что при заданной
   маске он всё ещё называет корректный будущий момент. Это самое
   вероятное место тихой ошибки: «откроется в 12:00» может оказаться
   неправдой, если сегодняшний день не в маске.
5. Хелпер отображения `[]int → "Sun, Mon, Tue"`. Живёт здесь, рядом с
   типом, потому что нужен ещё в этапах 2 и 5. Вторая копия маппинга —
   готовый источник расхождения.

Тесты: обратная совместимость (без `days`); каждый день 1..7 по
отдельности; невалидные значения (0, 8, −1, дубли); **овернайт с маской на
обеих ногах** — доказать, что пост-полуночная нога НЕ отказывается, когда
её день не в маске (это и есть зафиксированная семантика); границы
включительно/исключительно; пустое окно с заданными `days`; хелпер
отображения.

### Этап 2 — CLI (зависит от 1)

Файлы: `internal/cmd/providers.go`, `internal/cmd/ping.go` + тесты.

1. Синтаксис дней. Предложение: `--peak-hours 09:00-18:00@1,2,3,4,5` —
   дни после `@`, через запятую, с поддержкой диапазонов `1-5`. Без `@` —
   каждый день, как сейчас. Альтернатива — отдельный флаг `--peak-days`:
   проще парсить, но разносит связанные данные и порождает вопрос «что
   значит `--peak-days` без `--peak-hours`». Решить и обосновать одной
   строкой в коммите.
2. Флаг читается в **двух** местах (`providers.go:239` в `set` и `:309` в
   `add`). Обновить оба — пропустить второе легко.
3. **После успешной установки печатать подтверждение днями словами**:
   `peak hours set: 09:00-18:00 on Sun, Mon, Tue, Wed, Thu` (или
   `every day`, если дни не заданы). Использовать хелпер из этапа 1.
   Смысл требования оператора: пользователь должен увидеть ошибку
   нумерации на месте, а не через неделю.
4. Сообщения об ошибках называют допустимый диапазон **и** что 1 = Sunday.
   Иначе пользователь введёт ISO-нумерацию и молча получит не тот день.
5. `formatPeakHoursStatus` (`ping.go:713`) — тоже дни словами.
6. Обновить текст флага и `Example:` (`providers.go:209`).
7. `--peak-hours off` продолжает очищать окно целиком.

Тесты: валидные и невалидные формы; диапазоны; обратная совместимость;
`off`; **оба** call site; текст подтверждения; формат в `ping`.

> **Не запускать `rush providers` против реального конфига машины.**
> Для ручной проверки выставлять **обе** переменные `RUSH_GLOBAL_DATA`
> и `RUSH_GLOBAL_CONFIG` в одноразовые temp-каталоги. `--cwd` НЕ
> изолирует global scope; реальный конфиг содержит живые API-ключи и
> определения MCP-серверов (см. CLAUDE.md).

### Этап 3 — сервер и wire-протокол (зависит от 1)

Файлы: `internal/server/handlers.go` + тесты.

1. Добавить дни в `PeakHoursWirePayload`.
2. Обновить `peakHoursToWire` и `peakHoursFromWire` в обе стороны.
3. Валидировать на записи через `PeakHoursWindow.Validate()`, **не
   доверять клиенту**. Невалидные дни возвращают понятную ошибку, а не
   сохраняются молча.
4. Обновить **все три** write-пути (список выше), а не только
   `handleSetProviderPeakHours`.
5. Проверить **оба** места сборки `ProviderWire` (`:831`, `:858`).
6. Главный риск обратной совместимости на этом уровне: отсутствие поля в
   payload от старого клиента должно означать «каждый день», а не «ни
   одного дня». Пустой слайс и отсутствующее поле трактуются одинаково.

Тесты: round-trip to/from wire; отсутствующее поле; пустой слайс;
невалидные дни отвергаются на записи; все три write-пути; оба места
сборки `ProviderWire`.

### Этап 4 — Web UI (зависит от 3)

Файлы: `web/src/components/ProvidersModal.tsx`, `web/src/types.ts`,
`web/src/store.ts` + Playwright e2e. Проверить также
`web/src/keepAlive.ts` — он упоминает peak, понять зачем и не задет ли.

UI пиковых часов уже существует — расширять его, а не строить с нуля.

1. Тип в `types.ts` синхронизировать с реальным wire-payload из этапа 3.
2. Контрол выбора дней — семь переключателей с подписями Sun..Sat.
3. **Порядок отображения: Sunday первым**, потому что 1 = воскресенье.
   Если поставить понедельник первым «как привычнее», пользователь
   выберет визуально первый элемент и получит не тот день. Расхождение
   экранного порядка с нумерацией в конфиге — готовый баг.
4. Состояние «ничего не выбрано» = каждый день. Это должно быть **видно**
   в интерфейсе, а не подразумеваться: пустой набор переключателей
   читается как «выключено», хотя означает «всегда». Подписать явно.
5. Отправка через store в существующую команду `CmdSetProviderPeakHours`.

Тесты: Playwright e2e по конвенции репозитория.

### Этап 5 — документация, хелпы, jsonschema, CHANGELOG (зависит от 1–4)

1. Исправить jsonschema-описание и `example` в `config.go:183` — убрать
   «no weekday mask», показать `days` в примере.
2. Хелпы CLI: текст флага, `Long`, `Example` у `providers set` и
   `providers add`.
3. Текст отказа в час пик (`internal/agent/peak_hours_stop.go`,
   `PeakHoursGuidance`): если дни заданы, сообщение должно это отражать.
   Иначе оператор увидит «refusing until 12:00» и не поймёт, почему
   вчера в это же время всё работало. Дни — словами.
4. Документация в `docs/` и README — найти grep'ом, не гадать.
5. CHANGELOG.
6. Нумерация написана явно **в каждом** user-facing месте: 1 = Sunday,
   7 = Saturday, и что это не ISO-8601. Одного упоминания в глубине
   недостаточно.
7. Зафиксировать семантику овернайта как поведение (решение 2 выше).

> **Осторожно с `PeakHoursGuidance`.** Этот текст — не только для людей.
> Инструкция `/rush-fallback` распознаёт отказ по маркерным строкам из
> этого блока, и в этой сессии уже был инцидент: инструкция требовала три
> маркера, а путь sub-agent-стрима эмитил только один, из-за чего агент,
> следующий правилу буквально, не переключился на фоллбек (исправлено в
> `a167d36e`). Меняя формат сообщения, проверить обе формы вывода и при
> необходимости обновить `internal/cmd/claude_rush_fallback_command.md`
> **и** установленную копию в `~/.claude/commands/rush-fallback.md`.

---

## Дополнительная цель: пиковые часы на уровне конкретной модели, а не всего провайдера

**Статус: НЕ НАЧАТО.** Добавлено оператором 2026-08-06 как отдельная,
но связанная цель — по объёму и рискам сопоставима с недельной маской
выше, отдельная от неё последовательность этапов не расписана, но
основа (`PeakHoursWindow`, `Validate()`, `InPeakHours`) переиспользуется
без изменений.

### Требование оператора (дословно)

> сделать час пик — настройкой моделей у провайдеров. Чтобы не на весь
> провайдер вешался час пик, а только на конкретные модели.

### Зачем

Сейчас `PeakHoursWindow` живёт на `ProviderConfig.PeakHours`
(`internal/config/config.go:183`) — окно закрывает **весь** провайдер
целиком. Если у провайдера несколько моделей с разной стоимостью/лимитами
(например дешёвая `small`-модель и дорогая `large`-модель у одного и
того же `zai`), сейчас нет способа зажать час пик только на дорогую
модель, оставив дешёвую доступной круглосуточно — приходится либо
закрывать провайдер целиком (излишне), либо не настраивать час пик
вовсе.

### Проверено в коде 2026-08-06 — на что опираться

| Что | Где |
|---|---|
| `catwalk.Model` — **вендорный тип**, поле сюда не добавить | `charm.land/catwalk/pkg/catwalk.Model` (модуль `charm.land/catwalk`, НЕ в этом репозитории) |
| `ProviderConfig.Models []catwalk.Model` | `internal/config/config.go:175` |
| `ProviderConfig.PeakHours *PeakHoursWindow` (провайдер-уровень, останется как fallback/умолчание) | `internal/config/config.go:183` |
| Единственная точка принятия решения | `checkPeakHours(providerCfg config.ProviderConfig) error` — `internal/agent/coordinator.go:2452` |
| **Все пять** call site'ов `checkPeakHours` — в каждом ID модели уже в scope, менять сигнатуру не проблема | `coordinator.go:663` (`model.ModelCfg.Model`), `:736` (`model.ModelCfg.Model`), `:2398` (`c.currentAgent.Model().ModelCfg.Model`), `:2445`→`checkLivePeakHours` вызывается из `:1410` с `large.ModelCfg.Provider` — тут же рядом `large.ModelCfg.Model`, `:2652` (`model.ModelCfg.Model`) |
| CLI команда, которую нужно расширить | `internal/cmd/providers.go` — `providers set`/`providers add`, флаг `--peak-hours` |
| Wire-протокол | `internal/server/handlers.go` — `PeakHoursWirePayload`, `peakHoursFromWire`/`peakHoursToWire`, три write-пути (см. список в основном плане выше) |
| Web UI | `web/src/components/ProvidersModal.tsx` — сейчас UI на уровне провайдера, модельный список там же рядом |

Важно: раз в `catwalk.Model` поле не добавить, оверрайд обязан жить
**отдельной картой на `ProviderConfig`**, а не «рядом с моделью» —
например `ModelPeakHours map[string]*PeakHoursWindow` (ключ — `model.ID`
из `catwalk.Model`), с тем же `*PeakHoursWindow` типом, что уже есть.
Ключ по `model.ID`, а не по индексу в `Models[]` — индекс не переживает
переупорядочивание списка при `AutoDiscoverModels`.

### Открытые вопросы — требуют решения оператора, не решать самостоятельно

Эта секция специально не разбита на «Этап 1, 2, 3…» как недельная маска
выше — раньше нужно закрыть развилки, иначе реализация под них не ляжет:

1. **Приоритет при конфликте.** Если у провайдера задан `PeakHours`
   (весь провайдер) **и** у конкретной модели задан свой
   `ModelPeakHours[modelID]` — что побеждает? Варианты:
   - модельный оверрайд полностью замещает провайдерский для этой модели
     (провайдерский остаётся дефолтом для моделей без своего оверрайда);
   - действуют оба одновременно (модель недоступна, если попадает **в
     любое** из двух окон — более строгий вариант);
   - модельный оверрайд запрещён, если у провайдера уже задан
     `PeakHours` (явная ошибка валидации, самый простой, но менее гибкий).
2. **CLI-синтаксис.** Нужен ли отдельный флаг (`rush providers set <id>
   --peak-hours-model <model_id>=09:00-18:00`, повторяемый) или отдельная
   подкоманда (`rush providers models peak-hours <id> <model_id>
   09:00-18:00`)? Существующий `--peak-hours` без указания модели должен
   продолжать работать как «весь провайдер», для обратной совместимости.
3. **Пересекается с недельной маской.** Если недельная маска (см. основной
   план выше) будет реализована первой, `PeakHoursWindow` уже несёт
   `Days []int` к моменту старта этой цели — модельный оверрайд
   автоматически наследует и дни, и часы, отдельно проектировать не
   нужно. Порядок реализации (сначала дни, потом модели, или наоборот)
   — на усмотрение оператора при старте; в коде зависимости друг от
   друга у двух целей нет.
4. **`rush providers state`/`ping`-вывод и `sessions why`** должны
   показывать, ЧЬЁ именно окно (провайдера или конкретной модели)
   сработало, а не просто «в час пик» — иначе оператор увидит отказ по
   `large`-модели и не поймёт, почему `small`-модель того же провайдера
   в этот момент работает.

---

## Что уже проверено и на что можно опереться

- Фича пиковых часов работает end-to-end: в этой сессии провайдер `zai`
  реально отказывал 08:00–12:00, фоллбек отработал, cron восстановил
  `/rush` в 12:00.
- Отдельного e2e для peak-hours в `web/` на момент составления плана не
  найдено — этап 4, вероятно, создаёт первый.

## Чего в плане нет — сознательно

- **Таймзоны.** Осталось «machine local clock», как сейчас.
- **Несколько окон на провайдер целиком** (два разных окна на весь
  провайдер сразу). Одно окно на провайдер, как сейчас — но см. «Дополнительная
  цель» выше: несколько окон **на разные модели одного провайдера**
  — это другая, отдельно спроектированная фича, не путать с этим пунктом.
- **Календарные исключения** (праздники, конкретные даты). Только
  недельная маска.
