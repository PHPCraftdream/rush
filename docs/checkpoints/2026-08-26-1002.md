# Checkpoint — 2026-08-26 10:02

## Session summary

Continuation of a marathon session on `D:\dev\go\crush` (PHPCraftdream/rush fork). Earlier this window: shipped an initial batch of Anthropic prompt-cache-hit improvements (P0-P3, commit `f6aa416d`, already pushed) via `@sl` sub-agents under `/babygoal`. The user then had that batch code-reviewed independently, saved to `docs/reviews/2026-08-26-071427-cache-hit-review.md`. I verified every finding in that review against the actual code, confirmed 7 real distinct defects (including one race I hadn't caught myself), filed them as tasks #765-#771 in the review's own priority order, and — since the user did NOT say `@sl` this time — implemented ALL SEVEN myself directly in this session (no sub-agents), with the same rigor as the earlier delegated rounds: read the exact code, design the fix, write a regression test, prove non-vacuousness by temporarily reverting the fix and confirming red, then reapply and verify green under `-race`.

All 7 tasks are now complete and independently verified:
- **#765**: the keep-alive's `cancelCacheKeepAlive`/`fireCacheKeepAlive` interaction had a real race — the pending-entry removal and the in-flight-cancel registration were NOT atomic, so a cancel landing in that gap was silently lost, letting a stale replay run unopposed and even re-arm itself. Fixed by moving in-flight registration into the SAME mutex critical section as the removal, plus a `ctx.Err()` check before rearming. Needed a genuinely BLOCKING test seam (`cacheKeepAliveFireSeam`) — a fire-and-forget version was vacuous (passed even against deliberately-broken code) because the production goroutine's own scheduling raced ahead of the test's cancel call.
- **#766**: keep-alive was capturing the turn-START tools snapshot instead of what `PrepareStep` actually decided per-step. Added a `stepTools` variable (parallel to the existing `stepMessages`) and a new `runTurnToolsSnapshotSeam` to deterministically simulate a mid-turn `SetTools`. First test attempt was ALSO vacuous — `generateTitle`'s own concurrent model calls (sharing the same mock model instance) polluted the call-capture list; fixed by giving the test session a pre-existing title+message so `needsTitle` stays false.
- **#767**: `providerCacheSupport` always returned Native regardless of whether a provider was ever confirmed to report caching. Added a `knownCacheReporter` facet to `cacheProfile` (`cache_profile.go`), populated for anthropic/bedrock/vercel/openrouter/google/openai/local-cli. Had to also fix a stale `cache_profile_test.go` (written by an earlier task, #764) whose fixtures didn't know about the new field yet.
- **#768**: title-generation was costing raw (un-normalized) usage, double-counting cached tokens for Google/OpenRouter/Vercel exactly like the main turn did before task #760. Fixed with the same `normalizeProviderUsage` call the main turn and the keep-alive replay already use.
- **#769**: MaxCost was only checked BEFORE the up-to-30s replay call, not before the actual charge after it — a real TOCTOU window. Added a second check inside `recordCacheKeepAliveCost` (now returns `bool`) so a session that crosses its cap while the replay is in flight gets neither the charge nor a rearm.
- **#770**: the cache-invalidation detector in `rush sessions cache` couldn't distinguish a genuine prompt-prefix change from the cache's own TTL simply expiring after a normal idle gap. Added `LikelyTTLExpiry bool` (5-minute local threshold) computed from `message.CreatedAt` deltas; a new `backdateMessage` test helper directly UPDATEs the SQLite `messages.created_at` column since the public API only ever stamps "now".
- **#771**: trimmed `cacheKeepAliveEntry`'s dead `tools`/`providerOptions`/`maxCost` fields (written but never read back — the armed timer closure already captures everything needed).

**One notable incident this window**: while implementing #769, I attempted a temporary `ZZREVERTCHECK` edit to `agent_cache_keepalive.go` to prove non-vacuousness; the tool reported the edit as REJECTED by the user ("the new_string was NOT written to the file"). I trusted that message and moved on. A later FULL PACKAGE test run (under real system load, not the isolated single-test runs) caught a genuine, non-flaky failure: `TestFireCacheKeepAlive_SkipsChargeWhenSessionCrossesMaxCostMidFlight` observed a cost of `5.001` instead of `5.0` — meaning a charge went through that should have been blocked. Investigating, the file ACTUALLY still contained the rejected `ZZREVERTCHECK` state (the maxCost re-check logic was genuinely missing, with the stray comment still present) — despite the tool's rejection message. Root-caused and fixed by re-applying the correct re-check logic; confirmed with 25x `-race` reruns of that specific test, then a full-package clean run (0 failures, 581s). This is a reminder that a tool's reported outcome for a rejected edit should be independently re-verified against actual file content when anything seems inconsistent, rather than trusted blindly — flagged transparently to the user in-session.

All 7 tasks' final state confirmed via: `go build ./...` (clean), `go vet` (clean), `golangci-lint run` (0 issues), and full, non-cached `go test ./internal/agent -race -short` (581.3s, 0 failures) + `go test ./internal/cmd/... -race -short` (262.5s, 0 failures) — both run FRESH (not relying on any earlier, possibly-stale background job output, several of which came back empty/interrupted earlier in this window for unclear session-boundary reasons unrelated to the code itself).

## Active goal

None. No `/goal` Stop hook armed. No `/babysit` cron active (this window's #765-771 work was done directly by me, not via `/babygoal`).

## TaskList

Empty — all of #765, #766, #767, #768, #769, #770, #771 are completed. (Earlier tasks #756-764 from the prior `@sl`-delegated batch were also already completed and closed before this window started.)

## Decisions

- Implemented #765-#771 directly (no sub-agents) since the user's "изучи ревью, давай исправим" request this window did not say `@sl` — per CLAUDE.md's standing rule against invoking sub-agents without explicit request.
- For every fix, proved non-vacuousness by temporarily reverting to the broken behavior and confirming the new test goes red, then reapplying — twice this needed real debugging because the first test design was itself vacuous (see #765/#766 above), not just a mechanical revert-check.
- `likelyTTLExpiryThreshold` (#770) is a LOCAL constant in `internal/cmd/sessions_cache.go`, not a cross-package reference to `internal/agent`'s unexported `cacheProfiles` — `internal/cmd` is a diagnostic reader, not a policy owner.
- `cacheKeepAliveInFlight` cancellation (#765) is deliberately NOT covered by `cacheKeepAliveMu` — a plain take-and-cancel is safe since double-cancel/cancel-of-done-context is a no-op in Go; only the pending-entry map's check-and-act sequences need the mutex.
- After the ZZREVERTCHECK incident: going forward, always re-read a file's actual current content after ANY tool-reported rejection before assuming no change was made, rather than trusting the rejection message alone.

## Open questions

- Whether/when to flip `RUSH_CACHE_KEEPALIVE` on by default now that #761 (exact replay shape), #765 (race), #766 (stale tools), #769 (MaxCost TOCTOU) are all fixed — raised once already in an earlier summary to the user, not yet explicitly answered. Worth asking now that the full review's punch list is closed.
- User just asked for a checkpoint followed by "commit everything" — the commit itself has not happened yet as of this checkpoint being written; it is the very next action.

## Repo state

```
 M internal/agent/agent.go
 M internal/agent/agent_cache_keepalive.go
 M internal/agent/agent_cache_keepalive_test.go
 M internal/agent/agent_compaction.go
 M internal/agent/agent_control.go
 M internal/agent/agent_prompt.go
 M internal/agent/agent_title.go
 M internal/agent/agent_turn.go
 M internal/agent/run_lock_test.go
 M internal/agent/usage_record.go
 M internal/cmd/sessions_cache.go
 M internal/cmd/sessions_cache_test.go
 D web/dist/.gitkeep
?? docs/checkpoints/2026-08-25-1746.md
?? docs/checkpoints/2026-08-26-0919.md
?? docs/reviews/2026-08-26-071427-cache-hit-review.md
?? internal/agent/agent_title_normalize_test.go
?? internal/agent/cache_profile.go
?? internal/agent/cache_profile_test.go
?? internal/agent/usage_normalize.go
?? internal/agent/usage_normalize_test.go
?? internal/agent/usage_record_test.go
```

```
f6aa416d feat(agent,cmd): reduce prompt-cache invalidation, add keep-alive and cache_key affinity
81f2090b fix(agent,server): resolve 4 CI/test flakes found this session
0d1dceea fix(cmd): make case-insensitive-filesystem test check the right name
27301509 fix(web): scroll-up immediately disengages the auto-scroll magnet
db15e284 fix(agent): transient-retry no longer duplicates the user message
```

Nothing from #765-#771 is committed yet — that commit is the immediate next step per the user's explicit request.
