# Checkpoint — 2026-09-01 10:50

## Session summary

Continuation of a marathon multi-day session on the embeddable Rush SDK (`sdk/` package, PHPCraftdream/rush fork, repo `D:\dev\go\crush`). This window picked up right after review-round-2 (`docs/reviews/2026-08-31-sdk-library-review-round-1.md`, a review OF the review-round-1 work) had been decomposed into 4 parallel `rush run` + git-worktree groups. All 4 finished, were zero-trust-verified (build/vet/test + bounded `-race`), merged by hand (careful manual reconciliation of `sdk.go`/`README.md` across the 3 groups that all touched them), and committed as 4 commits (`e900aea0`, `4f88867a`, `9b9ec295`, `4269b799`) plus a 5th (`70e38b3b`) for a DB-pairing-guard false positive caught by the real pre-push hook run. Along the way, two genuinely pre-existing, load-induced test flakes were found and fixed (`TestReleaseGate_1_MetadataCleanupBlockedForever`'s 500ms→3s bound; `TestSearchFilesWithRegexRespectsMidWalkCancellation`'s cold-vs-warm cache timing) — both confirmed via isolated reruns and, in one case, reproduction on an unmodified baseline.

After the user explicitly authorized "push + CI now", all of the above was pushed to `fork/main` (`591b649a` at that point) and CI (`build`/`lint`/`security`) went green.

The user then surfaced a NEW external review — round 2 proper (`docs/reviews/2026-08-31-sdk-library-review-round-2.md`, reviewing the just-merged round-1 fixes) — with a new P0 (R2-1: a same-session fail-fast loser could delete the winner's just-armed permission policy) and two P1s (R2-2: `Client.Run` could race `Close` and touch a torn-down `App`; R2-3: a fail-fast loser could mutate the winner's live session metadata — prompt/budget/title — before learning it lost). The user chose to fix all three now (P0+P1), in parallel via 2 file-scope-disjoint `/rush`+worktree groups, deferring the 3 P2 findings (R2-4 forced-ephemeral docs, R2-5 `CallOptions` zero-value ambiguity, R2-6 "one Client per process" doc contradiction).

Group 2 (R2-2, `sdk.go` only) finished cleanly on the first attempt: replaced the bare `closed atomic.Bool` with a proper admission state machine (`admissionMu`+`closing`+`inflight`+`drained` channel) so `Close` now waits for already-admitted calls to finish before tearing the `App` down. Verified independently (build/vet/test/`-race -count=10`), merged, committed (`f4e3fd86`).

Group 1 (R2-1+R2-3, `internal/agent`+`internal/app`+`internal/permission`) was the hardest task of the whole session — it timed out twice (60m then 90m) purely from the sheer density of `mailbox_ownership.go`'s existing invariant documentation (dozens of past review rounds), but both partial results were sound and were resumed rather than restarted. The final design: a `reservedOwnership` one-shot context token (same pattern as the existing `CallOptions`/`CredentialSet` threading) that `ExecuteRun` obtains via the coordinator's *already-existing* `ReserveExclusive`/`RunWithReservedOwnership` primitives (originally built for `/compact` and rerun, task #614) — reused, not reinvented — right after the fast `IsSessionBusy` pre-check and before any session mutation, but *only* for `FailIfSessionBusy` callers (the legacy `rush run`/web queueing contract from R1-4 is completely untouched for non-SDK callers). The permission-policy race (R2-1) was closed by making per-session run-allowlist entries epoch-bound (`sessionRunAllowlistEntry{allowlist, ownerEpoch}`), mirroring `mailbox.abandonOwnership`'s own epoch-comparison idiom, and by removing the unconditional process-wide `SetRunAllowlist` write from this path entirely (fail-closed instead of inherit-stale). New tests exercise real end-to-end same-session races through a live restricted-run permission gate (not just field pokes). Verified independently (build/vet/full-package test/`-race -count=10..20`); one batch of `internal/agent` full-package test failures during my own verification was traced to 4 *unrelated* concurrent `rush run` sessions (`group3-data-hygiene`, `group4-locks-retire`, etc. — not mine, confirmed via `wmic process` inspection) pegging the host CPU at 100%, not a regression — confirmed by 9/9 isolated passes. Merged, committed (`cf0dfc64`).

Mid-session, the user also asked for a brand-new `/wrush` slash-command: identical to the existing `/rush` delegation skill, except worktree isolation is *mandatory* on every invocation (not just for parallel/overlapping runs), and the task isn't "done" until the reviewed diff is merged back into the primary checkout AND the worktree is removed. Initially built as a standalone global `~/.claude/commands/wrush.md` file, then — on the user's correction ("нужно было в нашем коде создать") — properly wired into the crush repo's own `claude-init`/`claude-del` installer (`internal/cmd/claude_wrush_command.md` new embed template, `claude_init.go`/`claude_del.go` write/remove logic, `root.go` help text fix, 5 new tests in `claude_init_test.go`, README + CHANGELOG updates) — none of this batch is committed yet. The user then asked to compress the skill's own instructions (it originally duplicated `/rush`'s entire ~400-line playbook) — rewritten to ~70 lines that explicitly tell the invoking agent to read the sibling `rush.md` file for everything unchanged, keeping only the worktree-specific additions. Finally, the user asked that worktrees always live under `<repo-root>/worktrees/` (never a sibling/external directory) with a `.gitignore` entry, specifically because external worktree paths were tripping Claude Code's dangerous-operation confirmation prompts during cleanup in a *different* repo (`cc-arch-hands`) — this repo's `.gitignore` already has the `/worktrees/` entry added (uncommitted), and both the in-repo template and the live global `~/.claude/commands/wrush.md` now mandate the in-repo path.

## Active goal

None — no `/goal` Stop hook armed this session.

## TaskList

Empty. The last two tasks (`#830` Group 1 R2-1+R2-3, `#831` Group 2 R2-2) were both marked `completed` after merge+verification; the `babysit` cron (job `5f901869`) self-detected the empty list and was deleted.

## Decisions

- Fix all of round-2's P0+P1 (R2-1, R2-2, R2-3) now, in parallel via 2 disjoint-file-scope `/rush`+worktree groups; defer the 3 P2s (R2-4/R2-5/R2-6) — explicit user choice, mirroring round-1's own P0-now/P2-later split.
- Reuse the coordinator's existing `ReserveExclusive`/`RunWithReservedOwnership`/`rebindDispatcher` primitives (built for `/compact`/rerun) for R2-1/R2-3's atomic pre-reservation, instead of inventing a parallel mechanism — kept the mailbox's already-dense invariant surface from growing a second code path.
- Keep the R2-1 fix scoped to `FailIfSessionBusy` callers only; the legacy `rush run`/web queueing contract (R1-4) is intentionally untouched for `FailIfSessionBusy=false` callers, regression-pinned by a new test.
- `/wrush` reads the sibling `rush.md` at invocation time instead of duplicating its content — avoids drift between the two skills as `/rush` evolves.
- `/wrush` worktrees always live under `<repo-root>/worktrees/` (gitignored), never a sibling/external path — avoids destructive-operation confirmation prompts on cleanup that occurred in a different repo.
- Not pushing the round-2/round-3 commits (`f4e3fd86`, `cf0dfc64`) or the uncommitted `/wrush` installer work without an explicit request — mirrors the standing "never push without being asked" rule; round-1's own push only happened after the user explicitly said so.

## Open questions

None outstanding. The user's most recent message was a bare "продолжай" (continue) in the middle of this `/ccheckpoint` invocation — no unresolved question is blocking progress.

## Repo state

```
 M .gitignore
 M CHANGELOG.md
 M README.md
 M internal/cmd/claude_del.go
 M internal/cmd/claude_init.go
 M internal/cmd/claude_init_test.go
 M internal/cmd/root.go
 D web/dist/.gitkeep
?? docs/checkpoints/2026-08-26-2015.md
?? docs/checkpoints/2026-08-27-0749.md
?? docs/checkpoints/2026-08-28-1210.md
?? docs/checkpoints/2026-08-28-1349.md
?? docs/checkpoints/2026-08-30-0859.md
?? docs/checkpoints/2026-08-30-1730.md
?? docs/checkpoints/2026-08-30-2015.md
?? docs/checkpoints/2026-08-31-0130.md
?? docs/checkpoints/2026-08-31-1615.md
?? docs/plans/2026-08-27-agent-tree-backend-requirements-response.md
?? internal/cmd/claude_wrush_command.md
```
```
cf0dfc64 fix(agent,app,permission): atomic owner-token before per-run mutations (review R2-1, R2-3)
f4e3fd86 fix(sdk): atomic admission/shutdown state machine on Client (review R2-2)
591b649a fix(agent/tools): warm the file cache before timing the mid-walk cancellation test
70e38b3b fix(githooks): allow-list shutdown_result_test.go's DB pairing false positive
4269b799 fix(agent): widen release-gate test's Run() bound to survive full-suite parallel load
9b9ec295 fix(message,session): reword origin-column doc comment to avoid a gofumpt bug
4f88867a fix(sdk,agent): unique ephemeral DB per client, fail-closed CredentialSet, public API cleanup (review R1-2,R1-3,R1-5,R1-6,R1-7,R1-8)
e900aea0 fix(agent,app,permission): per-call execution context, atomic session admission (review R1-1, R1-4)
```

Note: `fork/main` is synced through `591b649a` (the last explicitly-authorized push). The two review-round-2/3 commits above it (`f4e3fd86`, `cf0dfc64`) and the uncommitted `/wrush` installer changes (`.gitignore`, `CHANGELOG.md`, `README.md`, `internal/cmd/claude_del.go`, `claude_init.go`, `claude_init_test.go`, `root.go`, new `internal/cmd/claude_wrush_command.md`) have NOT been pushed.
