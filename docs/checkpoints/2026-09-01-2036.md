# Checkpoint — 2026-09-01 20:36

## Session summary

Continuation of a multi-day session on the embeddable Rush SDK (`sdk/`, PHPCraftdream/rush fork, repo `D:\dev\go\crush`). This window opened with a plain (non-committing) `/resume`-style recap of the prior round-2/round-3 review-fix arc, then the user asked to push and monitor CI for the work already sitting locally.

A first push landed cleanly (`c958425d..6a1c5fbd`), and the resulting `build (macos-latest)` CI run surfaced a genuinely reproducible bug: `runAgentTurnRecovered` in `internal/app/app_run.go` mapped `sessionAgent.Run`'s `(nil, nil)` return — which is EXCLUSIVELY the R1-4 legacy-queueing signal, never a real busy-rejection (those always carry a non-nil wrapped `agent.ErrSessionBusy`) — into a hard `ErrSessionBusy` failure. This silently broke the legacy (`FailIfSessionBusy=false`, i.e. `rush run`/web) queueing contract whenever a caller's timing actually landed in the genuine mid-turn queueing window instead of racing in as a fresh owner after the current one released (the common, masking case on a fast/idle machine — confirmed by writing and then reverting temporary debug traces in `agent_run.go` this session). Fixed and pushed (`6a1c5fbd`).

The user then asked to split `build.yml`'s single monolithic non-agent test step into named per-area groups (mirroring the existing `test-agent` job's philosophy) so a CI failure's step name is the diagnosis instead of requiring a full-log read. Implemented as ~8 named steps plus a dynamically-computed catch-all (bash array + exact-string exclusion, no `grep` dependency — the sandboxed Bash tool's own `grep` here turned out to be a non-standard "0.1.0" stub with broken end-anchor/leading-slash handling, unrelated to real CI's actual grep). Verified locally end-to-end and with `actionlint`. Pushed (`df6b2370`).

The user then surfaced review-round-3's remaining findings (`docs/reviews/2026-09-01-sdk-library-review-round-3-1055.md` in a sibling upstream-research checkout) reviewing the round-2 fixes just merged. Six findings: R3-1/R3-2 (P1), R3-3/R3-4/R3-5/R3-6 (P2). All six were decomposed into TaskList items (#832-#837) with explicit `blockedBy` chains reflecting real file-scope overlaps (mostly all touching `internal/app/app_run.go`), and solved via `/wrush` (worktree-mandatory delegation) in two waves: wave 1 parallel (#832 R3-2 SDK-shutdown-cancellation, #833 R3-1 per-call-tool-pinning — disjoint files), wave 2 strictly serial (#834 R3-5 docs → #835 R3-3 mailbox-hold-cancellation → #836 R3-4 queued-run-policy-versioning → #837 R3-6 CallOptions-timeout-presence, each depending on the previous due to shared `app_run.go`/`internal/agent` touch points).

All six were independently zero-trust-verified before merge (personally read every diff, ran `go build`/`go vet`/full package `go test`, and bounded `-race -count=15` on new/related tests) and merged by hand (`cp` + rebuild + retest, never blind `git merge`) into `main`, one commit each: `9b3841e0` (R3-1), `692b2854` (R3-2) + a same-session lint follow-up `746cf4df` (a `noctx` finding the pre-push hook caught before any push), `8474beb9` (R3-3, which ALSO fixed a real, reproduced test-synchronization flake in the pre-existing `TestExecuteRunSameSessionLegacyQueueingStillQueuesBehindOwner` test — traced via temporary `mb.mu`-adjacent debug tracing to the mailbox's `mbReleasing`-window "orphaned, detached durable-queue restart" path, which the test's own gate-close timing didn't account for; fixed the TEST with a `require.Eventually` barrier on `QueuedPrompts`, 300/300 green under `GOMAXPROCS=1` after), `430c79a6` (R3-5 docs), `8341077d` (R3-4), `4f15041d` (R3-6, the last one — push still in flight as this checkpoint is written).

Two full-worktree processes died mid-run this session for reasons outside my control (once from a Windows reboot losing the background `rush run` processes entirely — resumed both from scratch in fresh worktrees rebuilt off the post-fix HEAD; once from what looked like an agent/environment restart mid-session that orphaned a `rush run` child process for R3-6 without letting it flush its `--json` output — resumed in the SAME session with the existing worktree diff intact). Both `--timeout 60m` sub-agent runs for the hardest tasks (R3-2, R3-1, R3-4, and the R3-6 resume) needed a 90m-or-fresh-60m re-run at least once; all eventually completed with solid, independently-reproduced verification.

Along the way, encountered and correctly diagnosed (not "fixed" — nothing to fix) THREE separate instances of load-induced flakiness in `internal/agent`'s timing-sensitive watchdog/checkpoint tests (`TestP1_3_SuccessfulTickNotImpacted`, `TestSessionAgent_HandleWatchdogFire_StoresCauseAndDispatchesDumpAsync`/`TestStreamWatchdog_ArmedStillFiresTheHardCap`, `TestProbe_CheckpointStopDoesNotStallStep`, `TestStreamWatchdog_ToolCleanupGraceDelaysFire`) — each time confirmed via `wmic process`/`wmic cpu` that 2-3 genuinely unrelated concurrent `rush run` sessions (belonging to other, independent work on this same dev machine: `round12-group*`, `round13-group*`, `tis-sol4-w3-docdiet`) were pegging host CPU, and confirmed each specific failing test passes reliably (10-15/15) in isolation — consistent with, and matching, this exact session's own already-established precedent for this class of issue.

## Active goal

None — no `/goal` Stop hook armed.

## TaskList

Empty. All six review-round-3 tasks (#832-#837) completed and merged; the `babysit` cron (job `61b97513`) self-detected the empty list at this session's most recent tick and was deleted (confirmed via `CronList`/`CronDelete`, not just narrated).

## Decisions

- Fix all 6 round-3 findings (R3-1 through R3-6) now, via `/wrush` worktree-isolated delegation in 2 waves (parallel where files are disjoint, strictly serial where they share `app_run.go`/`internal/agent`) — mirrors the round-1/round-2 precedent of "fix what's found, defer nothing silently."
- When R3-3's sub-agent's own new test exposed a genuinely reproducible flake in a PRE-EXISTING test (`TestExecuteRunSameSessionLegacyQueueingStillQueuesBehindOwner`), fixed the test itself in the same commit rather than filing it as a separate follow-up — matches the standing "flakes are fixed on the spot, in the same turn" rule.
- For R3-6, chose "explicit presence bit" (`TimeoutOptionsSet`) over "make the whole CallOptions snapshot authoritative" specifically because `RunOverrides`' CLI-populated fields can't represent "flag explicitly passed as zero" vs "flag never passed" at all — the smaller, honestly-scoped fix given what's actually representable, not the more thorough-looking one.
- Diagnosed three separate `internal/agent` full-package test failures as load-induced (confirmed unrelated concurrent `rush run` processes via `wmic`, confirmed 10-15/15 isolated passes each time) rather than code regressions — no code/test changes made for these, correctly out of scope.
- Did not push CI-regrouping or review-fix work without doing independent zero-trust verification first on every single commit, even when a sub-agent's own report already claimed full verification with a revert-check proof.

## Open questions

None outstanding.

## Repo state

```
 M .gitignore
 M CHANGELOG.md
 M README.md
 M internal/cmd/claude_del.go
 M internal/cmd/claude_init.go
 M internal/cmd/claude_init_test.go
 M internal/cmd/root.go
 D web/dist/.gitkeep
?? docs/checkpoints/2026-08-26-2015.md
?? docs/checkpoints/2026-08-27-0749.md
?? docs/checkpoints/2026-08-28-1210.md
?? docs/checkpoints/2026-08-28-1349.md
?? docs/checkpoints/2026-08-30-0859.md
?? docs/checkpoints/2026-08-30-1730.md
?? docs/checkpoints/2026-08-30-2015.md
?? docs/checkpoints/2026-08-31-0130.md
?? docs/checkpoints/2026-08-31-1615.md
?? docs/plans/2026-08-27-agent-tree-backend-requirements-response.md
?? internal/cmd/claude_wrush_command.md
```
```
4f15041d fix(agent,app): make CallOptions timeout presence explicit, not zero-inferred (review R3-6)
8341077d fix(agent,app,permission): version queued-run permission policy by logical call id (review R3-4)
430c79a6 docs(sdk): bring shutdown/multi-client docs and CloseEphemeralConnsForced ordering in line with behavior (review R3-5)
8474beb9 fix(app): observe mailbox-directed cancellation during the admission hold (review R3-3)
746cf4df fix(sdk): use PingContext in close_stall_test.go (noctx lint)
692b2854 fix(sdk,app): make Client.Close cancel admitted work instead of waiting forever (review R3-2)
9b3841e0 fix(agent,app): pin per-call tool slice instead of publishing it (review R3-1)
df6b2370 ci(build): split the monolithic test step into named per-area groups
6a1c5fbd fix(app): stop mapping legacy queueing's (nil,nil) to ErrSessionBusy
c958425d checkpoint: 2026-09-01-1050
cf0dfc64 fix(agent,app,permission): atomic owner-token before per-run mutations (review R2-1, R2-3)
```

Note: `fork/main` was confirmed at `8341077d` (R3-4) as of this checkpoint's writing; the push for `4f15041d` (R3-6, the last review-round-3 commit) was still running its pre-push hook (build+lint+test) in the background at write time — not yet confirmed landed on `fork/main`. The `/wrush` installer changeset from an earlier session window (`.gitignore`, `CHANGELOG.md`, `README.md`, `internal/cmd/claude_del.go`/`claude_init.go`/`claude_init_test.go`/`root.go`, new `internal/cmd/claude_wrush_command.md`) remains uncommitted, unchanged from before this window — still awaiting an explicit user request to commit.
