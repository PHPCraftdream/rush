# Checkpoint — 2026-09-04 06:23

## Session summary

Continuation of the multi-day session on `D:\dev\go\crush` (PHPCraftdream/rush fork). This window covered a full round-14 SDK review fix chain, a structural fix for a recurring local Windows commit-limit OOM, and a full sweep-and-fix of every flaky CI test discovered along the way — culminating in **3 consecutive fully-green CI runs** across all jobs (build×3 OS, lint, security, test-race, test-agent×3 OS).

**Round-14 SDK review chain (#876-879):** studied `docs/reviews/2026-09-03-sdk-library-review-round-14-0928.md` (found in a separate research worktree). Confirmed via personal source reading: R14-1 (P0, real — worker sub-agent tool layering bypassed the ephemeral disabled-tools list; confirmed by a real revert-check showing `download` making an actual DNS dial), R14-3 (P1, real — `sdk.LibraryVirtualRoot` was a mutable var, not an alias), R14-4 (P2, real — `agentic_fetch` wrote to real OS temp), R14-5 (P2, real — two tests encoded host-topology assumptions), R14-6 (P3, real — cliprovider test didn't restore `log` package state). **R14-2 (P0 as filed) was found NOT to reproduce** — personally disproved via a real revert-check showing `CanonicalizeFolderScopeSpec` already fails closed via an unconditional WorkingDir-resolution step, independent of the claimed fix; documented this correction in the merge commit, same discipline as the round-13 timestamp-attribution catch earlier in this session. All fixed via `/wrush` in a worktree-chaining topology onto `sdk-review-r14-integration`, merged to `main` as `0b5bd37c`.

**The OOM investigation (user directive: "commit-limit OOM - это критическая болезнь. Давай её победим наконец-то"):** a hooked `git push` reproduced the exact `errno=1455`/`VirtualAlloc` crash class from earlier in this session — root cause: `.githooks/pre-push` ran `go build`/`go test ./...` with full default `-p`/`-parallel`, never applying the `-p 2` + `internal/agent`-isolation lesson CI's own `build.yml`/`test-agent` job already learned. Fixed with two layers: (1) `-p 2` everywhere + `internal/agent` tested alone with `-parallel 2`, portable, no dependency (commit `627219bf`); (2) an optional `run_capped` helper that hard-caps via `safego.ps1` (this machine's personal Job-Object memory-cap tool, **never committed to the repo**) only when `GOHOSTOS=windows` AND the file exists — gracefully degrades to bare execution everywhere else, confirmed explicitly with the user mid-session ("мы не должны полагаться в репе на редкие инструменты" → confirmed `run_capped` never becomes a repo dependency). A dedicated read-only sweep agent (`oxx`) then found and I fixed two more gaps: `Makefile`'s `go build` missing `-p 2` (commit `885989fb`), plus a heavier fallback-path miss in the hook itself (also `885989fb`).

**The flaky-test cascade:** user then explicitly authorized delegating research to `/rush`, `/rush reviewer`, `@oxx`, `@fh` and created task #883-888 for "победить все флейки, OOM, наладить все прогоны" (defeat all flakes/OOM, get all runs green). Each subsequent push revealed exactly ONE new, previously-latent CI-only flaky test (consistent with this being the very first sustained period of real CI runs against this codebase); each was root-caused via a dedicated `fh`/`oxx` read-only investigation agent with rigorous file:line-cited evidence, then fixed by me personally (test-only, never production code in this "battle-tested" session/lock subsystem CLAUDE.md warns about):
- **10+2 tests** (`fs_provider_fake_test.go`, 2 rebuild tests) and **6 more** (Group D: `testEnv`/`absWorkingDir` in `common_test.go`/`coordinator_disk_provider_test.go`) — the SAME host-topology bug class already found twice earlier this session (`73878311`, `b253bb70`): `t.TempDir()` not canonical on some CI runner images (macOS `/tmp`→`/private/tmp`; Windows temp-drive junctions), fixed via `filepath.EvalSymlinks` up front, matching established precedent exactly (commits `c108a2d8`, `678b5103`).
- **`TestFileMatchesHonoursDeadlineMidHugeLine`** (grep fallback) — timing-noise flake under real machine contention (a concurrent `/wrush` run), fixed with a bounded 3-attempt retry, verified with a real revert-check (temporarily neutered `readBoundedLine`'s `ctx.Err()` check, confirmed all 3 attempts fail identically — retry cannot mask a real regression) (commit `4d4e2e05`).
- **`TestRecoverInterruptedTurns_NoLiveHolder_StillRecovers`** (session lock) — `fh`-investigated: `SessionLock.Release()`'s async metadata-cleanup goroutine (bounded to 50ms, a deliberate documented P1-1 anti-hang trade-off that NAMES this exact test as the one that regressed when cleanup became async) can exceed 50ms on a loaded ubuntu-latest runner, racing the test's immediate back-date+inspect. Fixed with `require.Eventually` waiting for `ReadLockPID==0`, the same idiom already used in 8+ other tests in this codebase (commit `85623b1c`).
- **`TestReleaseGate_P0_2_GenuineFailureStillExhaustsAfterMaxAttempts`** (run-queue pump) — `fh`-investigated: this file's own two 25-cycle siblings were already widened from 5s to 20s after an identical windows-latest failure class (commit `deb3047a`, 2026-08-13); this 10-cycle wait was the one left at the original, now-too-tight 5s. Widened both `Eventually` calls to 20s, matching the established convention (commit `10b14a3b`).
- **`TestAppNew_RunQueuePump_ExecutesRealEnqueuedCall`** (macOS) — `fh`-investigated: the REAL production `RunQueuePump` (started by `App.New`) fires an "initial tick on startup" immediately/asynchronously; it legitimately raced the test's own immediate pending-only list-check, leasing the just-enqueued row first. Fixed with a status-agnostic existence check (`HasOutstandingRunQueueEntriesForSession` OR the test's own `providerCalls` fallback), naturally exercised by the fix's own local run (commit `9cf16671`).

**Final verification:** after all 6 fixes landed, CI was re-run **3 times consecutively** (`gh run rerun`, no new commits) — all fully green across every job every time. Task #883 ("multi-run stability") closed on that basis.

## Active goal

None — the Stop-hook goal "наладить все прогоны" (set earlier this session) has been satisfied and should auto-clear; the stale `bhamtwmp4` background-task reference that kept deferring its evaluation was explicitly stopped via `TaskStop` this window.

## TaskList

Empty. #876-888 all completed this window (round-14: 876-879; OOM+flake chain: 880-888).

## Decisions

- `run_capped`/`safego.ps1` in `.githooks/pre-push` is confirmed, explicitly, to never become a repo dependency — gated on `GOHOSTOS=windows` AND file-exists, bare-execution fallback otherwise. Discussed and confirmed directly with the user mid-session.
- R14-2 from the round-14 review was written up as NOT reproducing against actual code (a review error, not a real vulnerability) — documented in the merge commit rather than silently "fixing" a non-bug.
- All flaky-test fixes this window are test-only; none touch the "battle-tested" session-lock/run-queue-pump production code, per CLAUDE.md's explicit caution about that subsystem. Every investigation used a fresh, context-free `fh`/`oxx` agent in read-only mode first; fixes were applied by the orchestrator (me) only after the mechanism was confirmed via cited file:line evidence, never on the investigating agent's say-so alone.
- 3 consecutive green CI reruns (not just 1) required before declaring the flake-hunt done, since a single green run cannot statistically distinguish "fixed" from "got lucky" for timing-dependent flakes.
- Did not push anything beyond what was explicitly driven by the ongoing "наладить все прогоны" directive — every push this window was either an explicit "пуш" request or a direct continuation already authorized for this specific fix chain.

## Open questions

- None outstanding. The pre-existing `internal/csync/maps.go:148` `go vet` finding remains unfixed (out of scope for every task touched this session, repeatedly confirmed as the sole pre-existing exception across every verification pass).
- The pre-existing, still-uncommitted deletion of `web/dist/.gitkeep` remains untouched (per CLAUDE.md's explicit instruction).
- `main` is even with `fork/main` (0 commits ahead) — everything from this window has already been pushed.

## Repo state

```
 D web/dist/.gitkeep
```
```
9cf16671 fix(test): use status-agnostic existence check for the real pump's initial-tick race
10b14a3b fix(test): widen TestReleaseGate_P0_2_GenuineFailureStillExhaustsAfterMaxAttempts timeouts to 20s
85623b1c fix(test): wait for session-lock metadata cleanup before back-dating in recovery test
4d4e2e05 fix(test): retry TestFileMatchesHonoursDeadlineMidHugeLine on measurement noise
678b5103 fix(test): canonicalize testEnv/absWorkingDir via EvalSymlinks for temp-namespace parity on CI runners
c108a2d8 fix(test): canonicalize t.TempDir() in fake-disk and rebuild scope tests
885989fb fix(hooks,build): cap the two remaining unthrottled go build/test spots
b253bb70 fix(test): canonicalize t.TempDir() before building scopes in 3 symlink tests
627219bf fix(hooks): cap pre-push go build/test to stop Windows commit-limit OOM
0b5bd37c Merge sdk-review-r14-integration: R14-1/R14-2/R14-3/R14-4/R14-5/R14-6 from round-14 SDK review
```

`main` is even with `fork/main` (0 commits ahead/behind). No worktrees or branches of mine remain outstanding — the `worktrees/` directory is completely empty (the one leftover locked directory from earlier in this window, `p881-fake-disk-canonical`, cleared and was removed as part of this checkpoint). `.claude/worktrees/agent-*` (6 dirs), `crush-research-upstream-20260830`, `crush-verify-20260831`, `crush-wt/review-*` are unrelated, pre-existing, not mine.
