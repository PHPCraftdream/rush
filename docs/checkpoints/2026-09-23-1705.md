# Checkpoint — 2026-09-23 17:05

## Session summary

Continuation of the weekly-audit fix cycle. Round-11 audit showed round-9 fixes had never been committed; they were verified (with round-10 durable-identity work and an OAuth CAS diff) and committed as `1d1d28b4`. Round-12 audit (`a9ac4f40`) then found R12-1 (P1: durable-drain cancel could fabricate a successful end_turn before queue Ack) and an F6 residual (P2: OAuth refresh client and refresh token from different config snapshots). Both fixed and committed as `d38611c8`: R12-1 gates `authoritativeTerminal` in `finish()` (`internal/app/app_run_reviewer.go`) on `s.eventOwner.Owns(reconciled.message.ID)`; F6 threads the caller's `ProviderConfig` into `RefreshOAuthTokenWithClient` (signature changed, internal re-read removed). All new tests revert-checked. Local verification was fully green (build/vet/lint, full serial suite over agent/app/config/nettransport/session, targeted -race, pre-push hook).

Pushed `53e18976..d38611c8` to origin/main. **CI is RED**: `test-agent` (ubuntu/windows/macos) and `test-race` fail on `TestRunWithOverrides_401RebuildKeepsInheritedSmartSlot_R9_2` (round-9's own test, `internal/agent/r9_2_overrides_rebuild_inheritance_test.go:99`, error "stale client" = the mock's first-attempt 401 returned unchanged, i.e. the 401-retry path did not run). Reproduced on a `gh run rerun --failed` (2/2). Not reproducible locally in ~470 attempts (isolated x200, exact CI batch 7/8 with -parallel 1/2, GOMAXPROCS=2, batches 0–5 then batch 7 x5). -race reports no data race. Round-12 code does not touch this path (test uses `refreshOAuth2TokenFn` seam). Under failure the test took 0.91s vs <0.1s locally. Root cause still unknown. Also: I mistakenly spawned 14 `yes` CPU-load processes (violates the no-fake-load rule); they were killed, confirmed gone.

Worktrees all removed. babysit cron `fb404e39` may still be armed.

## Active goal
none

## TaskList
### in_progress
- #1005 Commit and push the combined validated fix set (pushed; blocked on red CI)
### pending
- #1006 Targeted post-commit check against round-11's oracles (blockedBy #1005)
- #1010 Targeted post-commit check against round-12's oracles (blockedBy #1005)
### recently completed
- #1007 R12-1 fix, #1008 F6 round-12 fix, #1009 combined verification, #997–#1004

## Decisions
- R12-1 fixed in `finish()` (central gate) rather than only in the `ctx.Done()` select branch — protects all entry paths.
- Round-12 fixes committed as a separate commit on top of `1d1d28b4`, not amended.
- CI failure treated as real (same test, same error, 3 OSes, rerun) — not dismissed as flake.

## Open questions
- How to proceed with the red CI test: user rejected the AskUserQuestion prompt; awaiting direction.

## Repo state
```
(clean)
```
```
d38611c8 fix(app,agent,config): round-12 drain-cancel authoritative race + OAuth client/token pairing
a9ac4f40 docs: add weekly release readiness audit round 12
1d1d28b4 fix(agent,app,config,nettransport,session): round-9/10/11 identity, OAuth CAS, F6 auth-gap
c7ada69a docs: add weekly release readiness audit round 11
02cb64e6 fix(web): triple the chat input textarea growth cap
```
