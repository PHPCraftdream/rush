# Checkpoint — 2026-09-23 22:11

## Session summary

Session covered: fixing red CI (root causes: R9-2 test fixture missing SetupAgents on clean runners; Windows test-DB cleanup racing title-generation goroutines; npm launcher EBUSY on Windows), adding `--peak-hours-message` to `rush providers set/add` (honors `--local`, which resolves to the git root's `.rush/rush.json`), putting peak-hours guidance + operator message into the `--json` envelope, and regression tests for that chain. Then a user-reported hang ("queued behind an active run" on 5 sefer-alloc sessions) was diagnosed on a COPY of the r2-14 DB/log (in `.rush/stuck-r2-14/`, gitignored): a --timeout left a durable run_queue row; each new `rush run` lost the race to its own process's RunQueuePump, exited "queued", and cancelled the pump turn. Fix a71cebf8: ExecuteRun (queueing callers) drains the session's pending durable work first (`internal/app/app_run_drain_pending.go`). The model had also wasted time on "outside every folder scope": fs_* tools were offered to unscoped runs with a zero scope; fixed in f1c0a983 (`applyCallFolderScope` strips fs_* without a scope). User then hit r2-15 on alpha.3: drained pump turn lost its lease (DB stall >45s, watchdog) and was still unwinding, so the run queued again; fixed in a9c29ede (after draining, wait until the local owner releases the session). Versions bumped to 0.2.0-alpha.3 (dd965aca) and 0.2.0-alpha.4 (5b2f2513); go.mod tidy fix 71d28fc1. All pushed. CI for 71d28fc1 was fully green; CI for 5b2f2513 (run 35913664124) was still running at checkpoint time — result unknown. The root cause of the r2-15 DB stall is unclear (likely machine load: 5 rush + Rust builds); lease-watchdog behavior left unchanged by design. No babysit cron armed.

## Active goal
none

## TaskList
### in_progress
none
### pending
none
### recently completed
- #1015 Проверка на копиях застрявших сессий (r2-14 copy verified: end_turn, queue empty)
- #1014 Аудит других вечных блокировок (outbox race residual, stale interrupt-inject log noise — both minor)
- #1013 А: rush run drains pending durable work first
- #1012 Б: re-assessed — no attempts reset; chain was a consequence of А
- #1011 CLI --peak-hours-message

## Decisions
- Hang fix option 1 (execute pending durable work before the new prompt) chosen over discarding stale rows.
- Wait-for-local-owner only when pending work existed, to keep the legacy in-process queueing contract for concurrent ExecuteRun callers.
- Lease watchdog / at-most-once terminal-fail for interrupted turns left as-is (intentional duplicate-execution guard).
- fs_* hidden for unscoped runs rather than granting an implicit working-dir scope.
- No release tag created for alpha.3/alpha.4 (not requested).

## Open questions
- Is CI green for 5b2f2513 (run 35913664124)?
- Delete `.rush/stuck-r2-14/` DB/log copies? (awaiting user)
- Other 3 stuck sessions (r2-16, r2-19, r2-20) will run their pending hour-long tasks first on next run — user not asked whether to drop them instead.

## Repo state
```
 D web/dist/.gitkeep
```
```
5b2f2513 chore(release): bump fork version to 0.2.0-alpha.4, update changelog
a9c29ede fix(app): after draining, wait for the local turn to release the session
71d28fc1 chore(deps): go mod tidy — pflag is now a direct dependency
dd965aca chore(release): bump fork version to 0.2.0-alpha.3, update changelog
f1c0a983 fix(agent): don't offer fs_* tools to unscoped runs
```
