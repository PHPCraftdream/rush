# Checkpoint — 2026-09-21 21:16

## Session summary

Продолжение долгой сессии (после чекпоинтов 2026-09-20). За эту сессию: (1) фича idle-timeout — `--timeout` по умолчанию отключён (был 60m), добавлен новый `--idle-timeout` (default 15m), сделан terminal (не ретраится через `shouldRetryTurn`/`shouldContinueTurn`) через новое поле `agent.CallOptions.IdleTimeout` — реализовано лично (не через `/rush`), закоммичено и запушено как `f7362f7f`, CI на нём зелёный (build/lint/security все success после одного `gh run rerun --failed` по не связанному macOS-флейку в `internal/agent/cliprovider`, подтверждено что код не тронут и тот же тест прошёл на предыдущем коммите). (2) По вопросу "все правки за неделю покрыты тестами?" — обнаружены 3 непокрытых web-коммита (timestamp always visible, model/effort в sub-agent header, tool-args preview+timestamp в sub-agent blocks). По явной просьбе пользователя ("используй /wrush") — делегировано через `/wrush` в worktree `web-test-coverage`: агент написал `web/tests/message-timestamp-always-visible.spec.ts` и `web/tests/subagent-block-metadata.spec.ts` (4 теста). Лично проверено: прочитаны оба файла, прогнаны тесты (4/4 зелёных), сделан ЛИЧНЫЙ revert-check (временно откатил все 3 прод-места, все 4 теста упали ровно на нужных ассертах, восстановлено, снова зелено) — тесты подтверждённо не тавтологичны. Смержено в основной чекаут (`cp`), worktree удалён (`git worktree remove --force` не смог из-за pnpm-junctions в node_modules — сработал `cmd //c rmdir /s /q`), ветка удалена. Эти 2 spec-файла остаются НЕЗАКОММИЧЕННЫМИ (untracked) в рабочем дереве — коммит не запрашивался.

(3) Пользователь сообщил, что отдельный "xa-агент" (другая сессия/агент) сделал коммит `e547e12e` — большой ревью `docs/reviews/2026-09-21-weekly-commit-audit.md` (539 строк, 5 P1 + 4 P2 находки по коммитам за 14–21 сентября, включая мои же фичи этой недели: reviewer-pass, nettransport, idle-timeout). Вердикт ревью: NO-GO для alpha. По команде пользователя "давай сделаем доработки по ревью, что можно — запусти с /wrush параллельно в worktrees" — применено правило CLAUDE.md "Review Stop Rule": P1-находки с воспроизводимым сценарием чинятся в этом же цикле, P2 — в backlog, не сейчас.

Я ЛИЧНО перепроверил (не доверяя ревью вслепую) 4 из 5 P1-находок чтением актуального кода — все подтвердились:
- **F1**: `internal/app/app_run_reviewer.go`'s `resetForReviewerPass(ctx)` не присваивает `s.ctx = ctx` — `runTurnPhase` (там же, ~:109) использует старый `s.ctx` (от primary-фазы), поэтому изолированный `reviewCtx` (role=reviewer, DisableSubAgents, cleared ownership) реально не доходит до выполняемого review-хода.
- **F2**: `shouldRunReviewerPass` не знает про `req.Credentials`/`credsRunner` — reviewer-pass безусловно идёт через `RunWithOverrides` (глобальный конфиг), а не через `credsRunner.RunWithCredentials` — нарушение tenant-изоляции для credentialed-запросов.
- **F3**: и `handleMessageEvent` (app_run_reviewer.go:~269, live-путь), и `reconcileTerminalMessage` (app_run_terminal.go, DB-путь), и `coordinator_run.go`'s retry-loop (`result, originalErr = run()`) — все берут "последнее завершённое сообщение", поэтому continuation после partial-ответа стирает начало итогового текста.
- **F4**: `internal/nettransport/proxy.go`'s `connectDialer` — после успешного ctx-bound TCP dial, `writeConnectRequest`/`http.ReadResponse` блокируются БЕЗ deadline и без привязки к ctx; аналогично `resolver.go`'s `dnsTCPQuery`. Подтверждено чтением кода.
- F5 НЕ перепроверял лично (передал делегату проверить самому перед фиксом, а не чинить вслепую).

По итогу декомпозировано на 2 ПАРАЛЛЕЛЬНЫЕ `/wrush`-задачи с непересекающимся файловым scope (безопасно для параллели):
- `worktrees/reviewer-pass-continuation-fixes` (task #972) — F1+F2+F3, scope: `internal/app/app_run.go`, `internal/app/app_run_reviewer.go`, `internal/app/app_run_terminal.go`, `internal/agent/coordinator_run.go` + тесты.
- `worktrees/nettransport-lifecycle-fixes` (task #973) — F4+F5, scope: `internal/nettransport/*.go`, `internal/agent/coordinator_providers_network.go`, `coordinator_models.go`, `coordinator_model_cache.go`, `internal/log/http.go`, `internal/oauth/copilot/client.go` + тесты.

Обеим задачам в промт заложены: точный проверенный механизм бага (не просто цитата отчёта), критерий приёмки, обязательный revert-check, явное предупреждение о scope соседа, запрет на commit/push/`go test ./...` (только scoped-пакеты через `capm 2g go test -p 1 -parallel 1`). Оба `rush run` запущены в фоне (`--role smart --timeout 60m --json`), на момент этого чекпоинта ОБА ЕЩЁ ВЫПОЛНЯЮТСЯ (живые heartbeat, `elapsed` порядка нескольких минут, видны нормальные orchestrator-mode worker-делегации внутри каждой сессии). P2-находки (F6 network-config snapshot race, F7 потеря `IdleTimeout` при durable round-trip — это баг в МОЕЙ СОБСТВЕННОЙ idle-timeout фиче этой недели, F8 потеря tool-counts основной фазы в reviewer-envelope, F9 JSON `null` ломает sub-agent render в `toolFormat.ts`) сознательно НЕ взяты в работу в этом цикле — зафиксированы только в тексте отчёта пользователю, в TaskList не заведены (чтобы babysit-тик их не подхватил).

## Active goal

Нет.

## TaskList

### in_progress
- #972 Fix reviewer-pass ctx loss + credential bypass + continuation text loss (F1+F2+F3) via /wrush — параллельная `/wrush`-сессия в `worktrees/reviewer-pass-continuation-fixes`, ещё выполняется на момент чекпоинта.
- #973 Fix nettransport handshake cancellation + transport lifecycle leaks (F4+F5) via /wrush — параллельная `/wrush`-сессия в `worktrees/nettransport-lifecycle-fixes`, ещё выполняется на момент чекпоинта.

### recently completed (эта сессия)
- #965–#968 idle-timeout / `--timeout` фича (реализация, тесты, документация) — все закрыты, смержено в `f7362f7f`.
- #969 Commit+push idle-timeout, get CI green — закрыт, CI зелёный.
- #970 Follow-up после зелёного CI — закрыт (reviewer-pass фича подтверждена уже реализованной ранее).
- #971 Add Playwright coverage for 3 untested web UI changes via /wrush — закрыт, смержено, worktree удалён.

## Decisions

- P1-находки ревью с воспроизводимым по коду сценарием чинятся в этом же цикле (CLAUDE.md Review Stop Rule); P2 — в backlog, не в этот цикл, и НЕ заводятся как TaskList-задачи (чтобы их не подхватил babysit).
- Перед делегированием фикса каждая P1-находка лично перепроверена чтением актуального кода (не доверять ревью вслепую, даже своему собственному коду этой недели) — F1–F4 подтверждены, F5 передан делегату на самостоятельную проверку.
- Две `/wrush`-задачи разделены по НЕПЕРЕСЕКАЮЩЕМУСЯ файловому scope (reviewer-pass/continuation vs nettransport-lifecycle) специально, чтобы избежать git-конфликтов при параллельном выполнении — несмотря на то, что F3 частично затрагивает тот же файл (`app_run_reviewer.go`), что и F1/F2, поэтому F1+F2+F3 объединены в ОДНУ задачу вместо трёх параллельных.
- 2 Playwright spec-файла (`web/tests/message-timestamp-always-visible.spec.ts`, `web/tests/subagent-block-metadata.spec.ts`) остаются untracked в рабочем дереве — коммит не запрашивался пользователем явно для этого куска работы.

## Open questions

- Пользователю не задан явный вопрос, но естественный следующий шаг после проверки обеих `/wrush`-задач (#972, #973) — спросить про коммит/пуш и про P2-backlog (F6–F9): чинить ли их отдельным циклом или оставить как есть для alpha с ограничениями, описанными в отчёте.

## Repo state

```
?? web/tests/message-timestamp-always-visible.spec.ts
?? web/tests/subagent-block-metadata.spec.ts
```
```
e547e12e docs: add weekly release readiness audit
f7362f7f feat(cmd): disable rush run --timeout by default, add terminal --idle-timeout backstop
bf9f6d3d checkpoint: 2026-09-20-1414
5c20c771 fix(agent): satisfy golangci-lint's noctx check in provider-network test
180f1544 fix(nettransport): satisfy golangci-lint's noctx check in test doubles
```

Worktrees active: `worktrees/reviewer-pass-continuation-fixes` (branch `reviewer-pass-continuation-fixes`), `worktrees/nettransport-lifecycle-fixes` (branch `nettransport-lifecycle-fixes`) — обе с живыми `rush run` процессами на момент чекпоинта, ОБЕ ЕЩЁ НЕ СМЕРЖЕНЫ. Babysit cron `80ca3415` активен (каждые 15 мин).
