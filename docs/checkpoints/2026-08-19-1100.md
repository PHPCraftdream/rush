# Checkpoint — 2026-08-19 11:00 [two-reviews-merged-nothing-implemented]

## Session summary

Nothing was implemented since the last checkpoint. This one exists to record a
merged backlog and hand it over cleanly.

**Two independent reviews of the same 21 commits, by different methods, both
NO-GO.** They agree on the shape of what is wrong and disagree about which
findings are the worst — which is the useful part, because each caught things
the other could not.

`@oh` reviewed **dynamically**: it reverted production changes and ran the
tests. That found two of my fixes broken in production while their tests stayed
green, and two tests of mine that are vacuous — including two whose own
"Revert-check:" comments assert the opposite of what actually happens. It also
found the two `web/src/store.ts` misses and the stale CLI help by running the
binary.

`docs/reviews/2026-08-19-post-fix-release-readiness-static-review.md` reviewed
**statically**, over the same range plus the surrounding production paths. It
reached the drain-outcome defect independently, classed it the single remaining
release blocker, and enumerated five background outcomes where my own comment
conceded one. It also found three things the dynamic pass never looked at: the
durable-recovery config mixing, the outbox fallback's exhausted context, and —
worst of the set — that the builtin `crush-config` skill still generates
`models.large`/`models.small`, so the agent asked to configure Crush will
confidently write a config the new code does not read.

Neither review found a classic lock-order deadlock. Neither will certify
freeze-free.

**Where they diverge, and why it matters:** the static review lists the
checkpoint-generation fence and the watchdog disarm under "what looks closed".
The dynamic pass found the first silently discarding every edit to a partial
message (confirmed with a probe), and traced the second to the success path
only. A reading-level "looks closed" is not evidence; both tasks carry a note
to trust the specific line numbers over the summary.

## Active goal

The /babygoal Stop hook is still armed with the earlier condition. It is NOT
satisfied and should not be treated as such: sixteen tasks are open. The
operator's later, explicit instructions — file tasks from the review, then
checkpoint and stop — take precedence over it, and are what this session did.

## TaskList

Sixteen open, none started. Ordered as I would work them.

### Release blockers
- #575 P0 — background-wins drain reports success without the background
  outcome. THE blocker in the static review. Supersedes the F9 item, which was
  removed from #572. Needs an owner/done/typed-outcome admission entry, and a
  regression contract covering all five background outcomes.
- #569 F1 — the checkpoint fence silently discards edits to partial messages.
  My regression from #555, confirmed by probe. All four editing handlers.
- #568 F2 — sticky WebSocket delivery dropped under exactly the load it was
  built for. Production buffer is 512; the shipped test allocates 2200.
- #570 P1-1+P1-4 — the two `web/src/store.ts` misses. One sends the SMART model
  when the operator explicitly asked for fast.
- #573 P1-5 — stale `large/small` in CLI help, runtime errors, and — the part
  the dynamic pass missed — the builtin config skill and `crush_info.md`.
- #571 F3 — outbox quarantine untested at its call site, and it dead-letters
  healthy work on five transient DB errors.

### High
- #577 P1-2 — durable recovery mixes two config generations. Recovery of all
  places is where determinism matters most.
- #576 P1-3 — `buildAgent`/`buildTools` still a series of live reads.

### Medium
- #578 P2-1 — the drain keeps a stale error after a later retry succeeds.
  Needs a decision, not just a patch.
- #579 P2-2 — the outbox fallback reuses an already-exhausted context.
- #580 P2-3 — `sessions kill` on Unix can leave the provider child tree alive.
  Fix or explicitly accept; undecided is what is not acceptable.
- #572 F4+F10+F11 — vacuous sticky test, watchdog disarm only on the success
  path, the disproved comment that survives in the second file.
- #581 — watchdog comments promise a heartbeat the code deliberately does not
  send. Third false-guarantee comment found in this subsystem.
- #574 F12+F13 — move the ASCII/sqlc guard into CI; rename leftovers.
- #567 — triage the 37 pre-existing Playwright failures; delete the stale ones.
- #582 — the two sub-agent tool defects, finally filed rather than carried as
  prose in three checkpoints.

## Work in flight

None. Working tree clean apart from untracked `dev/`. Twenty-one commits this
session, none pushed — and given two NO-GO verdicts, nothing should be.

## Decisions

- **Both reviews' findings were merged rather than kept as parallel lists.**
  Where they overlap (drain outcome, web store, CLI help) the task carries both
  perspectives and says which claim was confirmed by running something.
- **F9 was promoted out of a "should change" grouping into #575.** I had
  recorded it as a HYPOTHESIS in a bundle of small items; the second review
  independently made it the release blocker. When two reviews disagree about
  severity, take the higher one.
- **Nothing was implemented in this session after the reviews.** The operator
  asked for tasks and a checkpoint. Three of the blockers are my own
  regressions from this session, and re-entering them in the same pass that
  produced them is how the first round of vacuous tests happened.

## Open questions

- The static review asks for a **dynamic** release gate after the P0 and web
  fixes: targeted race scenarios, full Go test/lint/race, web typecheck/build,
  and a non-red e2e baseline. #567 is a prerequisite for the last of those.
- At-least-once side effects after an Ack failure or lease loss are still
  undocumented for users. The typed error tells the truth internally; there is
  no fencing or idempotent commit protocol, and the review wants that decided
  before a stable release.
- `internal/agent/agent_turn.go` is ~1859 lines of callback-driven state
  machine whose forensic comments have now been caught diverging from the code
  three separate times. Splitting it was considered earlier this session and
  rejected with evidence; the comment-drift rate is a new argument to revisit.

## Repo state

```
?? dev/
```

```
da10303f docs: changelog and closing checkpoint for the review backlog
bdcab3bf test(web): feed the e2e fixtures the config keys the UI now reads
32ace126 test(web): point the effort spec at the test ids the UI actually emits
aef182c5 docs(agent): correct why the title join grace cannot simply be shortened
7879683e fix(agent): build the prompt from the pinned config, not the live store
```
