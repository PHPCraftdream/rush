# Checkpoint — 2026-08-20 06:00 [sixth-review-worked-off]

## Session summary

The sixth review's backlog is closed. Six commits, `f8ffb68c` …
`a1bfccbc`, **none pushed**. Every task except the permanent fallback
sentinel is done; the ones open at the last checkpoint (#604, #605, #606,
#607, #608) are all closed, plus #609 which came out of verifying #604.

The sixth review found a sixth form of the drain defect: two ctx-death
exits at the top of `DrainSessionNow`'s loop computed a verdict without
recording why the loop stopped, so a call that committed one row and then
lost its context returned `(DrainComplete, nil)` — exit 0 over pending
work.

**Correction, added after the seventh review.** This checkpoint originally
claimed there was no seventh form and cited a table of all eight return
sites in the design doc. Both were wrong. No such table exists — the design
doc predates this round and was never touched by it — and the enumeration
that produced the claim covered only "context dead", while the seventh form
needs a live one: `GetOldestPendingRunQueueEntryForSession` filters
`status = 'pending'`, so a row left `leased` by a dead owner is invisible to
the drain, which then reports `DrainComplete`. Tracked as its own task.

The watchdog's ~40% failure rate had a cause nobody had guessed:
`lease_expires_at` is a whole-seconds column, so renewals round the deadline
up, and the watchdog reused that rounded value to decide when to fire —
donating up to a full second of its own safety margin, measured at 97–930ms
per renewal. Scheduling and the poll ticker were measured and cleared first.

**The most instructive failure this round was not in any single diff.** The
zombie fix and the #594 fence test were each verified independently and
correctly, and their combination hung the test suite for ten minutes —
because one test *deliberately* depends on the un-reaped zombie window that
the other fix removed. In CI that is a stuck job, not a red test. Nothing
but running the full suite after integration would have caught it.

Three fixes were returned this round because the **justification** was wrong
while the change was right: a barrier anchored at the renewal call's return
when the deadline is sampled before it; a cancellation recorded
unconditionally, silently turning `DrainNoWork` into `DrainFailed`; and,
earlier in the day, a fabricated `fsync(2)` citation supporting a correct
change. Checking the diff is not sufficient — the argument is what the next
person inherits.

Verification at `a1bfccbc`: `go test ./...` clean on Windows; `internal/
session`, `internal/app`, `internal/cmd` clean on real Linux without
`-short`; linux and darwin cross-builds clean; `gofmt` clean.

## Active goal

The /babygoal Stop hook: implement with `@sh` sub-agents and worktrees,
commit between tasks, then `/checkpoint`, update the changelog, commit the
markdown and run an `@oxx` review. The review ran once this round (it is
what produced this backlog); the closing sequence is running now.

## TaskList

### pending
- #601 crush-fallback sentinel — ACTIVE (hard-limit until 2026-08-20 17:01).
  Never complete or delete: it is state, not work.

### completed this round
#604, #605, #606, #607, #608, #609 — plus the whole fourth/fifth-review
backlog earlier in the session (#593–#603).

## Decisions

- **Full unification of the drain's eight exits was rejected, with the
  reason recorded.** Two already record a more specific cause, and the
  ledger reports the most recent failure, so adding `ctx.Err()` there would
  mask the better diagnosis. That needs a priority rule inside the ledger,
  not a substitution.
- **The holder reap became a parameter, not a default.** Two call sites
  need it, two need the zombie window and are load-bearing, ten are
  indifferent. All 14 were read individually rather than pattern-matched,
  and each says which mode it needs and why.
- **The constraint match requires both a marker and `sessions.id`.**
  Swallowing a category was a defect shipped earlier in this same series; a
  regression test asserts a `messages.id` violation still surfaces raw.
- **No bound was widened anywhere.** #604 and #609 both fixed the mechanism;
  where a frequency could not be reproduced, that was stated and the
  measurement distribution offered instead of a pass count.
- **CI and pre-push now run the fence suites without `-short`.** On Windows
  the hook says out loud that it is skipping them, because the `!windows`
  tag means there is nothing to run and a silent pass would be a lie.

## Open questions

- Nothing is pushed. Six reviews, six NO-GO verdicts; the sixth's blocker is
  closed but no seventh review has run against `a1bfccbc`.
- The ~40% watchdog rate and the 7-in-15 measurement failures could not be
  reproduced in the sessions that fixed them. Both fixes are justified by
  mechanism, proven by instrumentation, not by a before/after delta.
- `crush` delegation is unavailable until 2026-08-20 17:01; `@sh` carried
  this entire round. `dev/crush-local.exe` (built from `8393fa95`) is ready
  for when the limit resets.

## Repo state

```
 D web/dist/.gitkeep
?? dev/
```

```
a1bfccbc docs(changelog): the sixth review and the round that closed it
929f3bb8 test(session): anchor the watchdog measurement where the deadline is sampled
f4d19ca8 fix(app): a lost session-creation race reports busy, not a raw SQLite constraint error
b7d50642 fix(session): fire the lease watchdog on the true deadline, not the DB's rounded one
75c5ed52 test(cmd),ci: make the holder reap opt-in, and mirror the fence suites in pre-push
```
